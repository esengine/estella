node_modules
dist
dist-electron
release
*.log
.DS_Store

# Scratch bundles: a headless run of an electron-only module esbuilds it to .mjs
# in a temp directory here (inside desktop/, so its `import 'esbuild'` resolves),
# and a run that dies before its cleanup leaves the directory behind.
.dl-*/
.export-*/

# Synced from the engine/SDK build (build-tools/tasks/sync.js) — generated, not source.
public/sdk/
public/wasm/
public/wasm-wechat/
