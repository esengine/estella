---
title: UI components
description: "The UI box model, its visuals, and the widgets built on top of them."
tableOfContents: false
---

import ComponentReference from '@/components/ComponentReference.astro';

Every field below is derived from the engine's own component registry — the same
one the editor's **Details** panel reads — so the types and defaults here are the
values you get when you add the component, and they cannot drift from the code.
The prose that explains *how to use* each one lives in the guides linked per entry.

<ComponentReference category="ui" />
