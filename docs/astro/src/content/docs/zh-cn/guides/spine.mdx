---
title: Spine 动画
description: 用 SpineAnimation 组件播放 Spine 骨骼动画。
---

import { Aside } from '@astrojs/starlight/components';

Estella 内置 [Spine](https://esotericsoftware.com/) 骨骼动画,由 `SpineAnimation`
组件驱动。

## `SpineAnimation` 组件

通常你在编辑器里添加它并指向你的 Spine 资源。它的字段:

- `skeletonPath`、`atlasPath` —— 要加载的骨骼与图集。
- `animation`、`loop`、`playing` —— 当前轨道及其播放状态。
- `skin`、`timeScale`、`skeletonScale`、`flipX`、`flipY`、`color`、`layer`。

## 运行时控制

因为它就是个普通组件,你在系统里写它的字段即可改变播放——用 `Mut` 查询它:

```typescript
import { defineSystem, Query, Mut, SpineAnimation } from 'esengine';

const play = defineSystem([Query(Mut(SpineAnimation))], (q) => {
  for (const [entity, spine] of q) {
    spine.animation = 'walk';
    spine.loop = true;
    spine.playing = true;
  }
});
```

## 动画事件

`SpineEvents` 资源每帧发布一次轨道事件——在同一帧里读取它:

```typescript
import { defineSystem, Res, SpineEvents } from 'esengine';

const onSpine = defineSystem([Res(SpineEvents)], (spineEvents) => {
  for (const e of spineEvents.events) {
    // e.type: 'start' | 'interrupt' | 'end' | 'complete' | 'event'
    if (e.type === 'complete') { /* e.entity, e.animationName */ }
  }
});
```

<Aside type="note">
  Estella 支持 Spine **3.8 / 4.1 / 4.2** —— 每个骨骼版本按需加载各自的 WebAssembly
  后端(用到才加载)。共享同一套骨骼 + 图集的实体共享同一份加载后、引用计数的骨骼。
</Aside>

<Aside type="caution">
  随附的 Spine Runtimes 是专有软件。发布使用 Spine 的游戏需要 Esoteric Software 的
  有效 Spine 授权——与 Estella 自身的许可证相互独立。
</Aside>
