// Everything is authored in the scene: the prefab carries the mesh, its skin and
// the clip that waves it, and the two lights are entities. A game that only
// lights and draws needs no code — the packager just looks for this file.
export {};
