#pragma shader "DecalTarget"
#pragma version 300 es
#pragma domain Lit
#pragma param u_tint color default(1,0.32,0.12,1)

#pragma fragment
precision mediump float;

in vec4 v_color;
in vec2 v_texCoord;

out vec4 fragColor;

void main() {
    // A target: the SHAPE is what says the decal took the projector's
    // coordinates, which a solid patch of colour would not.
    vec2 d = v_texCoord - vec2(0.5);
    float r = length(d) * 2.0;
    float ring = step(0.62, r) * step(r, 0.86);
    float bar = max(step(abs(d.x), 0.035), step(abs(d.y), 0.035)) * step(r, 0.95);
    if (max(ring, bar) < 0.5) discard;
    fragColor = vec4(u_tint.rgb, 1.0);
}
#pragma end

#pragma fragment wgsl full
// GENERATED by tools/gen-shader-twins.mjs (glslang + naga) - do not edit.
// Regenerate after changing the GLSL stages above.
// source-hash: 144f76db1f5f521a
// assembled-hash: 90ffbdaa6f588209
struct MaterialConstants {
    u_tint: vec4<f32>,
}

struct FrameConstants {
    u_projection: mat4x4<f32>,
    u_camera: vec4<f32>,
    u_inverseViewProjection: mat4x4<f32>,
}

struct TimeConstants {
    u_time: vec4<f32>,
    u_viewport: vec4<f32>,
}

struct Light {
    posDir: vec4<f32>,
    color: vec4<f32>,
    spot: vec4<f32>,
    shadow: vec4<f32>,
    shadowMap: vec4<f32>,
    falloff: vec4<f32>,
}

struct LightConstants {
    u_ambient: vec4<f32>,
    u_lights: array<Light, 16>,
    u_mask2DRect: vec4<f32>,
    u_shadowMatrix: array<mat4x4<f32>, 16>,
    u_shadowTile: array<vec4<f32>, 16>,
    u_shadowParams: vec4<f32>,
    u_envIrradiance: array<vec4<f32>, 9>,
    u_envParams: vec4<f32>,
    u_envTint: vec4<f32>,
    u_reflParams: vec4<f32>,
    u_reflBox: array<vec4<f32>, 30>,
}

struct ProbeConstants {
    u_probeIrradiance: array<vec4<f32>, 1000>,
    u_probeTail: vec4<f32>,
}

var<private> v_texCoord_1: vec2<f32>;
var<private> fragColor: vec4<f32>;
@group(0) @binding(1) 
var<uniform> unnamed: MaterialConstants;
@group(0) @binding(0) 
var<uniform> unnamed_1: FrameConstants;
@group(0) @binding(3) 
var<uniform> unnamed_2: TimeConstants;
@group(0) @binding(2) 
var<uniform> unnamed_3: LightConstants;
@group(0) @binding(7) 
var<uniform> unnamed_4: ProbeConstants;
var<private> v_probeSlot_1: f32;
@group(1) @binding(7) 
var u_shadow2D_est: texture_2d<f32>;
@group(1) @binding(15) 
var u_shadow2D_ess: sampler;
@group(1) @binding(6) 
var u_lightShape2D_est: texture_2d<f32>;
@group(1) @binding(14) 
var u_lightShape2D_ess: sampler;
var<private> v_color_1: vec4<f32>;

fn main_1() {
    var d: vec2<f32>;
    var r: f32;
    var ring: f32;
    var bar: f32;

    let _e33 = v_texCoord_1;
    d = (_e33 - vec2<f32>(0.5f, 0.5f));
    let _e35 = d;
    r = (length(_e35) * 2f);
    let _e38 = r;
    let _e40 = r;
    ring = (step(0.62f, _e38) * step(_e40, 0.86f));
    let _e44 = d[0u];
    let _e48 = d[1u];
    let _e52 = r;
    bar = (max(step(abs(_e44), 0.035f), step(abs(_e48), 0.035f)) * step(_e52, 0.95f));
    let _e55 = ring;
    let _e56 = bar;
    if (max(_e55, _e56) < 0.5f) {
        discard;
    }
    let _e60 = unnamed.u_tint;
    let _e61 = _e60.xyz;
    fragColor = vec4<f32>(_e61.x, _e61.y, _e61.z, 1f);
    return;
}

@fragment 
fn fs_main(@location(1) v_texCoord: vec2<f32>, @location(6) @interpolate(flat) v_probeSlot: f32, @location(0) v_color: vec4<f32>) -> @location(0) vec4<f32> {
    v_texCoord_1 = v_texCoord;
    v_probeSlot_1 = v_probeSlot;
    v_color_1 = v_color;
    main_1();
    let _e7 = fragColor;
    return _e7;
}
#pragma end
