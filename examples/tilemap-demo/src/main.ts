// Tilemap Demo — the level is authored in the scene (assets/scenes/main.esscene)
// as a TilemapLayer that references the Kenney tileset atlas, and it is rendered by
// the engine's built-in tilemap plugin. Displaying it needs no gameplay systems, so
// this entry point is intentionally empty; add systems here to make the map interactive.
export {};
