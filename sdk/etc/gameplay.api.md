# API surface — esengine/gameplay

<!-- Generated by tools/api-surface.mjs — do not edit. Run --update to accept changes. -->
Symbols: 0 public · 0 beta · 50 experimental · 0 deprecated

## ATTACK_KEY — const @experimental
```
"KeyJ"
```

## COMBAT_ATTACK_END — const @experimental
```
"attack-end"
```

## COMBAT_ATTACK_START — const @experimental
```
"attack-start"
```

## COMBAT_HIT — const @experimental
```
"hit"
```

## DODGE_KEY — const @experimental
```
"ShiftLeft"
```

## Damage — const @experimental
```
EventDef<DamagePayload>
```

## DamagePayload — interface @experimental
```
amount: number
source: number
target: number
x: number
y: number
z: number
```

## GameplayPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): GameplayPlugin
static prototype: GameplayPlugin
```

## Health — const @experimental
```
ComponentDef<HealthData>
```

## HealthData — interface @experimental
```
current: number
max: number
```

## Hunter — const @experimental
```
ComponentDef<HunterData>
```

## HunterData — interface @experimental
```
attackInterval: number
attackRange: number
cooldown: number
enabled: boolean
idleThreshold: number
rotationSpeed: number
state: string
```

## HunterSituation — interface @experimental
```
alive: boolean
attackRange: number
distance: number
visible: boolean
```

## HunterState — type @experimental
```
'idle' | 'chase' | 'attack' | 'dead'
```

## MeleeAttack — const @experimental
```
ComponentDef<MeleeAttackData>
```

## MeleeAttackData — interface @experimental
```
anchor: number
attackId: number
damage: number
enabled: boolean
hitCount: number
layers: number
radius: number
```

## MeleeAttacks — class @experimental
```
clear: () => void
close: (entity: Entity) => void
forget: (entity: Entity) => void
live: (entity: Entity) => { id: number; hits: number; } | null
open: (entity: Entity) => number
record: (entity: Entity, target: Entity) => boolean
static new (): MeleeAttacks
static prototype: MeleeAttacks
```

## MeleeOverlapQuery — interface @experimental
```
overlapSphere: (centre: { x: number; y: number; z: number; }, radius: number, layerMask?: number) => readonly Overlap3DHit[]
```

## MoveBasis — interface @experimental
```
forward: Vec3
right: Vec3
```

## TPC_ATTACK — const @experimental
```
"attack"
```

## TPC_DODGE — const @experimental
```
"dodge"
```

## TPC_GROUNDED — const @experimental
```
"grounded"
```

## TPC_JUMP — const @experimental
```
"jump"
```

## TPC_SPEED — const @experimental
```
"speed"
```

## ThirdPersonCamera — const @experimental
```
ComponentDef<ThirdPersonCameraData>
```

## ThirdPersonCameraData — interface @experimental
```
distance: number
enabled: boolean
followDamping: number
maxPitch: number
minPitch: number
obstruction: boolean
obstructionLayers: number
obstructionRadius: number
pitch: number
sensitivity: number
target: number
targetOffset: Vec3
yaw: number
```

## ThirdPersonController — const @experimental
```
ComponentDef<ThirdPersonControllerData>
```

## ThirdPersonControllerData — interface @experimental
```
acceleration: number
airControl: number
camera: number
cameraRelative: boolean
deceleration: number
enabled: boolean
idleThreshold: number
jumpSpeed: number
moveSpeed: number
rotationSpeed: number
```

## WORLD_BASIS — const @experimental
```
MoveBasis
```

## applyDamage — function @experimental
```
(world: World, blows: Iterable<DamagePayload>): void
```

## approachVelocity — function @experimental
```
(current: { x: number; z: number; }, target: { x: number; z: number; }, acceleration: number, deceleration: number, dt: number): { x: number; z: number; }
```

## cameraGroundBasis — function @experimental
```
(yawDeg: number): { forward: Vec3; right: Vec3; }
```

## clampPitch — function @experimental
```
(pitch: number, min: number, max: number): number
```

## dampFactor — function @experimental
```
(damping: number, dt: number): number
```

## decideHunterState — function @experimental
```
(s: HunterSituation): HunterState
```

## desiredDirection — function @experimental
```
(input: { x: number; y: number; }, basis: MoveBasis): Vec3
```

## facingYaw — function @experimental
```
(direction: Vec3): number
```

## gameplayPlugin — const @experimental
```
GameplayPlugin
```

## huntTargets — function @experimental
```
(world: World, animator: AnimatorControllerAPI | null, attackTrigger: string, dt: number): void
```

## observeMotion — function @experimental
```
(world: World, animator: AnimatorControllerAPI | null, dt: number): void
```

## orbitOffset — function @experimental
```
(yawDeg: number, pitchDeg: number): Vec3
```

## requestMotion — function @experimental
```
(world: World, input: InputState, dt: number, animator?: AnimatorControllerAPI | null): void
```

## resolveMeleeHits — function @experimental
```
(world: World, attacks: MeleeAttacks, queries: MeleeOverlapQuery | null, events: Iterable<AnimatorEventPayload>, damage: { send(blow: DamagePayload): void; }): void
```

## restoreToFull — function @experimental
```
(world: World, entity: Entity): void
```

## rootMotionVelocity — function @experimental
```
(delta: { x: number; z: number; }, seconds: number): { x: number; z: number; }
```

## shortestAngleDelta — function @experimental
```
(from: number, to: number): number
```

## turnToward — function @experimental
```
(from: number, to: number, degreesPerSecond: number, dt: number): number
```

## updateCameras — function @experimental
```
(world: World, input: InputState, queries: Physics3DQueries | null, dt: number): void
```

## yawOfQuaternion — function @experimental
```
(q: { w: number; y: number; }): number
```

## yawQuaternion — function @experimental
```
(yawDeg: number): { w: number; x: number; y: number; z: number; }
```

