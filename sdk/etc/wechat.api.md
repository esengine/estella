# API surface — esengine/wechat

<!-- Generated by tools/api-surface.mjs — do not edit. Run --update to accept changes. -->
Symbols: 111 public · 51 beta · 1356 experimental · 3 deprecated

## ANCHOR_AXES — const @experimental
```
readonly AnchorAxis[]
```

## ANIM_CLIP_FORMAT_VERSION — const @experimental
```
"1.4"
```

## ASSET_GROUP_MODES — const @experimental
```
readonly ["local", "subpackage", "remote"]
```

## AchievementProvider — interface @experimental
```
getStat: (name: string) => number
platformBacked: boolean
reset: () => Promise<void>
setStat: (name: string, value: number) => void
store: () => Promise<void>
unlock: (id: string) => Promise<void>
unlocked: (id: string) => boolean
```

## Achievements — const @experimental
```
ResourceDef<AchievementsAPI>
```

## AchievementsAPI — class @experimental
```
available: boolean
getStat: (name: string) => number
reset: () => Promise<void>
setKnown: (ids: readonly string[] | null) => void
setProvider: (provider: AchievementProvider | null) => void
setStat: (name: string, value: number) => void
store: () => Promise<void>
unlock: (id: string) => Promise<void>
unlocked: (id: string) => boolean
static new (): AchievementsAPI
static prototype: AchievementsAPI
```

## ActionDef — interface @public
```
bindings: Binding[]
type: ActionType
```

## ActionType — type @public
```
'button' | 'axis' | 'axis2d'
```

## ActivationTrack — interface @experimental
```
childPath: string
name: string
ranges: ActivationRange[]
type: "activation"
```

## AdProvider — interface @experimental
```
createInterstitialAd: (adUnitId: string) => PlatformInterstitialAd
createRewardedAd: (adUnitId: string) => PlatformRewardedAd
```

## Added — function @experimental
```
<T extends AnyComponentDef>(component: T): AddedWrapper<T>
```

## AddedEntity — interface @experimental
```
components: ComponentData[]
name: string
parentId: string | null
prefabEntityId: string
visible: boolean
```

## AddedWrapper — interface @public
```
@internal _component: T
@internal _filterType: "added"
```

## AddressableAssetType — type @experimental
```
| 'texture' | 'material' | 'spine' | 'bitmap-font' | 'font'
    | 'prefab' | 'json' | 'text' | 'binary' | 'audio'
```

## AddressableManifest — interface @experimental
```
groups: Record<string, AddressableManifestGroup>
revision: string | undefined
version: "2.0"
```

## AddressableManifestAsset — interface @experimental
```
address: string | undefined
compressedFormats: string[] | undefined
contentHash: string | undefined
labels: string[]
metadata: { atlas?: string; atlasPage?: number; atlasFrame?: { x: number; y: number; width: number; height: number; }; atlasPageWidth?: number; atlasPageHeight?: number; } | undefined
path: string
size: number
textureImport: ParsedTextureImportSettings | undefined
type: AddressableAssetType
```

## AddressableManifestGroup — interface @experimental
```
assets: Record<string, AddressableManifestAsset>
bundleMode: string
labels: string[]
```

## Ads — const @experimental
```
ResourceDef<AdsAPI>
```

## AdsAPI — class @experimental
```
available: boolean
preloadRewarded: (adUnitId: string) => Promise<void>
setProvider: (provider: AdProvider | null) => void
showInterstitial: (adUnitId: string) => Promise<void>
showRewarded: (adUnitId: string) => Promise<PlatformRewardedAdResult>
showing: boolean
static new (takeover: Takeover): AdsAPI
static prototype: AdsAPI
```

## AiAction — type @experimental
```
(
    ctx: Ctx,
    bb: Blackboard,
    arg?: string,
    params?: AiParams,
) => void | Status
```

## AiActionInput — interface @experimental
```
arg: string | undefined
params: Readonly<Record<string, AiParamValue>> | undefined
```

## AiActionSpec — interface @experimental
```
params: readonly AiParamDef[] | undefined
run: AiAction<Ctx>
separator: string | undefined
```

## AiBt — const @experimental
```
ResourceDef<BehaviorTrees>
```

## AiCondition — type @experimental
```
(ctx: Ctx, bb: Blackboard) => boolean
```

## AiContext — interface @experimental
```
blackboard: Blackboard
commands: CommandsInstance
dt: number
entity: number
get: <C extends AnyComponentDef>(component: C) => ComponentData<C>
has: (component: AnyComponentDef) => boolean
set: <C extends AnyComponentDef>(component: C, data: ComponentData<C>) => void
world: World
```

## AiFsm — const @experimental
```
ResourceDef<StateMachines>
```

## AiParamDef — interface @experimental
```
label: string | undefined
max: number | undefined
min: number | undefined
name: string
options: readonly { label: string; value: string; }[] | undefined
optionsSource: string | undefined
step: number | undefined
tooltip: string | undefined
type: "string" | "number" | "bool" | "enum"
```

## AiParamValue — type @experimental
```
string | number | boolean
```

## AiParams — type @experimental
```
Readonly<Record<string, AiParamValue>>
```

## AiRegistry — class @experimental
```
actionNames: () => string[]
clear: () => void
conditionNames: () => string[]
getAction: (name: string) => AiAction<Ctx> | undefined
getActionParams: (name: string) => readonly AiParamDef[]
getActionSeparator: (name: string) => string
getCondition: (name: string) => AiCondition<Ctx> | undefined
hasAction: (name: string) => boolean
hasCondition: (name: string) => boolean
registerAction: (name: string, fn: AiAction<Ctx> | AiActionSpec<Ctx>) => void
registerCondition: (name: string, fn: AiCondition<Ctx>) => void
static new <Ctx = unknown>(): AiRegistry<Ctx>
static prototype: AiRegistry<any>
```

## AlignContent — type @experimental
```
(typeof AlignContent)[keyof typeof AlignContent]
```

## AlignItems — type @experimental
```
(typeof AlignItems)[keyof typeof AlignItems]
```

## AlignSelf — enum @experimental
```
Auto = 0
Center = 2
End = 3
Start = 1
Stretch = 4
```

## AnchorAxis — enum @experimental
```
Center = 1
End = 2
Start = 0
Stretch = 3
```

## AnchorFields — interface @experimental
```
height: Dimension | undefined
insetBottom: Dimension
insetLeft: Dimension
insetRight: Dimension
insetTop: Dimension
marginBottom: Dimension
marginLeft: Dimension
marginRight: Dimension
marginTop: Dimension
position: number
width: Dimension | undefined
```

## AnchorPreset — interface @experimental
```
h: AnchorAxis
v: AnchorAxis
```

## And — function @experimental
```
(...filters: FilterExpr[]): FilterExpr
```

## AnimClipAssetData — interface @experimental
```
events: AnimClipEventData[] | undefined
fps: number | undefined
frames: AnimClipFrameData[]
loop: boolean | undefined
pivot: AnimClipPivotData | undefined
sheet: AnimClipSheetData | undefined
type: "animation-clip"
version: string
```

## AnimClipEventData — interface @experimental
```
data: unknown
frame: number
name: string
```

## AnimClipFrameData — interface @experimental
```
atlasFrame: { x: number; y: number; width: number; height: number; pageWidth: number; pageHeight: number; } | undefined
cell: number | undefined
duration: number | undefined
pivot: AnimClipPivotData | undefined
texture: string | undefined
```

## AnimClipPivotData — interface @experimental
```
x: number
y: number
```

## AnimClipSheetData — interface @experimental
```
cellHeight: number
cellWidth: number
margin: number
pageHeight: number
pageWidth: number
spacing: number
texture: string
```

## AnimFramesTrack — interface @experimental
```
childPath: string
frames: AnimFrame[]
name: string
type: "animFrames"
```

## AnimationPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): AnimationPlugin
static prototype: AnimationPlugin
```

## Animator — const @experimental
```
ComponentDef<AnimatorData>
```

## AnimatorBlend1D — interface @experimental
```
parameter: string
thresholds: AnimatorBlendThreshold[]
```

## AnimatorBlendThreshold — interface @experimental
```
clip: string
loop: boolean | undefined
speed: number | undefined
value: number
```

## AnimatorCondition — type @experimental
```
| { param: string; op: 'gt' | 'lt' | 'eq' | 'neq'; value: number }
    | { param: string; op: 'true' | 'false' | 'trigger' }
```

## AnimatorController — const @experimental
```
ResourceDef<AnimatorControllerAPI>
```

## AnimatorControllerAPI — class @experimental
```
clearControllers: () => void
getBool: (entity: Entity, name: string) => boolean
getController: (name: string) => AnimatorControllerDef | undefined
getFloat: (entity: Entity, name: string) => number
registerController: (name: string, def: AnimatorControllerDef) => void
removeEntity: (entity: Entity) => void
resetTrigger: (entity: Entity, name: string) => void
setBool: (entity: Entity, name: string, value: boolean) => void
setFloat: (entity: Entity, name: string, value: number) => void
setSpineDriver: (driver: SpineAnimationDriver | null) => void
setTrigger: (entity: Entity, name: string) => void
unregisterController: (name: string) => void
update: (world: World, resolveKey?: (ref: string) => string) => void
static new (): AnimatorControllerAPI
static prototype: AnimatorControllerAPI
```

## AnimatorControllerDef — interface @experimental
```
anyStateTransitions: AnimatorTransition[] | undefined
initialState: string
parameters: AnimatorParam[]
states: AnimatorState[]
```

## AnimatorData — interface @experimental
```
controller: string
currentState: string
enabled: boolean
```

## AnimatorEdge — type @experimental
```
from: string
id: string
index: number
to: string
transition: AnimatorTransition
```

## AnimatorEvalResult — interface @experimental
```
consumedTriggers: string[]
next: string | null
```

## AnimatorParam — interface @experimental
```
default: number | boolean | undefined
name: string
type: AnimatorParamType
```

## AnimatorParamType — type @experimental
```
'float' | 'bool' | 'trigger'
```

## AnimatorParamValues — type @experimental
```
Readonly<Record<string, number | boolean>>
```

## AnimatorPathEvalResult — interface @experimental
```
consumedTriggers: string[]
nextPath: string | null
```

## AnimatorScope — type @experimental
```
Pick<AnimatorControllerDef, 'states' | 'initialState' | 'anyStateTransitions'>
```

## AnimatorSpineMotion — interface @experimental
```
animation: string
loop: boolean | undefined
```

## AnimatorState — interface @experimental
```
blend: AnimatorBlend1D | undefined
clip: string | undefined
loop: boolean | undefined
name: string
speed: number | undefined
spine: AnimatorSpineMotion | undefined
stateMachine: AnimatorSubMachine | undefined
transitions: AnimatorTransition[]
x: number | undefined
y: number | undefined
```

## AnimatorSubMachine — interface @experimental
```
anyStateTransitions: AnimatorTransition[] | undefined
initialState: string
states: AnimatorState[]
```

## AnimatorTransition — interface @experimental
```
conditions: AnimatorCondition[]
hasExitTime: boolean | undefined
to: string
```

## AnyComponentDef — type @public
```
ComponentDef<any> | BuiltinComponentDef<any>
```

## App — class @experimental
```
@internal addBundleSystems: (entries: ReadonlyArray<{ schedule: number; system: SystemDef | SystemSet; }>) => App
@internal connectCpp: (cppRegistry: CppRegistry, module?: ESEngineModule, options?: BridgeConnectOptions) => App
@internal wasmModule: ESEngineModule | null
addEvent: <T>(event: EventDef<T>) => App
addPlugin: (plugin: Plugin) => App
addPlugins: (plugins: Plugin[]) => App
addStartupSystem: (system: SystemDef) => App
addSystem: (system: SystemDef) => App
addSystemSet: (set: SystemSet) => App
addSystemSetToSchedule: (schedule: Schedule, set: SystemSet) => App
addSystemToSchedule: (schedule: Schedule, system: SystemDef, options?: { runBefore?: string[]; runAfter?: string[]; runIf?: RunCondition; }) => App
enableStats: () => App
getEntityCount: () => number
getFixedTimestep: () => number
getFrameScopes: () => ReadonlyMap<string, number> | null
getPhaseTimings: () => ReadonlyMap<string, number> | null
getPlaySpeed: () => number
getPlugin: <T extends Plugin>(ctor: new (...args: any[]) => T) => T | undefined
getRegisteredResourceNames: () => string[]
getResource: <T>(resource: ResourceDef<T>) => T
getResourceByName: (name: string) => unknown | undefined
getResourceChangeTick: (name: string) => number
getSystemTimings: () => ReadonlyMap<string, number> | null
getTargetFrameRate: () => number
hasResource: <T>(resource: ResourceDef<T>) => boolean
hotSwapSystems: (incoming: ReadonlyArray<{ schedule: number; system: SystemDef; }>) => boolean
insertResource: <T>(resource: ResourceDef<T>, value: T) => App
isPaused: () => boolean
isPhysicsReady: boolean
measureFrameScope: <T>(name: string, fn: () => T) => T
onError: (handler: (error: unknown, systemName: string) => void) => App
onSystemError: (handler: (error: Error, systemName?: string) => "continue" | "pause") => App
onWasmError: (handler: (error: unknown, context: string) => void) => App
pipeline: RenderPipeline | null
quit: (options?: { keepRenderer?: boolean; }) => void
registerScene: (config: SceneConfig) => App
removeSystem: (systemId: symbol) => boolean
run: () => Promise<void>
setFixedTimestep: (timestep: number) => App
setInitialScene: (name: string) => App
setMaxDeltaTime: (v: number) => App
setMaxFixedSteps: (v: number) => App
setPaused: (paused: boolean) => void
setPipeline: (pipeline: RenderPipeline) => void
setPlaySpeed: (speed: number) => void
setSideModules: (host: SideModuleHost) => void
setTargetFrameRate: (fps: number) => void
sideModules: SideModuleHost | null
spineInitPromise: Promise<unknown> | undefined
stepFrame: () => void
stepFrames: (frames?: number, dt?: number) => Promise<void>
subsystems: SubsystemRegistry
tick: (delta: number) => Promise<void>
waitForPhysics: () => Promise<void>
world: World
static new (): App
static new: () => App
static prototype: App
```

## AppContext — class @experimental
```
componentRegistry: Map<string, any>
drainPendingSystems: () => PendingSystemEntry[]
editorBridge: EditorBridge | null
editorMode: boolean
pendingSystems: PendingSystemEntry[]
playMode: boolean
reset: () => void
wasmError: WasmErrorState
static new (): AppContext
static prototype: AppContext
```

## ApplyTilesetRefs — type @experimental
```
(entity: number, refs: readonly string[]) => void
```

## ArrayDataSource — class @experimental
```
append: (items: readonly T[]) => void
getCount: () => number
getItem: (index: number) => T
insert: (index: number, items: readonly T[]) => void
remove: (index: number, count?: number) => void
setItems: (items: readonly T[]) => void
subscribe: (listener: (c: DataSourceChange) => void) => Unsubscribe
update: (index: number, item: T) => void
static new <T>(items?: readonly T[]): ArrayDataSource<T>
static prototype: ArrayDataSource<any>
```

## AssetBuildTransform — type @experimental
```
(content: string, context: unknown) => string
```

## AssetContentType — type @experimental
```
'json' | 'text' | 'binary' | 'image' | 'audio'
```

## AssetFieldMeta — interface @public
```
field: string
type: AssetFieldType
```

## AssetFieldType — type @public
```
'texture' | 'material' | 'font' | 'anim-clip' | 'audio' | 'video' | 'tilemap' | 'tileset' | 'timeline' | 'statemachine' | 'behaviortree' | 'animatorcontroller'
```

## AssetGroupDef — interface @experimental
```
alwaysInclude: boolean | undefined
folder: string
mode: "local" | "subpackage" | "remote"
```

## AssetGroupMode — type @experimental
```
typeof ASSET_GROUP_MODES[number]
```

## AssetGroupsConfig — interface @experimental
```
activeProfile: string | undefined
atlases: Record<string, AtlasDef> | undefined
groups: Record<string, AssetGroupDef> | undefined
profiles: Record<string, BuildProfile> | undefined
version: string
```

## AssetPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): AssetPlugin
static prototype: AssetPlugin
```

## AssetRef — interface @public
```
path: string
type: string
```

## AssetRefCounter — class @experimental
```
addFontRef: (path: string, entity: number) => void
addMaterialRef: (path: string, entity: number) => void
addTextureRef: (path: string, entity: number) => void
clear: () => void
getAllFontRefs: () => AssetRefInfo[]
getAllMaterialRefs: () => AssetRefInfo[]
getAllTextureRefs: () => AssetRefInfo[]
getFontRefCount: (path: string) => number
getFontRefs: (path: string) => number[]
getMaterialRefCount: (path: string) => number
getMaterialRefs: (path: string) => number[]
getTextureRefCount: (path: string) => number
getTextureRefs: (path: string) => number[]
getTotalRefCount: () => { textures: number; fonts: number; materials: number; }
getTotalRefRows: () => number
removeAllRefsForEntity: (entity: number) => void
removeFontRef: (path: string, entity: number) => void
removeMaterialRef: (path: string, entity: number) => void
removeTextureRef: (path: string, entity: number) => void
static new (): AssetRefCounter
static prototype: AssetRefCounter
```

## AssetRefInfo — interface @experimental
```
assetPath: string
entities: number[]
refCount: number
```

## AssetTypeEntry — interface @experimental
```
addressableType: AddressableAssetType | null
buildTransform: AssetBuildTransform | undefined
contentType: AssetContentType
editorType: EditorAssetType
extensions: string[]
hasTransitiveDeps: boolean
suffixes: string[] | undefined
wechatPackInclude: boolean
```

## Assets — const @beta
```
ResourceDef<AssetsClass>
```

## AssetsData — type @beta
```
@internal releaseTyped: (type: string, ref: string) => void
@internal sizes: () => { textureCached: number; pendingLoads: number; refCounts: number; refRows: number; genericCaches: number; genericCached: number; handlePaths: number; invalidateListeners: number; registryEntries: number; trackedRefRows: number; }
applyUpdate: (onProgress?: (loaded: number, total: number) => void) => Promise<ApplyUpdateResult>
backend: Backend
baseUrl: string | undefined
catalog: Catalog
checkForUpdate: (options: CheckForUpdateOptions) => Promise<UpdatePlan>
fetchBinary: (ref: string) => Promise<ArrayBuffer>
fetchJson: <T = unknown>(ref: string) => Promise<T>
fetchText: (ref: string) => Promise<string>
getAssetRefResolver: () => AssetRefResolver | null
getAssetRegistry: () => AssetRegistry | null
getAtlasFrame: (ref: string) => AtlasFrameInfo | null
getLoader: <T>(type: string) => AssetLoader<T> | undefined
getManifest: () => ManifestModel | null
getRefCounter: () => AssetRefCounter | null
getSpineLoader: () => SpineAssetLoader
getTexture: (ref: string) => TextureResult | undefined
getTextureLoader: () => TextureLoader
invalidate: (ref: string) => boolean
load: <T>(type: string, ref: string) => Promise<T>
loadAnimClip: (ref: string) => Promise<AnimClipResult>
loadAnimatorController: (ref: string) => Promise<AnimatorControllerResult>
loadAudio: (ref: string) => Promise<AudioResult>
loadBehaviorTree: (ref: string) => Promise<BtResult>
loadByLabel: (label: string, onProgress?: (loaded: number, total: number) => void) => Promise<AssetBundle>
loadFont: (ref: string) => Promise<FontResult>
loadGroup: (groupName: string, onProgress?: (loaded: number, total: number) => void) => Promise<AssetBundle>
loadJson: <T = unknown>(ref: string) => Promise<JsonResult<T>>
loadLocaleTable: (ref: string) => Promise<LocaleResult>
loadMaterial: (ref: string) => Promise<MaterialResult>
loadPrefab: (ref: string) => Promise<PrefabResult>
loadSpine: (skeletonRef: string, atlasRef?: string) => Promise<SpineResult>
loadStateMachine: (ref: string) => Promise<FsmResult>
loadTexture: (ref: string) => Promise<TextureResult>
loadTextureRaw: (ref: string) => Promise<TextureResult>
loadTilemap: (ref: string) => Promise<TilemapResult>
loadTileset: (ref: string) => Promise<TilesetResult>
loadTimeline: (ref: string) => Promise<TimelineResult>
onInvalidate: (listener: InvalidateListener) => () => void
pathForHandle: (kind: string, handle: number) => string | null
preload: (refs: ReadonlyArray<string>, onProgress?: (loaded: number, total: number) => void, options?: { readonly maxConcurrent?: number; }) => Promise<{ failed: MissingAsset[]; }>
preloadSceneAssets: (sceneData: SceneData, onProgress?: (loaded: number, total: number) => void, options?: { readonly maxConcurrent?: number; readonly skipSpine?: boolean; }) => Promise<SceneAssetResult>
recoverFromDeviceLoss: () => Promise<boolean>
register: <T>(loader: AssetLoader<T>) => void
releaseAll: () => void
releaseAnimClip: (ref: string) => void
releaseAssets: (byType: ReadonlyMap<string, ReadonlySet<string>>) => void
releaseAudio: (ref: string) => void
releaseFont: (ref: string) => void
releaseGroup: (groupName: string) => void
releaseMaterial: (handle: number) => void
releasePrefab: (ref: string) => void
releaseTexture: (ref: string) => void
releaseTilemap: (ref: string) => void
releaseTimeline: (ref: string) => void
remoteRoot: string | undefined
resolveLoadPath: (ref: string) => string
resolveRef: (ref: string) => string | null
resolveSceneAssetPaths: (sceneData: SceneData, result: SceneAssetResult) => void
restorePersistedUpdate: (key: string) => boolean
reuploadTexturesAfterDeviceLoss: () => Promise<number>
setAssetRefResolver: (resolver: AssetRefResolver) => void
setAssetRegistry: (registry: AssetRegistry) => void
setManifest: (manifest: AddressableManifest | ManifestModel | null) => void
setRefCounter: (counter: AssetRefCounter) => void
setRemoteRoot: (url: string | undefined) => void
setSpineController: (controller: SpineModuleController) => void
setTextureImportSettingsResolver: (resolver: TextureImportSettingsResolver | null) => void
static create: (options: AssetsOptions) => AssetsClass
static new (options: AssetsOptions): AssetsClass
static prototype: AssetsClass
```

## AsyncCache — class @experimental
```
@internal sizes: () => { cached: number; pending: number; failed: number; }
clear: () => void
clearAll: () => void
delete: (key: string) => boolean
entries: () => IterableIterator<[string, T]>
get: (key: string) => T | undefined
getOrLoad: (key: string, loader: () => Promise<T>, timeout?: number) => Promise<T>
has: (key: string) => boolean
invalidate: (key: string) => boolean
set: (key: string, value: T) => void
values: () => IterableIterator<T>
static new <T>(dispose_?: ((value: T) => void) | undefined): AsyncCache<T>
static prototype: AsyncCache<any>
```

## AtlasDef — interface @experimental
```
folder: string
```

## AttenuationModel — enum @experimental
```
Exponential = 2
Inverse = 1
Linear = 0
```

## Audio — const @beta
```
ResourceDef<AudioAPI>
```

## AudioAPI — class @experimental
```
baseUrl: string
bufferBudget: number
dispose: () => void
ensureBus: (name: string, parent?: string) => boolean
getBufferHandle: (url: string) => AudioBufferHandle | undefined
getBufferStats: () => AudioBufferStats
getBusDucking: (target: string) => BusDuckRule | null
getBusEffects: (busName: string) => BusEffectDef[]
getBusVolume: (busName: string) => number
getMasterVolume: () => number
getMusicVolume: () => number
getSFXVolume: () => number
getSpectrum: (out: Uint8Array) => boolean
getUIVolume: () => number
invalidateBuffer: (url: string) => boolean
isBusMuted: (busName: string) => boolean
muteBus: (busName: string, muted: boolean) => void
playBGM: (url: string, config?: { volume?: number; fadeIn?: number; crossFade?: number; }) => void
playSFX: (url: string, config?: { volume?: number; pitch?: number; pan?: number; priority?: number; }) => AudioHandle
playTrack: (url: string, config?: PlayConfig) => Promise<AudioHandle | null>
preload: (url: string) => Promise<void>
preloadAll: (urls: string[]) => Promise<void>
preloadFromData: (url: string, data: ArrayBuffer) => Promise<void>
releaseBuffer: (url: string) => void
resume: () => void
retainBuffer: (url: string) => boolean
setAssetResolver: (resolver: (url: string) => ArrayBuffer | null) => void
setBufferBudget: (bytes: number | null) => void
setBusDucking: (target: string, rule: BusDuckRule | null) => boolean
setBusEffects: (busName: string, effects: BusEffectDef[]) => boolean
setBusVolume: (busName: string, volume: number) => void
setMasterVolume: (volume: number) => void
setMusicVolume: (volume: number) => void
setRefResolver: (resolver: ((ref: string) => string) | null) => void
setSFXVolume: (volume: number) => void
setUIVolume: (volume: number) => void
stopAll: () => void
stopBGM: (fadeOut?: number) => void
suspend: () => void
trimBufferCache: () => number
updateDucking: () => void
updateFades: (dt: number) => void
static new (backend: PlatformAudioBackend, mixer?: AudioMixer | null): AudioAPI
static prototype: AudioAPI
```

## AudioBackendInitOptions — interface @experimental
```
initialPoolSize: number | undefined
mixerConfig: AudioMixerConfig | undefined
```

## AudioBufferHandle — interface @experimental
```
bytes: number | undefined
duration: number
id: number
```

## AudioBufferStats — interface @experimental
```
bufferBudget: number
bufferBytes: number
bufferCount: number
evictableCount: number
```

## AudioBus — class @experimental
```
addChild: (child: AudioBus) => void
connect: (destination: AudioBus | AudioNode) => void
duckTo: (level: number, timeConstant: number) => void
effects: BusEffectDef[]
input: GainNode
muted: boolean
name: string
node: GainNode
setEffects: (defs: BusEffectDef[]) => void
volume: number
static new (context: AudioContext, config: AudioBusConfig): AudioBus
static prototype: AudioBus
```

## AudioBusConfig — interface @experimental
```
muted: boolean | undefined
name: string
parent: string | undefined
volume: number | undefined
```

## AudioBusDecl — interface @experimental
```
duck: BusDuckRule | undefined
effects: BusEffectDef[] | undefined
muted: boolean | undefined
name: string
parent: string | undefined
volume: number | undefined
```

## AudioHandle — interface @experimental
```
currentTime: number
duration: number
id: number
isPlaying: boolean
onEnd: (() => void) | undefined
pause: () => void
resume: () => void
setLoop: (loop: boolean) => void
setPan: (pan: number) => void
setPlaybackRate: (rate: number) => void
setVolume: (volume: number) => void
stop: () => void
```

## AudioListener — const @experimental
```
ComponentDef<AudioListenerData>
```

## AudioListenerData — interface @experimental
```
enabled: boolean
```

## AudioMixer — class @experimental
```
busNames: () => string[]
createBus: (config: AudioBusConfig) => AudioBus
getBus: (name: string) => AudioBus | undefined
getDucking: (target: string) => BusDuckRule | null
master: AudioBus
music: AudioBus
setDucking: (target: string, rule: BusDuckRule | null) => boolean
sfx: AudioBus
ui: AudioBus
updateDucking: () => void
voice: AudioBus
static new (context: AudioContext, config?: AudioMixerConfig): AudioMixer
static prototype: AudioMixer
```

## AudioMixerConfig — interface @experimental
```
masterVolume: number | undefined
musicVolume: number | undefined
sfxVolume: number | undefined
uiVolume: number | undefined
voiceVolume: number | undefined
```

## AudioPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
stopAllSources: () => void
static new (config?: AudioPluginConfig): AudioPlugin
static prototype: AudioPlugin
```

## AudioPluginConfig — interface @experimental
```
initialPoolSize: number | undefined
masterVolume: number | undefined
musicVolume: number | undefined
sfxVolume: number | undefined
```

## AudioPool — class @experimental
```
acquire: () => PooledAudioNode
activeCount: number
capacity: number
release: (node: PooledAudioNode) => void
static new (context: AudioContext, initialSize?: number): AudioPool
static prototype: AudioPool
```

## AudioProjectConfig — interface @experimental
```
buses: AudioBusDecl[] | undefined
```

## AudioSource — const @experimental
```
ComponentDef<AudioSourceData>
```

## AudioSourceData — interface @experimental
```
attenuationModel: number
bus: string
clip: string
enabled: boolean
loop: boolean
maxDistance: number
minDistance: number
pitch: number
playOnAwake: boolean
priority: number
rolloff: number
spatial: boolean
volume: number
```

## AudioTrack — interface @experimental
```
childPath: string
events: AudioEvent[]
name: string
type: "audio"
```

## Axis1D — const @public
```
(...bindings: Binding[]) => ActionDef
```

## Axis2D — const @public
```
(...bindings: Binding[]) => ActionDef
```

## BUILTIN_SHADER_TEMPLATES — const @experimental
```
readonly BuiltinShaderTemplate[]
```

## BUILTIN_UI_PREFABS — const @experimental
```
Record<string, PrefabData>
```

## BUILTIN_UI_WIDGET_NAMES — const @experimental
```
readonly ["Button", "Toggle", "Slider", "Dialog", "TextInput", "Progress", "Dropdown", "ListView", "ScrollView"]
```

## BUNDLE_MODES — const @experimental
```
readonly BundleMode[]
```

## Backend — interface @experimental
```
fetchBinary: (path: string) => Promise<ArrayBuffer>
fetchText: (path: string) => Promise<string>
resolveUrl: (path: string) => string
setBaseUrl: ((url: string) => void) | undefined
```

## BehaviorContext — interface @experimental
```
commands: CommandsInstance
entity: number
get: <C extends AnyComponentDef>(component: C) => ComponentData<C>
has: (component: AnyComponentDef) => boolean
input: InputState
self: S
set: <C extends AnyComponentDef>(component: C, data: ComponentData<C>) => void
time: TimeData
world: World
```

## BehaviorDef — interface @experimental
```
destroy: ((ctx: BehaviorContext<S>) => void) | undefined
metadata: ComponentMetadata | undefined
schedule: Schedule | undefined
start: ((ctx: BehaviorContext<S>) => void) | undefined
state: S | undefined
update: ((ctx: BehaviorContext<S>, dt: number) => void) | undefined
```

## BehaviorTreeAgent — const @experimental
```
ComponentDef<BehaviorTreeAgentData>
```

## BehaviorTreeAgentData — interface @experimental
```
bt: string
status: string
```

## BehaviorTrees — class @experimental
```
blackboard: (entity: Entity) => Blackboard
status: (entity: Entity) => string | null
static new (states: Map<Entity, BtAgentState>): BehaviorTrees
static prototype: BehaviorTrees
```

## BezierPoints — interface @experimental
```
p1x: number
p1y: number
p2x: number
p2y: number
```

## BinaryHandler — type @experimental
```
(payload: Uint8Array) => void
```

## Binding — type @public
```
| { kind: 'key'; code: string }
    | { kind: 'mouse'; button: number }
    | { kind: 'gpButton'; button: number; pad?: number }
    | { kind: 'gpAxis'; axis: number; pad?: number; scale?: number }
    | { kind: 'keys1d'; neg: string; pos: string }
    | { kind: 'keys2d'; up: string; down: string; left: string; right: string }
    | { kind: 'stick'; stick: 'left' | 'right'; pad?: number }
    | { kind: 'virtual'; id: string }
```

## BitmapCache — interface @experimental
```
_rt: RenderTextureHandle | null
height: number
textureId: number
valid: boolean
width: number
```

## BitmapText — const @experimental
```
BuiltinComponentDef<BitmapTextData>
```

## BitmapTextData — interface @experimental
```
align: number
color: Color
enabled: boolean
font: number
fontSize: number
layer: number
parallax: Vec2
spacing: number
text: string
```

## Blackboard — class @experimental
```
clearTriggers: () => void
consume: (trigger: string) => void
delete: (key: string) => void
fire: (trigger: string) => void
get: <T = unknown>(key: string) => T | undefined
has: (key: string) => boolean
isFired: (trigger: string) => boolean
set: (key: string, value: unknown) => void
static new (): Blackboard
static prototype: Blackboard
```

## BlackboardGuard — interface @experimental
```
key: string
op: CompareOp
value: string | number | boolean | undefined
```

## BlendCurve — type @experimental
```
(typeof BlendCurve)[keyof typeof BlendCurve]
```

## BlendMode — enum @experimental
```
Additive = 1
Darken = 7
Lighten = 6
Multiply = 2
None = 9
Normal = 0
Overlay = 8
PmaAdditive = 5
PremultipliedAlpha = 4
Screen = 3
```

## BodyType — enum @experimental
```
Dynamic = 2
Kinematic = 1
Static = 0
```

## BoxCollider — const @experimental
```
BuiltinComponentDef<BoxColliderData>
```

## BoxColliderData — interface @experimental
```
categoryBits: number
density: number
enabled: boolean
friction: number
halfExtents: Vec2
isSensor: boolean
maskBits: number
offset: Vec2
radius: number
restitution: number
```

## BtDefinition — interface @experimental
```
orphans: BtNode[] | undefined
root: BtNode
```

## BtEdge — interface @experimental
```
from: string
id: string
to: string
```

## BtNode — interface @experimental
```
arg: string | undefined
children: BtNode[] | undefined
count: number | undefined
id: string | undefined
name: string | undefined
params: Record<string, AiParamValue> | undefined
policy: "all" | "one" | undefined
seconds: number | undefined
type: BtNodeType
x: number | undefined
y: number | undefined
```

## BtNodeType — type @experimental
```
| 'sequence' | 'selector' | 'parallel'
    // decorators (one child)
    | 'inverter' | 'succeeder' | 'repeater' | 'wait'
    // leaves
    | 'action' | 'condition'
```

## BtPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): BtPlugin
static prototype: BtPlugin
```

## BtRunState — type @experimental
```
Map<string, NodeState>
```

## BuildNavGridOptions — interface @experimental
```
blockedTileIds: Iterable<number> | undefined
cellSize: number
height: number
isBlocked: ((tileId: number) => boolean) | undefined
origin: Vec2 | undefined
width: number
```

## BuildProfile — interface @experimental
```
remoteRoot: string | undefined
```

## BuiltinComponentDef — interface @public
```
@internal _builtin: true
@internal _cppName: string
@internal _default: T
@internal _id: symbol
@internal _name: string
animatableFields: readonly string[]
assetFields: readonly AssetFieldMeta[]
colorKeys: readonly string[]
discoverAssets: ((data: Record<string, unknown>) => AssetRef[]) | undefined
entityFields: readonly string[]
fieldMeta: Readonly<Record<string, FieldMeta>>
readonlyFields: readonly string[]
renderableField: string | null
replicatedFields: readonly string[]
skeletalFields: SkeletalFieldMeta | undefined
transient: boolean
```

## BuiltinShaderTemplate — interface @experimental
```
defaults: Record<string, unknown>
description: string
id: string
label: string
source: string
```

## BuiltinSideModuleId — type @experimental
```
'physics' | 'basis' | 'videodec' | 'dragonbones' | `spine:${SpineVersion}`
```

## BundleMode — type @experimental
```
'local' | 'lazy' | 'remote'
```

## BusDuckRule — interface @experimental
```
amount: number
attack: number | undefined
release: number | undefined
threshold: number | undefined
trigger: string
```

## BusEffectDef — type @experimental
```
FilterEffectDef | ReverbEffectDef | CompressorEffectDef
```

## Button — const @public
```
(...bindings: Binding[]) => ActionDef
```

## ButtonHandle — interface @experimental
```
dispose: () => void
entity: number
setDisabled: (disabled: boolean) => void
```

## ButtonOptions — interface @experimental
```
background: UIVisualInit | undefined
disabled: boolean | undefined
events: UIEventQueue
fadeDuration: number | undefined
focusable: boolean | undefined
node: UINodeInit | undefined
onClick: ((entity: Entity) => void) | undefined
parent: number | undefined
states: Record<string, ButtonStateVisual> | undefined
tabIndex: number | undefined
text: string | TextInit | undefined
world: World
```

## ButtonStateVisual — interface @experimental
```
color: Color | undefined
scale: number | undefined
sprite: number | undefined
```

## CAPSULE_ARC_SEGMENTS — const @experimental
```
16
```

## CHUNK_SIZE — const @experimental
```
16
```

## COLLISION_BRUSHES — const @experimental
```
CollisionBrush[]
```

## COLLISION_PALETTE_REF — const @experimental
```
"builtin:collision"
```

## CacheAsBitmap — const @experimental
```
ComponentDef<CacheAsBitmapData>
```

## CacheAsBitmapData — interface @experimental
```
dirty: boolean
enabled: boolean
height: number
width: number
```

## CacheBitmap — const @experimental
```
{ create(width: number, height: number): BitmapCache; release(cache: BitmapCache): void; beginDraw(cache: BitmapCache, viewProjection: Float32Array): void; endDraw(): void; resize(cache: BitmapCache, width: number, height: number): void; }
```

## Camera — const @beta
```
BuiltinComponentDef<CameraData>
```

## CameraBounds — const @experimental
```
ComponentDef<CameraBoundsData>
```

## CameraBoundsData — interface @experimental
```
maxX: number
maxY: number
minX: number
minY: number
```

## CameraData — interface @beta
```
aspectRatio: number
clearFlags: number
cullingMask: number
farPlane: number
fov: number
isActive: boolean
nearPlane: number
orthoSize: number
pixelPerfect: boolean
priority: number
projectionType: number
showFrustum: boolean
viewport: Vec4
```

## CameraDirector — const @experimental
```
ResourceDef<CameraDirectorState>
```

## CameraDirectorState — interface @experimental
```
blending: boolean
currentMain: CameraPOV | null
curve: number
duration: number
from: CameraPOV | null
hasPending: boolean
pendingCurve: number
pendingTarget: number
pendingTime: number
shakeSeq: number
shakes: ActiveShake[]
startTime: number
target: number
```

## CameraExtents — interface @experimental
```
cameraX: number
cameraY: number
entity: number
halfH: number
halfW: number
```

## CameraPOV — interface @experimental
```
clearFlags: number
cullingMask: number
entity: number
far: number
fov: number
isActive: boolean
near: number
orthoSize: number
pixelPerfect: boolean
priority: number
projection: number
rotation: number
viewport: { x: number; y: number; z: number; w: number; }
x: number
y: number
z: number
```

## CameraRenderParams — interface @experimental
```
cameraEntity: number | undefined
clearColor: { x: number; y: number; z: number; w: number; } | undefined
clearFlags: number
cullingMask: number | undefined
elapsed: number
registry: { _cpp: CppRegistry; }
viewProjection: Float32Array<ArrayBufferLike>
viewportPixels: Viewport
```

## CameraView — const @experimental
```
ResourceDef<CameraViewAPI>
```

## CameraViewAPI — class @experimental
```
getWorldBounds: () => { left: number; right: number; bottom: number; top: number; } | null
getWorldMousePosition: () => { x: number; y: number; } | null
screenToWorld: (screenX: number, screenY: number, planeZ?: number) => { x: number; y: number; } | null
worldToScreen: (worldX: number, worldY: number, worldZ?: number) => { x: number; y: number; } | null
static new (app_: App): CameraViewAPI
static prototype: CameraViewAPI
```

## Canvas — const @experimental
```
BuiltinComponentDef<CanvasData>
```

## CanvasData — interface @experimental
```
backgroundColor: Color
designResolution: Vec2
layer: number
matchWidthOrHeight: number
pixelsPerUnit: number
scaleMode: number
```

## CanvasScale — interface @experimental
```
designResolution: { x: number; y: number; }
matchWidthOrHeight: number
scaleMode: number
```

## CapsuleCollider — const @experimental
```
BuiltinComponentDef<CapsuleColliderData>
```

## CapsuleColliderData — interface @experimental
```
categoryBits: number
density: number
enabled: boolean
friction: number
halfHeight: number
isSensor: boolean
maskBits: number
offset: Vec2
radius: number
restitution: number
```

## Catalog — class @experimental
```
getAllLabels: () => string[]
getAtlasFrame: (path: string) => AtlasFrameInfo | null
getBuildPath: (path: string) => string
getByLabel: (label: string) => string[]
getDeps: (path: string) => string[]
getEntry: (path: string) => CatalogEntry | null
hasAddress: (address: string) => boolean
hasEntry: (path: string) => boolean
isEmpty: boolean
resolve: (ref: string) => string
static empty: () => Catalog
static fromJson: (data: CatalogData) => Catalog
static new (entries: Map<string, CatalogEntry>, addresses: Map<string, string>, labels: Map<string, string[]>): Catalog
static prototype: Catalog
```

## CatalogData — interface @experimental
```
addresses: Record<string, string> | undefined
entries: Record<string, CatalogEntry>
labels: Record<string, string[]> | undefined
version: number
```

## CatalogEntry — interface @experimental
```
atlas: string | undefined
buildPath: string | undefined
deps: string[] | undefined
frame: { x: number; y: number; w: number; h: number; } | undefined
trim: { sourceW: number; sourceH: number; offsetX: number; offsetY: number; } | undefined
type: string
uv: { offset: [number, number]; scale: [number, number]; } | undefined
```

## Cell — interface @experimental
```
x: number
y: number
```

## Census — interface @experimental
```
atMs: number
entries: ReadonlyMap<string, CensusEntry>
failedProbes: readonly string[]
```

## CensusContext — interface @experimental
```
app: App | undefined
module: ESEngineModule | null | undefined
world: World | undefined
```

## CensusDelta — interface @experimental
```
after: number
before: number
delta: number
key: string
tier: CensusTier
unit: "count" | "bytes"
```

## CensusEntry — interface @experimental
```
key: string
tier: CensusTier
unit: "count" | "bytes"
value: number
```

## CensusProbe — interface @experimental
```
id: string
read: (ctx: CensusContext) => readonly CensusEntry[] | undefined
```

## CensusReport — interface @experimental
```
cycles: number
failedProbes: readonly string[]
judgedFrom: number
leaks: readonly CensusVerdict[]
projectCycles: number
verdicts: readonly CensusVerdict[]
```

## CensusSeriesOptions — interface @experimental
```
boundedSlopeTolerance: number | undefined
projectCycles: number | undefined
trendByteBudgetPerCycle: number | undefined
warmupCycles: number | undefined
```

## CensusTier — type @experimental
```
'conserved' | 'bounded' | 'trend' | 'info'
```

## CensusVerdict — interface @experimental
```
confidentSlopePerCycle: number
first: number
key: string
last: number
leaking: boolean
projected: number
reason: string | undefined
risingFraction: number
slopePerCycle: number
tier: CensusTier
unit: "count" | "bytes"
```

## ChainCollider — const @experimental
```
ComponentDef<ChainColliderData>
```

## Changed — function @experimental
```
<T extends AnyComponentDef>(component: T): ChangedWrapper<T>
```

## ChangedWrapper — interface @public
```
@internal _component: T
@internal _filterType: "changed"
```

## CharacterController — const @beta
```
ComponentDef<CharacterControllerData>
```

## CharacterControllerData — interface @experimental
```
floorMaxAngle: number
floorNormal: Vec2
isOnCeiling: boolean
isOnFloor: boolean
isOnWall: boolean
maskBits: number
maxSlides: number
realVelocity: Vec2
skinWidth: number
slideOnCeiling: boolean
snapLength: number
up: Vec2
velocity: Vec2
```

## Children — const @public
```
BuiltinComponentDef<ChildrenData>
```

## ChildrenData — interface @public
```
entities: number[]
```

## CircleCollider — const @experimental
```
BuiltinComponentDef<CircleColliderData>
```

## CircleColliderData — interface @experimental
```
categoryBits: number
density: number
enabled: boolean
friction: number
isSensor: boolean
maskBits: number
offset: Vec2
radius: number
restitution: number
```

## ClearFlags — enum @experimental
```
Color = 1
ColorAndDepth = 3
Depth = 2
Nothing = 0
```

## ColliderInstance — interface @experimental
```
enabled: boolean
isSensor: boolean
shape: ColliderShape
```

## ColliderOutline — interface @experimental
```
circles: { c: Vec2; r: number; }[]
polylines: Vec2[][]
```

## ColliderShape — type @experimental
```
| { kind: 'box'; halfExtents: Vec2; offset: Vec2 }
    | { kind: 'circle'; radius: number; offset: Vec2 }
    | { kind: 'capsule'; radius: number; halfHeight: number; offset: Vec2 }
    | { kind: 'segment'; point1: Vec2; point2: Vec2 }
    | { kind: 'polygon'; vertices: Vec2[] }
    | { kind: 'chain'; points: Vec2[]; isLoop: boolean }
```

## CollisionBrush — interface @experimental
```
collision: ResolvedTileCollision
id: number
key: string
```

## CollisionEnterEvent — interface @experimental
```
contactX: number
contactY: number
entityA: number
entityB: number
normalX: number
normalY: number
```

## CollisionHitEvent — interface @experimental
```
approachSpeed: number
entityA: number
entityB: number
normalX: number
normalY: number
pointX: number
pointY: number
```

## CollisionMaterial — interface @experimental
```
density: number | undefined
friction: number | undefined
restitution: number | undefined
```

## Color — interface @public
```
a: number
b: number
g: number
r: number
```

## ColorRole — type @experimental
```
keyof ThemeColors
```

## Commands — function @public
```
(): CommandsDescriptor
```

## CommandsDescriptor — interface @public
```
@internal _type: "commands"
```

## CommandsInstance — class @public
```
@internal spawnImmediate: (components: SpawnComponentEntry[], entityRef: { entity: Entity; }, name?: string) => void
despawn: (entity: Entity) => CommandsInstance
entity: (entity: Entity) => EntityCommands
flush: () => void
insertResource: <T>(resource: ResourceDef<T>, value: T) => CommandsInstance
queueInsert: (entity: Entity, component: AnyComponentDef, data: unknown) => void
queueRemove: (entity: Entity, component: AnyComponentDef) => void
spawn: (name?: string) => EntityCommands
static new (world: World, resources: ResourceStorage): CommandsInstance
static prototype: CommandsInstance
```

## CompareOp — type @experimental
```
'==' | '!=' | '<' | '<=' | '>' | '>=' | 'truthy' | 'falsy'
```

## CompiledFsm — interface @experimental
```
initial: string
states: Map<string, FsmState>
```

## Completable — interface @experimental
```
cancel: () => void
pause: () => void
resume: () => void
state: TweenState
```

## ComponentData — type @public
```
C extends BuiltinComponentDef<infer T> ? T :
    C extends ComponentDef<infer T> ? T :
    never
```

## ComponentDef — interface @public
```
@internal _builtin: false
@internal _default: T
@internal _id: symbol
@internal _name: string
animatableFields: readonly string[]
assetFields: readonly AssetFieldMeta[]
colorKeys: readonly string[]
create: (data?: Partial<T>) => T
discoverAssets: ((data: Record<string, unknown>) => AssetRef[]) | undefined
entityFields: readonly string[]
fieldMeta: Readonly<Record<string, FieldMeta>>
readonlyFields: readonly string[]
renderableField: string | null
replicatedFields: readonly string[]
skeletalFields: SkeletalFieldMeta | undefined
transient: boolean
```

## ComponentMetadata — interface @public
```
animatableFields: string[] | undefined
assetFields: AssetFieldMeta[] | undefined
discoverAssets: ((data: Record<string, unknown>) => AssetRef[]) | undefined
entityFields: string[] | undefined
fields: Record<string, FieldMeta> | undefined
readonlyFields: string[] | undefined
renderableField: string | undefined
replicatedFields: string[] | undefined
skeletalFields: SkeletalFieldMeta | undefined
transient: boolean | undefined
```

## ComponentsData — type @public
```
{
    [K in keyof C]: ComponentData<UnwrapQueryArg<C[K]>>;
}
```

## CompressorEffectDef — interface @experimental
```
attack: number | undefined
kneeDb: number | undefined
ratio: number | undefined
release: number | undefined
thresholdDb: number | undefined
type: "compressor"
```

## ControllerState — interface @experimental
```
current: string
name: string
pages: string[]
```

## CookedAtlasInfo — interface @experimental
```
frame: { x: number; y: number; width: number; height: number; }
page: number | undefined
pageHeight: number
pageWidth: number
```

## CreateListViewOptions — interface @experimental
```
background: UIVisualInit | undefined
data: DataSource<T> | readonly T[]
decelerationRate: number | undefined
direction: "vertical" | "horizontal" | "both" | undefined
dragScroll: boolean | undefined
host: ListViewHost
item: ListItemTemplate<T> | Record<string, ListItemTemplate<T>>
layout: ListLayoutSpec | LayoutProvider
node: UINodeInit | undefined
onItemBound: ((entity: Entity, data: T, index: number) => void) | undefined
parent: number | undefined
recycleBuffer: number | undefined
viewportSize: Vec2
wheelSpeed: number | undefined
world: World
```

## CreateWebAppOptions — interface @experimental
```
colorSpace: "gamma" | "linear" | undefined
depthLayers: number | undefined
getViewportSize: (() => { width: number; height: number; }) | undefined
plugins: Plugin[] | undefined
randomSeed: number | undefined
renderSurface: RenderSurfaceSource | undefined
screenFit: ScreenScalingData | undefined
sideModules: SideModuleHost | undefined
wasmBaseUrl: string | undefined
ySortLayers: number | undefined
```

## CullMode — enum @experimental
```
Back = 1
Front = 2
None = 0
```

## DARK_TOKENS — const @experimental
```
ThemeTokens
```

## DEFAULT_ANIM_CLIP_PIVOT — const @experimental
```
AnimClipPivotData
```

## DEFAULT_DESIGN_HEIGHT — const @experimental
```
number
```

## DEFAULT_DESIGN_WIDTH — const @experimental
```
number
```

## DEFAULT_EDITOR_GRID — const @experimental
```
EditorGridData
```

## DEFAULT_EDITOR_VIEW — const @experimental
```
EditorViewData
```

## DEFAULT_FALLBACK_DT — const @experimental
```
number
```

## DEFAULT_FIXED_TIMESTEP — const @experimental
```
number
```

## DEFAULT_FONT_FAMILY — const @experimental
```
"Arial"
```

## DEFAULT_FONT_SIZE — const @experimental
```
24
```

## DEFAULT_GRAVITY — const @experimental
```
{ x: number; y: number; }
```

## DEFAULT_LINE_HEIGHT — const @experimental
```
1.2
```

## DEFAULT_MAX_DELTA_TIME — const @experimental
```
0.5
```

## DEFAULT_PIXELS_PER_UNIT — const @experimental
```
number
```

## DEFAULT_SCREEN_SCALING — const @experimental
```
ScreenScalingData
```

## DEFAULT_SPINE_SKIN — const @experimental
```
"default"
```

## DEFAULT_SPRITE_SIZE — const @experimental
```
{ x: number; y: number; }
```

## DEFAULT_TEXT_CANVAS_SIZE — const @experimental
```
512
```

## DEFAULT_UI_CAMERA_INFO — const @experimental
```
{ readonly viewProjection: Float32Array<ArrayBuffer>; readonly vpX: 0; readonly vpY: 0; readonly vpW: 0; readonly vpH: 0; readonly screenW: 0; readonly screenH: 0; readonly worldLeft: 0; readonly worldBottom: 0; readonly worldRight: 0; readonly worldTop: 0; readonly worldMouseX: 0; readonly worldMouseY: 0; readonly valid: false; }
```

## DataSource — interface @experimental
```
getCount: () => number
getItem: (index: number) => T
getItemId: ((index: number, item: T) => string | number) | undefined
getItemType: ((index: number) => string) | undefined
subscribe: ((listener: (change: DataSourceChange) => void) => Unsubscribe) | undefined
```

## DataSourceChange — type @experimental
```
| { type: 'insert'; index: number; count: number }
    | { type: 'remove'; index: number; count: number }
    | { type: 'update'; index: number; count: number }
    | { type: 'reset' }
```

## DataType — enum @experimental
```
Float = 1
Float2 = 2
Float3 = 3
Float4 = 4
Int = 5
Int2 = 6
Int3 = 7
Int4 = 8
```

## DecodedChunk — interface @experimental
```
tiles: Uint16Array<ArrayBufferLike>
x: number
y: number
```

## DecodedPixels — interface @experimental
```
height: number
pixels: Uint8Array<ArrayBufferLike>
width: number
```

## DeviceIdentity — interface @experimental
```
backend: string
renderer: string
vendor: string
version: string
```

## DeviceLostReason — const @experimental
```
{ readonly Unknown: 0; readonly ContextLost: 1; readonly OutOfMemory: 2; readonly Reset: 3; readonly Removed: 4; readonly Destroyed: 5; readonly Validation: 6; readonly Internal: 7; }
```

## DeviceStatus — const @experimental
```
{ readonly Live: 0; readonly Lost: 1; readonly Recovering: 2; readonly Dead: 3; }
```

## DiagnosticEvent — interface @experimental
```
context: Record<string, unknown> | undefined
count: number
firstAt: number
id: string
kind: DiagnosticKind
lastAt: number
message: string
source: string | undefined
stack: string | undefined
```

## DiagnosticKind — type @experimental
```
| 'engine'
    /** Reached the host with nobody catching it: `window.onerror`,
     *  `unhandledrejection`, `wx.onError`. Usually game code outside a system —
     *  a callback, a promise, a timer. */
    | 'unhandled'
    /** The GPU took the context away (backgrounded, driver reset, too many
     *  contexts). The frame after this one draws nothing. */
    | 'context-lost'
    /** The OS says memory is running out. Not yet a failure — it is the warning
     *  that arrives before the process is killed, which is the crash no report
     *  ever gets sent for. */
    | 'memory'
    /** The game itself called `Diagnostics.report` — a failed purchase, a save
     *  that would not write, whatever it considers worth knowing. */
    | 'game'
```

## DiagnosticReport — interface @experimental
```
context: Record<string, unknown> | undefined
error: unknown
kind: DiagnosticKind
message: string
source: string | undefined
```

## Diagnostics — const @experimental
```
ResourceDef<DiagnosticsAPI>
```

## DiagnosticsAPI — class @experimental
```
clear: () => void
dropped: number
events: readonly DiagnosticEvent[]
flush: () => void
report: (report: DiagnosticReport) => void
reportError: (kind: DiagnosticReport["kind"], error: unknown, source?: string) => void
setSink: (sink: DiagnosticsSink | null) => void
tick: (deltaSeconds: number) => void
static new (options?: DiagnosticsOptions): DiagnosticsAPI
static prototype: DiagnosticsAPI
```

## DiagnosticsOptions — interface @experimental
```
flushIntervalSec: number | undefined
maxDistinct: number | undefined
```

## DiagnosticsPlugin — class @experimental
```
build: (app: App) => void
cleanup: (app: App) => void
name: string
static new (options_?: DiagnosticsPluginOptions): DiagnosticsPlugin
static prototype: DiagnosticsPlugin
```

## DiagnosticsPluginOptions — interface @experimental
```
captureLevel: LogLevel | undefined
flushIntervalSec: number | undefined
maxDistinct: number | undefined
```

## DiagnosticsSink — type @experimental
```
(events: readonly DiagnosticEvent[]) => void
```

## DialogHandle — interface @experimental
```
close: () => void
dispose: () => void
entity: number
isOpen: () => boolean
open: () => void
panelEntity: number
```

## DialogOptions — interface @experimental
```
backdropNode: UINodeInit | undefined
backdropVisual: UIVisualInit | undefined
closeOnBackdrop: boolean | undefined
closeOnEscape: boolean | undefined
events: UIEventQueue
onOpenChange: ((open: boolean) => void) | undefined
panelNode: UINodeInit | undefined
panelVisual: UIVisualInit | undefined
parent: number | undefined
startHidden: boolean | undefined
world: World
```

## DiffOptions — interface @experimental
```
floatEpsilon: number | undefined
ignoreEntityNames: readonly string[] | undefined
ignoreMetadataKeys: readonly string[] | undefined
```

## Dimension — interface @public
```
unit: number
value: number
```

## DimensionUnit — type @public
```
(typeof DimensionUnit)[keyof typeof DimensionUnit]
```

## Disabled — const @experimental
```
ComponentDef<{}>
```

## DistanceJoint — const @experimental
```
ComponentDef<DistanceJointData>
```

## DragPlugin — class @experimental
```
build: (app: App) => void
dependencies: "uiInteraction"[]
name: "drag"
static new (): DragPlugin
static prototype: DragPlugin
```

## DragState — const @experimental
```
ComponentDef<DragStateData>
```

## DragStateData — interface @experimental
```
currentWorldPos: { x: number; y: number; }
deltaWorld: { x: number; y: number; }
isDragging: boolean
pointerStartWorld: { x: number; y: number; }
startWorldPos: { x: number; y: number; }
totalDeltaWorld: { x: number; y: number; }
```

## Draggable — const @experimental
```
ComponentDef<DraggableData>
```

## DraggableData — interface @experimental
```
constraintMax: { x: number; y: number; } | null
constraintMin: { x: number; y: number; } | null
dragThreshold: number
enabled: boolean
lockX: boolean
lockY: boolean
```

## DragonBones — const @experimental
```
ResourceDef<DragonBonesManager | null>
```

## DragonBonesAnimation — const @experimental
```
BuiltinComponentDef<DragonBonesAnimationData>
```

## DragonBonesAnimationData — interface @experimental
```
animation: string
armature: string
atlasPath: string
color: Color
enabled: boolean
fadeInTime: number
flipX: boolean
flipY: boolean
layer: number
loop: boolean
material: number
playing: boolean
skeletonPath: string
skeletonScale: number
timeScale: number
```

## DragonBonesPlugin — class @experimental
```
acquire: () => Promise<DragonBonesManager | null>
build: (app: App) => void
cleanup: () => void
manager: DragonBonesManager | null
name: string
static new (manager?: DragonBonesManager): DragonBonesPlugin
static prototype: DragonBonesPlugin
```

## Draw — const @experimental
```
DrawAPI
```

## DrawAPI — interface @experimental
```
begin: (viewProjection: Float32Array) => void
circle: (center: Vec2, radius: number, color: Color, filled?: boolean, segments?: number) => void
circleOutline: (center: Vec2, radius: number, color: Color, thickness?: number, segments?: number) => void
drawMesh: (geometry: GeometryHandle, shader: ShaderHandle, transform: Float32Array) => void
drawMeshWithMaterial: (geometry: GeometryHandle, material: MaterialHandle, transform?: Float32Array) => void
end: () => void
getDrawCallCount: () => number
getPrimitiveCount: () => number
line: (from: Vec2, to: Vec2, color: Color, thickness?: number) => void
rect: (position: Vec2, size: Vec2, color: Color, filled?: boolean) => void
rectOutline: (position: Vec2, size: Vec2, color: Color, thickness?: number) => void
setBlendMode: (mode: BlendMode) => void
setDepth: (depth: number) => void
setDepthTest: (enabled: boolean) => void
setLayer: (layer: number) => void
texture: (position: Vec2, size: Vec2, textureHandle: number, tint?: Color) => void
textureRotated: (position: Vec2, size: Vec2, rotation: number, textureHandle: number, tint?: Color) => void
```

## DrawCallInfo — interface @experimental
```
blendMode: number
cameraIndex: number
entities: number[]
entityCount: number
entityOffset: number
flushReason: FlushReason
index: number
layer: number
materialId: number
scissorEnabled: boolean
scissorH: number
scissorW: number
scissorX: number
scissorY: number
shaderId: number
stage: number
stencilRef: number
stencilTest: boolean
stencilWrite: boolean
textureId: number
textureSlotUsage: number
triangleCount: number
type: RenderType
vertexCount: number
```

## DrawCallback — type @experimental
```
(elapsed: number) => void
```

## DrawRank — interface @experimental
```
layer: number
order: LayerOrder
worldY: number
worldZ: number
```

## DropShadowFilterOptions — interface @experimental
```
blur: number | undefined
color: { r: number; g: number; b: number; a: number; } | undefined
offsetX: number | undefined
offsetY: number | undefined
texelSize: { x: number; y: number; } | undefined
```

## DropdownHandle — interface @experimental
```
close: () => void
dispose: () => void
entity: number
getSelected: () => T
getSelectedIndex: () => number
isOpen: () => boolean
labelEntity: number
open: () => void
setSelectedIndex: (index: number) => void
```

## DropdownOptions — interface @experimental
```
buttonStates: { normal?: Color; hover?: Color; pressed?: Color; } | undefined
disabled: boolean | undefined
events: UIEventQueue
focusable: boolean | undefined
node: UINodeInit | undefined
onSelect: ((index: number, option: T, entity: Entity) => void) | undefined
optionHeight: number | undefined
optionToLabel: ((option: T, index: number) => string) | undefined
options: readonly T[]
parent: number | undefined
selectedIndex: number | undefined
tabIndex: number | undefined
world: World
```

## EDITOR_UI_ANCHOR — const @experimental
```
{ readonly x: 0; readonly y: 0; }
```

## EDITOR_VIEW_ENTITY — const @experimental
```
-1
```

## EasingType — type @experimental
```
(typeof EasingType)[keyof typeof EasingType]
```

## EditorAssetType — type @experimental
```
| 'texture' | 'material' | 'shader' | 'spine-atlas' | 'spine-skeleton'
    | 'dragonbones-atlas' | 'dragonbones-skeleton'
    | 'bitmap-font' | 'font' | 'prefab' | 'json' | 'audio' | 'video' | 'scene' | 'anim-clip'
    | 'tilemap' | 'tileset' | 'timeline'
    | 'unknown'
```

## EditorBridge — interface @experimental
```
onComponentAdded: ((entity: number, component: string) => void) | undefined
onComponentChanged: ((entity: number, component: string) => void) | undefined
onComponentRemoved: ((entity: number, component: string) => void) | undefined
onEntityDespawned: ((entity: number) => void) | undefined
onEntitySpawned: ((entity: number, name?: string) => void) | undefined
onParentChanged: ((child: number, parent: number | null) => void) | undefined
registerComponent: (name: string, defaults: Record<string, unknown>, isTag: boolean) => void
```

## EditorGrid — const @experimental
```
ResourceDef<EditorGridData>
```

## EditorGridData — interface @experimental
```
axisX: GridColor
axisY: GridColor
color: GridColor
enabled: boolean
majorColor: GridColor
majorEvery: number
spacing: number
```

## EditorView — const @experimental
```
ResourceDef<EditorViewData>
```

## EditorViewData — interface @experimental
```
active: boolean
distance: number
fov: number
orthoSize: number
perspective: boolean
uiPreviewAspect: number
x: number
y: number
```

## EffectDef — interface @experimental
```
factory: () => ShaderHandle
label: string
multiPass: EffectSubPass[] | undefined
textures: EffectTextureDef[] | undefined
type: string
uniforms: EffectUniformDef[]
```

## EffectUniformDef — interface @experimental
```
defaultValue: number
label: string
max: number
min: number
name: string
neutralValue: number | undefined
step: number
```

## EmbeddedSideModuleEntry — interface @experimental
```
glueBase64: string
wasmBase64: string
```

## EmbeddedSideModuleRegistry — type @experimental
```
Partial<Record<SideModuleId, EmbeddedSideModuleEntry>>
```

## EmitterShape — enum @experimental
```
Circle = 1
Cone = 3
Point = 0
Rectangle = 2
```

## EngineApi — type @experimental
```
NativeEngineApi & Partial<NativeHeap>
```

## Entity — type @public
```
number
```

## EntityCommands — class @public
```
finalize: () => void
id: () => Entity
insert: <T extends object>(component: AnyComponentDef, data?: Partial<T> | undefined) => EntityCommands
remove: (component: AnyComponentDef) => EntityCommands
static new (commands: CommandsInstance, entity: Entity | null, name?: string): EntityCommands
static prototype: EntityCommands
```

## EntityEvent — interface @experimental
```
currentTarget: number
data: TData
defaultPrevented: boolean
preventDefault: () => void
propagationStopped: boolean
stopPropagation: () => void
target: number
type: string
```

## EntityEventHandler — type @experimental
```
(event: EntityEvent<TData>) => void
```

## EntityEventQueue — class @experimental
```
clear: () => void
drain: () => readonly EntityEvent[]
emit: <TData = unknown>(entity: Entity, type: string, data?: TData) => EntityEvent<TData>
emitBubbled: (ancestor: Entity, rootEvent: EntityEvent) => EntityEvent
hasListenersFor: (type: string) => boolean
on: { (entity: Entity, type: string, handler: EntityEventHandler): Unsubscribe; (type: string, handler: EntityEventHandler): Unsubscribe; }
query: (type: string) => readonly EntityEvent[]
removeAll: (entity: Entity) => void
static new (): EntityEventQueue
static prototype: EntityEventQueue
```

## EntityEvents — const @experimental
```
ResourceDef<EntityEventQueue>
```

## EventBinding — const @experimental
```
ComponentDef<EventBindingData>
```

## EventBindingData — interface @experimental
```
rows: EventBindingRow[]
```

## EventBindingPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): EventBindingPlugin
static prototype: EventBindingPlugin
```

## EventBindingRow — interface @experimental
```
action: string
arg: string | undefined
enabled: boolean | undefined
event: string
guard: string | undefined
once: boolean | undefined
params: Record<string, AiParamValue> | undefined
target: string | undefined
```

## EventDef — interface @public
```
@internal _id: symbol
@internal _name: string
@internal _phantom: T | undefined
```

## EventReader — function @public
```
<T>(event: EventDef<T>): EventReaderDescriptor<T>
```

## EventReaderDescriptor — interface @public
```
@internal _event: EventDef<T>
@internal _type: "event_reader"
```

## EventReaderInstance — class @public
```
[Symbol.iterator]: () => Iterator<T>
isEmpty: () => boolean
toArray: () => T[]
static new <T>(bus: EventBus<T>): EventReaderInstance<T>
static prototype: EventReaderInstance<any>
```

## EventRegistry — class @experimental
```
getBus: <T>(event: EventDef<T>) => EventBus<T>
register: <T>(event: EventDef<T>) => void
swapAll: () => void
static new (): EventRegistry
static prototype: EventRegistry
```

## EventWriter — function @public
```
<T>(event: EventDef<T>): EventWriterDescriptor<T>
```

## EventWriterDescriptor — interface @public
```
@internal _event: EventDef<T>
@internal _type: "event_writer"
```

## EventWriterInstance — class @public
```
send: (event: T) => void
static new <T>(bus: EventBus<T>): EventWriterInstance<T>
static prototype: EventWriterInstance<any>
```

## ExternalEntityRef — interface @experimental
```
componentType: string
entityId: number
field: string
target: number
```

## ExtractEntity — interface @experimental
```
children: number[]
components: ComponentData[]
id: number
name: string
parent: number | null
visible: boolean | undefined
```

## FILL_AXIS — const @experimental
```
Record<LinearFillDirection, readonly [FillMethod, FillOrigin]>
```

## FieldMeta — interface @public
```
advanced: boolean | undefined
bitmask: { bits?: number; source?: string; } | undefined
category: string | undefined
curve: boolean | undefined
enum: readonly { label: string; value: number; }[] | undefined
enumSource: string | undefined
flags: readonly { label: string; value: number; }[] | undefined
gradient: boolean | undefined
label: string | undefined
map: boolean | undefined
max: number | undefined
min: number | undefined
normalizedOf: string | undefined
slider: boolean | undefined
step: number | undefined
tooltip: string | undefined
unit: string | undefined
```

## FillMethod — enum @experimental
```
Horizontal = 0
Radial180 = 4
Radial360 = 2
Radial90 = 3
Vertical = 1
```

## FillOrigin — enum @experimental
```
Bottom = 2
Left = 0
Right = 1
Top = 3
```

## FilterEffectDef — interface @experimental
```
filter: "lowpass" | "highpass" | "bandpass" | "lowshelf" | "highshelf" | "peaking" | "notch"
frequency: number
gainDb: number | undefined
q: number | undefined
type: "filter"
```

## FilterExpr — type @public
```
| { readonly kind: 'with'; readonly component: AnyComponentDef }
    | { readonly kind: 'without'; readonly component: AnyComponentDef }
    | { readonly kind: 'and'; readonly filters: readonly FilterExpr[] }
    | { readonly kind: 'or'; readonly filters: readonly FilterExpr[] }
    | { readonly kind: 'not'; readonly filter: FilterExpr }
```

## Filters — const @experimental
```
{ identityMatrix(): number[]; grayscaleMatrix(): number[]; sepiaMatrix(): number[]; brightnessMatrix(value: number): number[]; contrastMatrix(value: number): number[]; saturationMatrix(value: number): number[]; invertMatrix(): number[]; getColorMatrixShader: () => ShaderHandle; colorMatrixUniforms(matrix: number[]): Record<string, number[]>; }
```

## FlattenContext — interface @experimental
```
allocateId: () => number
depth: number | undefined
loadPrefab: (path: string) => PrefabData | null
visited: Set<string> | undefined
```

## FlattenResult — interface @experimental
```
entities: ProcessedEntity[]
rootId: number
```

## FlexContainer — const @experimental
```
BuiltinComponentDef<FlexContainerData>
```

## FlexContainerData — interface @experimental
```
alignContent: AlignContent
alignItems: AlignItems
direction: FlexDirection
gap: Vec2
justifyContent: JustifyContent
padding: Padding
wrap: FlexWrap
```

## FlexDirection — type @experimental
```
(typeof FlexDirection)[keyof typeof FlexDirection]
```

## FlexWrap — type @experimental
```
(typeof FlexWrap)[keyof typeof FlexWrap]
```

## FlushReason — enum @experimental
```
BatchFull = 0
BlendModeChange = 5
FrameEnd = 8
MaterialChange = 4
ScissorChange = 2
StageEnd = 6
StencilChange = 3
TextureSlotsFull = 1
TypeChange = 7
```

## FocusManager — const @experimental
```
ResourceDef<FocusManagerState>
```

## FocusManagerState — class @experimental
```
blur: () => Entity | null
focus: (entity: Entity, visible?: boolean) => Entity | null
focusVisible: boolean
focusedEntity: number | null
isVisiblyFocused: (entity: Entity) => boolean
static new (): FocusManagerState
static prototype: FocusManagerState
```

## FocusPlugin — class @experimental
```
build: (app: App) => void
dependencies: "uiInteraction"[]
name: "focus"
static new (): FocusPlugin
static prototype: FocusPlugin
```

## Focusable — const @experimental
```
ComponentDef<FocusableData>
```

## FocusableData — interface @experimental
```
isFocused: boolean
tabIndex: number
```

## FollowTarget — const @experimental
```
ComponentDef<FollowTargetData>
```

## FollowTargetData — interface @experimental
```
damping: number
deadzone: number
offsetX: number
offsetY: number
target: number
```

## FontHandle — type @experimental
```
number
```

## ForceFieldType — enum @experimental
```
Directional = 0
Drag = 3
Point = 1
Vortex = 2
```

## FrameCaptureData — interface @experimental
```
cameraCount: number
drawCalls: DrawCallInfo[]
```

## FrameHistory — class @experimental
```
count: number
getAll: () => FrameSnapshot[]
getLatest: () => FrameSnapshot | null
push: (frameTimeMs: number, phaseTimings: Map<string, number>, systemTimings?: Map<string, number>) => void
reset: () => void
static new (capacity?: number): FrameHistory
static prototype: FrameHistory
```

## FrameSnapshot — interface @experimental
```
frameTimeMs: number
phaseTimings: Map<string, number>
systemTimings: Map<string, number>
```

## FrameStats — interface @experimental
```
culled: number
drawCalls: number
entityCount: number
fps: number
frameTimeMs: number
meshes: number
phaseTimings: Map<string, number>
spine: number
sprites: number
systemTimings: Map<string, number>
text: number
triangles: number
```

## FsmActionRef — type @experimental
```
string | { name: string; arg?: string; params?: Record<string, AiParamValue> }
```

## FsmDefinition — interface @experimental
```
initial: string
states: FsmState[]
```

## FsmEdge — type @experimental
```
from: string
id: string
index: number
to: string
transition: FsmTransition
```

## FsmPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): FsmPlugin
static prototype: FsmPlugin
```

## FsmRunState — interface @experimental
```
current: string
entered: boolean
previous: string | null
```

## FsmState — interface @experimental
```
name: string
onEnter: FsmActionRef | undefined
onExit: FsmActionRef | undefined
onUpdate: FsmActionRef | undefined
transitions: FsmTransition[] | undefined
x: number | undefined
y: number | undefined
```

## FsmTransition — interface @experimental
```
condition: string | undefined
guard: BlackboardGuard | BlackboardGuard[] | undefined
to: string
trigger: string | undefined
```

## GLDebug — const @experimental
```
{ enable(): void; disable(): void; check(context: string): number; diagnose(): void; }
```

## GameSocket — class @experimental
```
close: (code?: number, reason?: string) => void
connect: () => void
on: <K extends keyof PlatformSocketEvents>(event: K, handler: (...args: PlatformSocketEvents[K]) => void) => () => void
readyState: PlatformSocketReadyState
send: (data: string | ArrayBuffer) => void
static new (options: GameSocketOptions): GameSocket
static prototype: GameSocket
```

## GameSocketOptions — type @experimental
```
PlatformSocketOptions
```

## GamepadAxis — enum @public
```
LeftX = 0
LeftY = 1
RightX = 2
RightY = 3
```

## GamepadButton — enum @public
```
Back = 8
DpadDown = 13
DpadLeft = 14
DpadRight = 15
DpadUp = 12
East = 1
Guide = 16
LeftBumper = 4
LeftStick = 10
LeftTrigger = 6
North = 3
RightBumper = 5
RightStick = 11
RightTrigger = 7
South = 0
Start = 9
West = 2
```

## GamepadSnapshot — interface @experimental
```
axes: number[]
buttons: number[]
connected: boolean
index: number
mapping: string
```

## GearBinding — interface @experimental
```
component: string
controller: string
pages: Record<string, GearValue>
property: string
tween: GearTween | undefined
```

## GearTween — interface @experimental
```
duration: number
easing: EasingType
```

## GearValue — type @experimental
```
number | boolean | string | Color | Vec2 | Vec3
```

## Geometry — const @experimental
```
{ create(options: GeometryOptions): GeometryHandle; updateVertices(handle: GeometryHandle, vertices: Float32Array, offset?: number): void; release(handle: GeometryHandle): void; isValid(handle: GeometryHandle): boolean; createQuad(width?: number, height?: number): GeometryHandle; createCircle(radius?: number, segments?: number): GeometryHandle; createPolygon(points: Array<{ x: number; y: number; }>): GeometryHandle; }
```

## GeometryHandle — type @experimental
```
number
```

## GeometryOptions — interface @experimental
```
dynamic: boolean | undefined
indices: Uint16Array<ArrayBufferLike> | Uint32Array<ArrayBufferLike> | undefined
layout: VertexAttributeDescriptor[]
vertices: Float32Array<ArrayBufferLike>
```

## GestureDetector — class @experimental
```
onLongPress: ((x: number, y: number) => void) | null
onPinch: ((scale: number, centerX: number, centerY: number) => void) | null
onSwipe: ((direction: SwipeDirection, speed: number) => void) | null
onTap: ((x: number, y: number) => void) | null
reset: () => void
update: (dt: number) => void
static new (input: InputState): GestureDetector
static prototype: GestureDetector
```

## GetWorld — function @public
```
(): GetWorldDescriptor
```

## GetWorldDescriptor — interface @public
```
@internal _type: "get_world"
```

## GpAxis — const @public
```
(axis: number, pad?: number, scale?: number) => Binding
```

## GpButton — const @public
```
(button: number, pad?: number) => Binding
```

## GraphNodeType — type @beta
```
| 'output' // the root: its `color` input becomes fragColor
  | 'uv' // v_texCoord
  | 'vertexColor' // v_color (the sprite's per-instance tint)
  | 'time' // u_time.x — the injected engine frame clock (seconds)
  | 'screenUV' // gl_FragCoord over the canvas size (0..1, y-up)
  | 'constFloat' // a scalar material param (#pragma param ... float)
  | 'constColor' // an RGBA material param (#pragma param ... color)
  | 'textureSample' // sample a texture material param at a UV (#pragma param ... texture)
  | 'multiply'
  | 'add'
  | 'lerp'
  | 'oneMinus' // 1 - x (invert a mask)
  | 'saturate' // clamp(x, 0, 1)
  | 'sin'
  | 'smoothstep' // smoothstep(edge0, edge1, x) — edges are node literals
  | 'noise' // procedural value noise over a UV (scale is a node literal)
  | 'panner'
```

## GraphType — type @beta
```
'float' | 'vec2' | 'vec3' | 'vec4'
```

## Graphics — class @experimental
```
arc: (cx: number, cy: number, radius: number, startAngle: number, endAngle: number, anticlockwise?: boolean, segments?: number) => void
beginFill: (color: Color) => void
clear: () => void
cubicCurveTo: (cp1x: number, cp1y: number, cp2x: number, cp2y: number, x: number, y: number, segments?: number) => void
curveTo: (cpx: number, cpy: number, x: number, y: number, segments?: number) => void
drawArc: (cx: number, cy: number, radius: number, startAngle: number, endAngle: number, anticlockwise?: boolean, segments?: number) => void
drawCircle: (cx: number, cy: number, radius: number, segments?: number) => void
drawEllipse: (cx: number, cy: number, rw: number, rh: number, segments?: number) => void
drawRect: (x: number, y: number, width: number, height: number) => void
drawRoundRect: (x: number, y: number, width: number, height: number, radius: number) => void
endFill: () => void
fillCommands: FillCmd[]
flush: () => void
lineCommands: LineCmd[]
lineStyle: (thickness: number, color: Color) => void
lineTo: (x: number, y: number) => void
moveTo: (x: number, y: number) => void
pathLength: number
static new (): Graphics
static prototype: Graphics
```

## GridColor — type @experimental
```
[number, number, number, number]
```

## GridLayoutProvider — class @experimental
```
getContentSize: (count: number) => Vec2
getItemRect: (index: number) => Rect
getVisibleRange: (viewport: Rect, count: number) => [number, number]
static new (opts: GridLayoutOptions): GridLayoutProvider
static prototype: GridLayoutProvider
```

## HeadlessAppOptions — interface @experimental
```
plugins: Plugin[] | undefined
sideModules: SideModuleHost | undefined
```

## HitAreaShape — type @experimental
```
| { type: 'rect'; x: number; y: number; width: number; height: number }
    | { type: 'circle'; cx: number; cy: number; radius: number }
    | { type: 'polygon'; points: number[] }
```

## HttpBackend — class @experimental
```
fetchBinary: (path: string) => Promise<ArrayBuffer>
fetchText: (path: string) => Promise<string>
resolveUrl: (path: string) => string
setBaseUrl: (url: string) => void
static new (options: HttpBackendOptions): HttpBackend
static prototype: HttpBackend
```

## INTERACTION_CONTROLLER — const @experimental
```
"$interaction"
```

## INTERACTION_PAGES — const @experimental
```
readonly ["normal", "hover", "pressed", "disabled", "focused"]
```

## INVALID_ENTITY — const @experimental
```
number
```

## INVALID_FONT — const @experimental
```
number
```

## INVALID_MATERIAL — const @experimental
```
0
```

## INVALID_TEXTURE — const @experimental
```
number
```

## Identity — const @experimental
```
ResourceDef<IdentityAPI>
```

## IdentityAPI — class @experimental
```
available: boolean
login: () => Promise<LoginResult>
sessionValid: () => Promise<boolean>
static new (): IdentityAPI
static prototype: IdentityAPI
```

## ImageLoadResult — interface @experimental
```
height: number
pixels: Uint8Array<ArrayBufferLike>
width: number
```

## ImageSegment — interface @experimental
```
height: number
offsetX: number
offsetY: number
scale: number
src: string
tint: Color | null
type: "image"
valign: ImageValign
width: number
```

## InferParam — type @public
```
P extends QueryDescriptor<infer C> ? QueryInstance<C> :
    P extends ResDescriptor<infer T> ? T :
    P extends ResMutDescriptor<infer T> ? ResMutInstance<T> :
    P extends CommandsDescriptor ? CommandsInstance :
    P extends EventWriterDescriptor<infer T> ? EventWriterInstance<T> :
    P extends EventReaderDescriptor<infer T> ? EventReaderInstance<T> :
    P extends RemovedQueryDescriptor<infer _T> ? RemovedQueryInstance<_T> :
    P extends GetWorldDescriptor ? World :
    never
```

## InferParams — type @public
```
{
    [K in keyof P]: InferParam<P[K]>;
}
```

## Input — const @beta
```
ResourceDef<InputState>
```

## InputHandler — interface @experimental
```
onKeyDown: ((code: string, mods: Modifiers) => boolean | void) | undefined
onKeyUp: ((code: string, mods: Modifiers) => boolean | void) | undefined
onPointerDown: ((button: number, x: number, y: number, mods: Modifiers) => boolean | void) | undefined
onPointerMove: ((x: number, y: number, mods: Modifiers) => boolean | void) | undefined
onPointerUp: ((button: number, mods: Modifiers) => boolean | void) | undefined
onTouchCancel: ((id: number) => boolean | void) | undefined
onTouchEnd: ((id: number) => boolean | void) | undefined
onTouchMove: ((id: number, x: number, y: number) => boolean | void) | undefined
onTouchStart: ((id: number, x: number, y: number) => boolean | void) | undefined
onWheel: ((deltaX: number, deltaY: number, mods: Modifiers) => boolean | void) | undefined
```

## InputMap — class @public
```
@internal evaluate: (input: InputState) => void
actions: () => string[]
axis2d: (name: string) => Vec2
cancelListen: () => void
down: (name: string) => boolean
getBindings: (name: string) => Binding[]
isListening: () => boolean
listenForBinding: (opts?: ListenOptions) => Promise<Binding | null>
load: (key: string) => boolean
loadJSON: (data: Record<string, Binding[]>) => void
pressed: (name: string) => boolean
rebind: (name: string, opts?: ListenOptions & { append?: boolean; }) => Promise<Binding | null>
released: (name: string) => boolean
save: (key: string) => void
setBindings: (name: string, bindings: Binding[]) => void
toAsset: () => InputMapAsset
toJSON: () => Record<string, Binding[]>
value: (name: string) => number
static new (actions: Record<string, ActionDef>): InputMap
static prototype: InputMap
```

## InputMapAsset — interface @public
```
actions: Record<string, ActionDef>
version: number
```

## InputPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (target?: unknown): InputPlugin
static prototype: InputPlugin
```

## InputRouter — class @experimental
```
currentMods: Modifiers
dispatchKeyDown: (code: string) => boolean
dispatchKeyUp: (code: string) => boolean
dispatchPointerDown: (button: number, x: number, y: number) => boolean
dispatchPointerMove: (x: number, y: number) => boolean
dispatchPointerUp: (button: number) => boolean
dispatchTouchCancel: (id: number) => boolean
dispatchTouchEnd: (id: number) => boolean
dispatchTouchMove: (id: number, x: number, y: number) => boolean
dispatchTouchStart: (id: number, x: number, y: number) => boolean
dispatchWheel: (deltaX: number, deltaY: number) => boolean
setEditorHandler: (handler: InputHandler | null) => () => void
setUIHandler: (handler: InputHandler | null) => () => void
static new (): InputRouter
static prototype: InputRouter
```

## InputState — class @beta
```
beginFixedStep: () => void
clearFrameState: () => void
clearVirtual: () => void
endFixedStep: () => void
gamepadButtonThreshold: number
gamepadDeadzone: number
gamepads: Map<number, PadState>
getGamepadAxis: (axis: number, pad?: number) => number
getGamepadButtonValue: (button: number, pad?: number) => number
getGamepads: () => number[]
getMousePosition: () => { x: number; y: number; }
getScrollDelta: () => { x: number; y: number; }
getTouch: (id: number) => TouchPoint | null
getTouchCount: () => number
getTouches: () => TouchPoint[]
getVirtual: (id: string) => Vec2
isGamepadButtonDown: (button: number, pad?: number) => boolean
isGamepadButtonPressed: (button: number, pad?: number) => boolean
isGamepadButtonReleased: (button: number, pad?: number) => boolean
isGamepadConnected: (pad?: number) => boolean
isKeyDown: (key: string) => boolean
isKeyPressed: (key: string) => boolean
isKeyReleased: (key: string) => boolean
isMouseButtonDown: (button: number) => boolean
isMouseButtonPressed: (button: number) => boolean
isMouseButtonReleased: (button: number) => boolean
isPointerOverUI: () => boolean
isTouchActive: (id: number) => boolean
keysDown: Set<string>
keysPressed: Set<string>
keysPressedFixed: Set<string>
keysReleased: Set<string>
keysReleasedFixed: Set<string>
mouseButtons: Set<number>
mouseButtonsPressed: Set<number>
mouseButtonsPressedFixed: Set<number>
mouseButtonsReleased: Set<number>
mouseButtonsReleasedFixed: Set<number>
mouseX: number
mouseY: number
noteKeyDown: (code: string) => void
noteKeyUp: (code: string) => void
noteMouseDown: (button: number) => void
noteMouseUp: (button: number) => void
pointerOverUI: boolean
scrollDeltaX: number
scrollDeltaY: number
setVirtual: (id: string, x: number, y?: number) => void
touchAvailable: boolean
touches: Map<number, TouchPoint>
touchesEnded: Set<number>
touchesStarted: Map<number, TouchPoint>
updateGamepads: (snapshots: GamepadSnapshot[]) => void
static new (): InputState
static prototype: InputState
```

## InstantiatePrefabOptions — interface @experimental
```
assetBaseUrl: string | undefined
assets: Assets | undefined
overrides: PrefabOverride[] | undefined
parent: number | undefined
```

## InstantiatePrefabResult — interface @beta
```
entities: Map<number, number>
root: number
```

## Interactable — const @experimental
```
BuiltinComponentDef<InteractableData>
```

## InteractableData — interface @experimental
```
blockRaycast: boolean
enabled: boolean
raycastTarget: boolean
```

## InterestPolicy — type @experimental
```
(view: InterestView) => ReadonlySet<Entity> | 'all'
```

## InterestView — interface @experimental
```
candidates: readonly number[]
connectionId: number
world: World
```

## InterpType — type @experimental
```
(typeof InterpType)[keyof typeof InterpType]
```

## JsonResult — interface @experimental
```
data: T
```

## JustifyContent — type @experimental
```
(typeof JustifyContent)[keyof typeof JustifyContent]
```

## Key — const @public
```
(code: string) => Binding
```

## Keyframe — interface @experimental
```
inTangent: number
interpolation: InterpType | undefined
outTangent: number
time: number
value: number
```

## Keys1D — const @public
```
(neg: string, pos: string) => Binding
```

## Keys2D — const @public
```
(up: string, down: string, left: string, right: string) => Binding
```

## KineticScroll — class @experimental
```
beginDrag: () => void
endDrag: () => void
getVelocity: () => Vec2
isCoasting: () => boolean
isDragging: () => boolean
killAxis: (axis: "x" | "y") => void
sample: (delta: Vec2, dt: number) => void
stop: () => void
tick: (dt: number) => Vec2
static new (opts?: KineticScrollOptions): KineticScroll
static prototype: KineticScroll
```

## LIGHT_TOKENS — const @experimental
```
ThemeTokens
```

## LayerOrder — enum @experimental
```
Depth = 2
Painter = 0
YSort = 1
```

## LayoutProvider — interface @experimental
```
getContentSize: (count: number) => Vec2
getItemRect: (index: number) => Rect
getVisibleRange: (viewport: Rect, count: number) => [start: number, end: number]
```

## Leaderboard — const @experimental
```
ResourceDef<LeaderboardAPI>
```

## LeaderboardAPI — class @experimental
```
@internal dispose: () => void
@internal sample: () => void
available: boolean
hide: () => void
setProvider: (provider: LeaderboardProvider | null) => void
show: (options?: LeaderboardOptions) => boolean
submit: (score: number, options?: { key?: string; extra?: Readonly<Record<string, string>>; }) => boolean
texture: number
visible: boolean
static new (module_: () => ESEngineModule | null): LeaderboardAPI
static prototype: LeaderboardAPI
```

## LeaderboardOptions — interface @experimental
```
key: string | undefined
limit: number | undefined
order: "desc" | "asc" | undefined
scope: LeaderboardScope | undefined
style: LeaderboardStyle | undefined
```

## LeaderboardProvider — interface @experimental
```
canvas: () => PlatformCanvas | null
post: (message: Record<string, unknown>) => void
```

## LeaderboardScope — type @experimental
```
'friends' | 'group'
```

## LeaderboardStyle — interface @experimental
```
avatars: boolean | undefined
background: string | undefined
color: string | undefined
fontFamily: string | undefined
fontSize: number | undefined
lineColor: string | undefined
rowHeight: number | undefined
selfColor: string | undefined
```

## Lifecycle — const @experimental
```
ResourceDef<LifecycleManager>
```

## LifecycleEvent — type @experimental
```
'show' | 'hide' | 'pause' | 'resume'
```

## LifecycleListener — type @experimental
```
(event: LifecycleEvent) => void
```

## LifecycleManager — class @experimental
```
@internal emit_: (event: LifecycleEvent) => void
@internal setFocused_: (v: boolean) => void
@internal setVisible_: (v: boolean) => void
autoPause: boolean
focused: boolean
off: (listener: LifecycleListener) => void
on: (listener: LifecycleListener) => () => void
removeAllListeners: () => void
visible: boolean
static new (autoPause?: boolean): LifecycleManager
static prototype: LifecycleManager
```

## LifecyclePlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (options_?: LifecyclePluginOptions | undefined): LifecyclePlugin
static prototype: LifecyclePlugin
```

## LifecyclePluginOptions — interface @experimental
```
autoPause: boolean | undefined
```

## Light2D — const @experimental
```
BuiltinComponentDef<Light2DData>
```

## Light2DData — interface @experimental
```
color: Color
direction: Vec2
enabled: boolean
innerAngle: number
intensity: number
outerAngle: number
radius: number
shadowDistance: number
shadowSoftness: number
type: number
```

## Light2DType — enum @experimental
```
Ambient = 2
Directional = 1
Point = 0
Spot = 3
```

## LinearFillDirection — type @experimental
```
'right' | 'left' | 'up' | 'down'
```

## LinearLayoutProvider — class @experimental
```
getContentSize: (count: number) => Vec2
getItemRect: (index: number) => Rect
getVisibleRange: (viewport: Rect, count: number) => [number, number]
static new (opts: LinearLayoutOptions): LinearLayoutProvider
static prototype: LinearLayoutProvider
```

## ListItemTemplate — interface @experimental
```
bind: (entity: Entity, data: T, index: number) => void
create: (world: World, parent: Entity) => Entity
```

## ListLayoutSpec — type @experimental
```
| { itemHeight: number | ((index: number) => number); spacing?: number; direction?: 'column' }
    | { itemWidth: number; spacing?: number; direction: 'row' }
    | { columns: number; itemSize: Vec2; spacing?: Vec2 }
```

## ListView — class @experimental
```
dispose: () => void
getContentSize: () => Vec2
getMountedCount: () => number
getViewport: () => Rect
getVisibleRange: () => [number, number]
refresh: () => void
setScrollOffset: (offset: Vec2) => void
setViewportSize: (size: Vec2) => void
update: () => void
static new <T = unknown>(opts: ListViewOptions<T>): ListView<T>
static prototype: ListView<any>
```

## ListViewHandle — interface @experimental
```
content: number
data: DataSource<T>
dispose: () => void
entity: number
mountedCount: () => number
refresh: () => void
scroll: ScrollContainer
scrollToIndex: (index: number) => void
view: ListView<T>
```

## ListViewHost — interface @experimental
```
attachScrollContainer: (entity: Entity, container: ScrollContainer) => void
detachScrollContainer: (entity: Entity) => void
registerListView: (list: ListView<unknown>) => void
unregisterListView: (list: ListView<unknown>) => void
```

## ListViewOptions — interface @experimental
```
dataSource: DataSource<T>
layout: LayoutProvider
onItemBound: ((entity: Entity, data: T, index: number) => void) | undefined
parent: number
placeItem: ((world: World, entity: Entity, rect: Rect, index: number) => void) | undefined
recycleBuffer: number | undefined
setVisible: ((world: World, entity: Entity, visible: boolean) => void) | undefined
templates: Record<string, ListViewItemTemplate<T>>
viewportSize: Vec2
world: World
```

## ListenOptions — interface @public
```
gamepad: boolean | undefined
keyboard: boolean | undefined
mouse: boolean | undefined
```

## LoadRuntimeSceneOptions — interface @experimental
```
app: App
module: ESEngineModule | null
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
physicsModule: PhysicsWasmModule | null | undefined
sceneData: SceneData
sceneName: string | undefined
source: RuntimeAssetSource
spineManager: SpineManager | null | undefined
spineModule: SpineWasmModule | null | undefined
uiTheme: "dark" | "light" | undefined
uiThemeOverrides: ThemeOverrides | undefined
```

## LoadedTilemapLayer — interface @experimental
```
chunks: LoadedTilemapChunk[]
height: number
infinite: boolean
name: string
tiles: Uint16Array<ArrayBufferLike>
width: number
```

## LoadedTilemapSource — interface @experimental
```
collisionTileIds: number[] | undefined
hexSideLength: number | undefined
layers: LoadedTilemapLayer[]
objectGroups: TiledObjectGroupData[] | undefined
orientation: string | undefined
staggerAxis: string | undefined
staggerIndex: string | undefined
tileAnimations: Map<number, { tileId: number; duration: number; }[]> | undefined
tileHeight: number
tileProperties: Map<number, Map<string, string>> | undefined
tileShapes: Map<number, ResolvedTileCollision> | undefined
tileWidth: number
tilesets: LoadedTilemapTileset[]
```

## LoadedTilemapTileset — interface @experimental
```
columns: number
firstId: number
margin: number
rows: number
spacing: number
textureHandle: number
```

## LocalLeaderboardOptions — interface @experimental
```
friends: readonly CloudPlayer[] | undefined
height: number | undefined
width: number | undefined
```

## LocalTransform — const @experimental @deprecated
```
BuiltinComponentDef<TransformData>
```

## LocalTransformData — type @experimental
```
TransformData
```

## LocaleCatalog — type @experimental
```
Record<string, LocaleEntry>
```

## LocaleEntry — type @experimental
```
string | PluralForms
```

## LocaleResult — interface @experimental
```
keyCount: number
locale: string
```

## LocaleTableAsset — interface @experimental
```
entries: LocaleCatalog
locale: string
version: number
```

## Localization — const @experimental
```
ResourceDef<LocalizationAPI>
```

## LocalizationAPI — class @experimental
```
addCatalog: (locale: string, entries: LocaleCatalog) => void
availableLocales: () => string[]
fallbackLocale: string
has: (key: string) => boolean
locale: string
setFallbackLocale: (locale: string) => void
setLocale: (locale: string) => void
setPluralSelector: (locale: string, selector: PluralSelector) => void
t: (key: string, params?: TParams) => string
static new (locale?: string, fallback?: string): LocalizationAPI
static prototype: LocalizationAPI
```

## LocalizationOptions — interface @experimental
```
catalogs: Record<string, LocaleCatalog> | undefined
fallback: string | undefined
locale: string | undefined
tables: string[] | undefined
```

## LocalizationPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (opts?: LocalizationOptions): LocalizationPlugin
static prototype: LocalizationPlugin
```

## LogEntry — interface @experimental
```
category: string
data: unknown
level: LogLevel
message: string
timestamp: number
```

## LogHandler — interface @experimental
```
handle: (entry: LogEntry) => void
```

## LogLevel — enum @experimental
```
Debug = 0
Error = 3
Info = 1
Warn = 2
```

## Logger — class @experimental
```
addHandler: (handler: LogHandler) => void
clearHandlers: () => void
debug: (category: string, message: string, data?: unknown) => void
error: (category: string, message: string, data?: unknown) => void
info: (category: string, message: string, data?: unknown) => void
removeHandler: (handler: LogHandler) => void
setMinLevel: (level: LogLevel) => void
warn: (category: string, message: string, data?: unknown) => void
static new (): Logger
static prototype: Logger
```

## LoginResult — interface @experimental
```
code: string
```

## LoopMode — type @experimental
```
(typeof LoopMode)[keyof typeof LoopMode]
```

## ManifestModel — class @experimental
```
allAssets: () => AddressableManifestAsset[]
assetByKey: (key: string) => AddressableManifestAsset | null
assetByPath: (path: string) => AddressableManifestAsset | null
assetPathsInGroup: (name: string) => string[]
assetsByLabel: (label: string) => AddressableManifestAsset[]
assetsInGroup: (name: string) => AddressableManifestAsset[]
bundleMode: (name: string) => BundleMode
entries: () => Array<{ group: string; key: string; asset: AddressableManifestAsset; }>
findAsset: (pathOrAddress: string) => AddressableManifestAsset | null
group: (name: string) => AddressableManifestGroup | null
groupNames: () => string[]
groupsByMode: (mode: BundleMode) => string[]
remoteAssetPath: (ref: string) => string | null
resolvePath: (ref: string, normalize?: (ref: string) => string) => string
revision: () => string | null
textureImportLookup: () => (ref: string) => ParsedTextureImportSettings | undefined
static empty: () => ManifestModel
static fromJson: (manifest: AddressableManifest) => ManifestModel
static new (manifest: AddressableManifest): ManifestModel
static prototype: ManifestModel
```

## Marker — const @experimental
```
ComponentDef<MarkerData>
```

## MarkerData — interface @experimental
```
properties: Record<string, string>
type: string
```

## MaskMode — type @experimental
```
(typeof MaskMode)[keyof typeof MaskMode]
```

## Material — const @experimental
```
{ createShader(vertexSrc: string, fragmentSrc: string): ShaderHandle; releaseShader(shader: ShaderHandle): void; create(options: MaterialOptions): MaterialHandle; compileShader(esshaderSource: string, features?: string[]): ShaderHandle; getSwitch(material: MaterialHandle, name: string): boolean; setSwitch(material: MaterialHandle, name: string, on: boolean): void; get(material: MaterialHandle): MaterialData | undefined; setUniform(material: MaterialHandle, name: string, value: UniformValue): void; getUniform(material: MaterialHandle, name: string): UniformValue | undefined; setBlendMode(material: MaterialHandle, mode: BlendMode): void; getBlendMode(material: MaterialHandle): BlendMode; setDepthTest(material: MaterialHandle, enabled: boolean): void; setDepthWrite(material: MaterialHandle, enabled: boolean): void; setCull(material: MaterialHandle, cull: CullMode): void; getShader(material: MaterialHandle): ShaderHandle; release(material: MaterialHandle): void; isValid(material: MaterialHandle): boolean; renderPreview(material: MaterialHandle, w: number, h: number): Promise<ImageData | null>; releaseAll(): void; createFromAsset(data: MaterialAssetData, shaderHandle: ShaderHandle, parentHandle?: MaterialHandle): MaterialHandle; createInstance(source: MaterialHandle): MaterialHandle; toAssetData(material: MaterialHandle, shaderPath: string, parentPath?: string): MaterialAssetData | null; getUniforms(material: MaterialHandle): Map<string, UniformValue>; tex(textureId: number, slot?: number): TextureRef; }
```

## MaterialAssetData — interface @experimental
```
blendMode: number | undefined
cull: number | undefined
depthTest: boolean | undefined
depthWrite: boolean | undefined
instanceOf: string | undefined
properties: Record<string, unknown>
shader: string
switches: Record<string, boolean> | undefined
type: "material"
version: string
```

## MaterialData — interface @experimental
```
blendMode: BlendMode
cachedBuffer_: Float32Array<ArrayBufferLike> | null
cachedIdx_: number
cull: CullMode
depthTest: boolean
depthWrite: boolean
dirty_: boolean
overrides: Set<string>
parent: number | undefined
shader: number
switches: Record<string, boolean>
uniforms: Map<string, UniformValue>
```

## MaterialGraph — interface @beta
```
domain: string | undefined
name: string | undefined
nodes: MaterialGraphNode[]
output: string
```

## MaterialGraphNode — interface @beta
```
id: string
inputs: Record<string, string> | undefined
params: Record<string, unknown> | undefined
type: GraphNodeType
x: number | undefined
y: number | undefined
```

## MaterialHandle — type @experimental
```
number
```

## MaterialOptions — interface @experimental
```
blendMode: BlendMode | undefined
cull: CullMode | undefined
depthTest: boolean | undefined
depthWrite: boolean | undefined
shader: number
switches: Record<string, boolean> | undefined
uniforms: Record<string, UniformValue> | undefined
```

## MeasureTextOptions — interface @experimental
```
bold: boolean | undefined
fontFamily: string | undefined
fontSize: number
italic: boolean | undefined
letterSpacing: number | undefined
lineHeight: number | undefined
maxWidth: number | undefined
```

## MeasuredLinearLayoutProvider — class @experimental
```
getContentSize: (count: number) => Vec2
getItemRect: (index: number) => Rect
getVisibleRange: (viewport: Rect, count: number) => [number, number]
invalidate: () => void
static new (opts: MeasuredLinearLayoutOptions): MeasuredLinearLayoutProvider
static prototype: MeasuredLinearLayoutProvider
```

## MemoryTransport — class @experimental
```
dropPending: () => void
flush: (limit?: number) => void
on: (event: "message", handler: (data: string | ArrayBuffer) => void) => () => void
pendingCount: number
send: (data: string | ArrayBuffer) => void
static new (options?: MemoryTransportOptions): MemoryTransport
static pair: (options?: MemoryTransportOptions) => [MemoryTransport, MemoryTransport]
static prototype: MemoryTransport
```

## Mesh2D — const @experimental
```
BuiltinComponentDef<Mesh2DData>
```

## Mesh2DAPI — class @experimental
```
clearGeometry: (entity: Entity) => void
getGeometry: (entity: Entity) => Mesh2DGeometry | undefined
setGeometry: (entity: Entity, geometry: Mesh2DGeometry) => void
static new (module_: NonNullable<EngineApi>, registry_: CppRegistry): Mesh2DAPI
static prototype: Mesh2DAPI
```

## Mesh2DData — interface @experimental
```
color: Color
enabled: boolean
geometry: Mesh2DGeometry
layer: number
lit: boolean
material: number
parallax: Vec2
texture: number
```

## Mesh2DGeometry — interface @experimental
```
colors: number[] | undefined
indices: number[]
positions: number[]
uvs: number[] | undefined
```

## Mesh2DPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): Mesh2DPlugin
static prototype: Mesh2DPlugin
```

## Meshes2D — const @experimental
```
ResourceDef<Mesh2DAPI>
```

## MessageHandler — type @experimental
```
(payload: T) => void
```

## MessagePortLike — interface @experimental
```
close: (() => void) | undefined
onmessage: ((event: { data: unknown; }) => void) | null
postMessage: (data: unknown) => void
```

## MessagePortTransport — class @experimental
```
close: () => void
on: (event: "message", handler: (data: string | ArrayBuffer) => void) => () => void
send: (data: string | ArrayBuffer) => void
static new (port: MessagePortLike | MessagePort): MessagePortTransport
static prototype: MessagePortTransport
```

## MigrationResult — interface @experimental
```
data: PrefabData
fromVersion: string
migrated: boolean
toVersion: string
```

## MiniGameSocket — class @experimental
```
close: (code?: number, reason?: string) => void
connect: () => void
on: <K extends keyof PlatformSocketEvents>(event: K, handler: (...args: PlatformSocketEvents[K]) => void) => () => void
readyState: PlatformSocketReadyState
send: (data: string | ArrayBuffer) => void
static new (options: GameSocketOptions, global: MiniGameGlobal): MiniGameSocket
static prototype: MiniGameSocket
```

## MissingAssetCallback — type @experimental
```
(missing: MissingAsset[]) => void
```

## MissingAssetsError — class @experimental
```
missing: MissingAsset[]
static new (missing: MissingAsset[]): MissingAssetsError
static prototype: MissingAssetsError
```

## MockAdProviderOptions — interface @experimental
```
completed: boolean | undefined
durationMs: number | undefined
```

## Modifiers — interface @experimental
```
alt: boolean
ctrl: boolean
meta: boolean
shift: boolean
```

## MotorJoint — const @experimental
```
ComponentDef<MotorJointData>
```

## MouseButton — const @public
```
(button: number) => Binding
```

## Mut — function @public
```
<T extends AnyComponentDef>(component: T): MutWrapper<T>
```

## MutWrapper — interface @public
```
@internal _component: T
@internal _type: "mut"
```

## NODE_SPECS — const @beta
```
Record<GraphNodeType, NodeSpec>
```

## Name — const @public
```
ComponentDef<NameData>
```

## NameData — interface @public
```
value: string
```

## Nav — const @experimental
```
ResourceDef<Navigation>
```

## NavAgent — const @experimental
```
ComponentDef<NavAgentData>
```

## NavAgentData — interface @experimental
```
arriveRadius: number
arrived: boolean
hasTarget: boolean
radius: number
repathInterval: number
speed: number
targetX: number
targetY: number
```

## NavGrid — class @experimental
```
cellSize: number
cellToWorld: (gx: number, gy: number) => Vec2
clearanceAt: (gx: number, gy: number) => number
height: number
inBounds: (gx: number, gy: number) => boolean
index: (gx: number, gy: number) => number
isWalkable: (gx: number, gy: number) => boolean
nearestWalkable: (gx: number, gy: number, maxRadius?: number, clearance?: number) => Cell | null
originX: number
originY: number
setWalkable: (gx: number, gy: number, walkable: boolean) => void
walkable: Uint8Array<ArrayBufferLike>
width: number
worldToCell: (wx: number, wy: number) => Cell
static new (opts: NavGridOptions): NavGrid
static prototype: NavGrid
```

## NavGridOptions — interface @experimental
```
cellSize: number
height: number
origin: Vec2 | undefined
walkable: ArrayLike<number> | undefined
width: number
```

## NavPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): NavPlugin
static prototype: NavPlugin
```

## Navigation — class @experimental
```
findWorldPath: (from: Vec2, to: Vec2, opts?: PathfindOptions & { radius?: number; }) => Vec2[] | null
grid: NavGrid | null
hasGrid: () => boolean
setGrid: (grid: NavGrid | null) => void
static new (): Navigation
static prototype: Navigation
```

## NestedPrefabRef — interface @experimental
```
overrides: PrefabOverride[]
prefabPath: string
```

## Net — const @experimental
```
ResourceDef<NetSession>
```

## NetChannel — class @experimental
```
dispose: (reason?: string) => void
handle: <Req = unknown, Res = unknown>(type: string, handler: RequestHandler<Req, Res>) => () => void
on: <T = unknown>(type: string, handler: MessageHandler<T>) => () => void
onBinary: (channel: number, handler: BinaryHandler) => () => void
request: <Res = unknown, Req = unknown>(type: string, payload: Req, timeoutMs?: number) => Promise<Res>
send: <T = unknown>(type: string, payload: T) => void
sendBinary: (channel: number, payload: Uint8Array) => void
static new (transport: NetTransport, opts?: NetChannelOptions): NetChannel
static prototype: NetChannel
```

## NetChannelOptions — interface @experimental
```
requestTimeoutMs: number | undefined
```

## NetGhost — const @experimental
```
ComponentDef<{}>
```

## NetRoleKind — type @experimental
```
'offline' | 'server' | 'client'
```

## NetSession — class @experimental
```
client: ReplicationClient | null
connect: (transport: NetTransport, options?: ReplicationClientOptions) => Promise<ReplicationClient>
role: NetRoleKind
server: ReplicationServer | null
startServer: () => ReplicationServer
stop: () => void
static new (app_: App): NetSession
static prototype: NetSession
```

## NetTransport — interface @experimental
```
on: (event: "message", handler: (data: string | ArrayBuffer) => void) => () => void
send: (data: string | ArrayBuffer) => void
```

## NodeParamSpec — interface @beta
```
key: string
kind: "texture" | "float" | "color"
label: string
```

## NodePort — interface @beta
```
name: string
type: GraphType
```

## NodeSpec — interface @beta
```
addable: boolean
inputs: NodePort[]
label: string
output: GraphType | null
params: NodeParamSpec[]
```

## Not — function @experimental
```
(filter: FilterExpr): FilterExpr
```

## OneWayPlatform — const @experimental
```
ComponentDef<OneWayPlatformData>
```

## Or — function @experimental
```
(...filters: FilterExpr[]): FilterExpr
```

## OutlineFilterOptions — interface @experimental
```
color: { r: number; g: number; b: number; a: number; } | undefined
texelSize: { x: number; y: number; } | undefined
width: number | undefined
```

## PREFAB_FORMAT_VERSION — const @experimental
```
"2"
```

## PTR_LAYOUTS — const @experimental
```
Record<string, PtrLayout>
```

## PackagedAssetIndex — interface @experimental
```
addressOf: (ref: string) => string | null
assetPaths: () => string[]
catalog: Catalog | undefined
manifest: AddressableManifest
model: ManifestModel
resolvePath: (ref: string) => string
textureImport: (ref: string) => ParsedTextureImportSettings | undefined
```

## PackagedAssetSourceOptions — interface @experimental
```
backend: Backend | undefined
decodePixels: ((path: string, flip: boolean) => Promise<{ width: number; height: number; pixels: Uint8Array; }>) | undefined
```

## PackagedGameConfig — interface @experimental
```
achievements: string[] | undefined
audioConfig: AudioProjectConfig | undefined
colorSpace: "gamma" | "linear" | undefined
depthLayers: number | undefined
entryScene: string
hotUpdate: { remoteRoot?: string; persistUpdateKey?: string; } | undefined
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
scenes: { name: string; path: string; }[] | undefined
screenFit: { designWidth: number; designHeight: number; scaleMode: number; matchWidthOrHeight: number; } | undefined
sideModules: { id: string; file: string; globalName?: string; }[] | undefined
steamAppId: number | undefined
uiTheme: "light" | undefined
uiThemeColors: Record<string, string> | undefined
ySortLayers: number | undefined
```

## Parent — const @public
```
BuiltinComponentDef<ParentData>
```

## ParentData — interface @public
```
entity: number
```

## ParsedTextureImportSettings — interface @experimental
```
filter: "linear" | "nearest" | undefined
sliceBorder: { left: number; right: number; top: number; bottom: number; } | undefined
srgb: boolean | undefined
wrap: "repeat" | "clamp" | "mirror" | undefined
```

## Particle — const @experimental
```
ResourceDef<ParticleAPI>
```

## ParticleAPI — class @experimental
```
getAliveCount: (entity: Entity) => number
play: (entity: Entity) => void
reset: (entity: Entity) => void
setColorLut: (entity: Entity, lut: Float32Array | null) => void
setSizeLut: (entity: Entity, lut: Float32Array | null) => void
stop: (entity: Entity) => void
update: (dt: number) => void
static new (module: NonNullable<EngineApi>, registry: CppRegistry): ParticleAPI
static prototype: ParticleAPI
```

## ParticleEasing — enum @experimental
```
EaseIn = 1
EaseInOut = 3
EaseOut = 2
Linear = 0
```

## ParticleEmitter — const @experimental
```
BuiltinComponentDef<ParticleEmitterData>
```

## ParticleEmitterData — interface @experimental
```
angleSpreadMax: number
angleSpreadMin: number
angularVelocityMax: number
angularVelocityMin: number
blendMode: number
burstCount: number
burstInterval: number
collisionBounce: number
collisionEnabled: boolean
collisionFloor: number
collisionFriction: number
collisionLifetimeLoss: number
colorEasing: number
colorGradient: { stops: { t: number; color: Color; }[]; }
damping: number
duration: number
enabled: boolean
endColor: Color
endSizeMax: number
endSizeMin: number
gravity: Vec2
layer: number
lifetimeMax: number
lifetimeMin: number
looping: boolean
material: number
maxParticles: number
noiseFrequency: number
noiseOctaves: number
noiseScrollSpeed: number
noiseStrength: number
playOnStart: boolean
rate: number
rotationMax: number
rotationMin: number
shape: number
shapeAngle: number
shapeRadius: number
shapeSize: Vec2
simulationSpace: number
sizeCurve: { keys: { t: number; v: number; }[]; }
sizeEasing: number
speedMax: number
speedMin: number
spriteColumns: number
spriteFPS: number
spriteLoop: boolean
spriteRows: number
startColor: Color
startSizeMax: number
startSizeMin: number
subEmitter: number
subEmitterChance: number
subEmitterInheritVelocity: number
subEmitterTrigger: number
texture: number
trailEnabled: boolean
trailMinDistance: number
trailPoints: number
trailWidth: number
```

## ParticleForceField — const @experimental
```
BuiltinComponentDef<ParticleForceFieldData>
```

## ParticleForceFieldData — interface @experimental
```
direction: Vec2
enabled: boolean
falloff: boolean
radius: number
strength: number
type: number
```

## ParticlePlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): ParticlePlugin
static prototype: ParticlePlugin
```

## PathfindOptions — interface @experimental
```
clearance: number | undefined
diagonal: boolean | undefined
snapRadius: number | undefined
```

## Payment — const @experimental
```
ResourceDef<PaymentAPI>
```

## PaymentAPI — class @experimental
```
available: boolean
request: (request: PaymentRequest) => Promise<void>
static new (): PaymentAPI
static prototype: PaymentAPI
```

## PaymentFailure — interface @experimental
```
code: number | undefined
```

## PaymentRequest — type @experimental
```
offerId: string
quantity: number
sandbox: boolean | undefined
zoneId: string | undefined
```

## Perceiver — const @experimental
```
ComponentDef<PerceiverData>
```

## PerceiverData — interface @experimental
```
fovDegrees: number
range: number
```

## Perception — const @experimental
```
ComponentDef<PerceptionData>
```

## PerceptionData — interface @experimental
```
dirX: number
dirY: number
distance: number
targetX: number
targetY: number
visible: boolean
```

## PerceptionPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): PerceptionPlugin
static prototype: PerceptionPlugin
```

## PerceptionTarget — const @experimental
```
ComponentDef<{}>
```

## Physics — const @beta
```
ResourceDef<PhysicsAPI>
```

## PhysicsContactEventData — interface @experimental
```
approachSpeed: number | undefined
isSensor: boolean | undefined
other: number
```

## PhysicsEventType — type @experimental
```
typeof PhysicsEventType[keyof typeof PhysicsEventType]
```

## PhysicsEvents — const @beta
```
ResourceDef<PhysicsEventsData>
```

## PhysicsEventsData — interface @experimental
```
collisionEnters: CollisionEnterEvent[]
collisionExits: { entityA: Entity; entityB: Entity; }[]
collisionHits: CollisionHitEvent[]
sensorEnters: SensorEvent[]
sensorExits: SensorEvent[]
```

## PhysicsModuleFactory — type @experimental
```
(config?: Record<string, unknown>) => Promise<PhysicsWasmModule>
```

## PhysicsPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (wasmUrl: string, config?: PhysicsPluginConfig, factory?: PhysicsModuleFactory): PhysicsPlugin
static prototype: PhysicsPlugin
```

## PhysicsPluginConfig — interface @experimental
```
collisionLayerMasks: number[] | undefined
contactDampingRatio: number | undefined
contactHertz: number | undefined
contactSpeed: number | undefined
enableContinuous: boolean | undefined
enableSleep: boolean | undefined
fixedTimestep: number | undefined
gravity: Vec2 | undefined
maxLinearSpeed: number | undefined
restitutionThreshold: number | undefined
subStepCount: number | undefined
```

## PhysicsWasmModule — interface @experimental
```
HEAPF32: Float32Array<ArrayBufferLike>
HEAPU32: Uint32Array<ArrayBufferLike>
HEAPU8: Uint8Array<ArrayBufferLike>
_free: (ptr: number) => void
_malloc: (size: number) => number
_physics_addBoxShape: (entityId: number, halfW: number, halfH: number, offX: number, offY: number, radius: number, density: number, friction: number, restitution: number, isSensor: number, categoryBits: number, maskBits: number) => void
_physics_addCapsuleShape: (entityId: number, radius: number, halfHeight: number, offX: number, offY: number, density: number, friction: number, restitution: number, isSensor: number, categoryBits: number, maskBits: number) => void
_physics_addChainShape: (entityId: number, pointsPtr: number, pointCount: number, isLoop: number, friction: number, restitution: number, categoryBits: number, maskBits: number) => void
_physics_addCircleShape: (entityId: number, radius: number, offX: number, offY: number, density: number, friction: number, restitution: number, isSensor: number, categoryBits: number, maskBits: number) => void
_physics_addPolygonShape: (entityId: number, verticesPtr: number, vertexCount: number, radius: number, density: number, friction: number, restitution: number, isSensor: number, categoryBits: number, maskBits: number) => void
_physics_addSegmentShape: (entityId: number, x1: number, y1: number, x2: number, y2: number, density: number, friction: number, restitution: number, isSensor: number, categoryBits: number, maskBits: number) => void
_physics_applyAngularImpulse: (entityId: number, impulse: number) => void
_physics_applyForce: (entityId: number, forceX: number, forceY: number) => void
_physics_applyImpulse: (entityId: number, impulseX: number, impulseY: number) => void
_physics_applyTorque: (entityId: number, torque: number) => void
_physics_capturePoses: () => void
_physics_clearShapes: (entityId: number) => void
_physics_collectEvents: () => void
_physics_createBody: (entityId: number, bodyType: number, x: number, y: number, angle: number, gravityScale: number, linearDamping: number, angularDamping: number, fixedRotation: number, bullet: number) => void
_physics_createDistanceJoint: (entityIdA: number, entityIdB: number, anchorAx: number, anchorAy: number, anchorBx: number, anchorBy: number, length: number, enableSpring: number, hertz: number, dampingRatio: number, enableLimit: number, minLength: number, maxLength: number, enableMotor: number, maxMotorForce: number, motorSpeed: number, collideConnected: number) => number
_physics_createMotorJoint: (entityIdA: number, entityIdB: number, linearVelX: number, linearVelY: number, maxVelocityForce: number, angularVelocity: number, maxVelocityTorque: number, linearHertz: number, linearDampingRatio: number, maxSpringForce: number, angularHertz: number, angularDampingRatio: number, maxSpringTorque: number, collideConnected: number) => number
_physics_createMouseJoint: (entityId: number, targetX: number, targetY: number, hertz: number, dampingRatio: number, maxForce: number) => number
_physics_createPrismaticJoint: (entityIdA: number, entityIdB: number, anchorAx: number, anchorAy: number, anchorBx: number, anchorBy: number, axisX: number, axisY: number, enableSpring: number, hertz: number, dampingRatio: number, enableLimit: number, lowerTranslation: number, upperTranslation: number, enableMotor: number, maxMotorForce: number, motorSpeed: number, collideConnected: number) => number
_physics_createRevoluteJoint: (entityIdA: number, entityIdB: number, anchorAx: number, anchorAy: number, anchorBx: number, anchorBy: number, enableMotor: number, motorSpeed: number, maxMotorTorque: number, enableLimit: number, lowerAngle: number, upperAngle: number, collideConnected: number) => number
_physics_createWeldJoint: (entityIdA: number, entityIdB: number, anchorAx: number, anchorAy: number, anchorBx: number, anchorBy: number, linearHertz: number, angularHertz: number, linearDampingRatio: number, angularDampingRatio: number, collideConnected: number) => number
_physics_createWheelJoint: (entityIdA: number, entityIdB: number, anchorAx: number, anchorAy: number, anchorBx: number, anchorBy: number, axisX: number, axisY: number, enableSpring: number, hertz: number, dampingRatio: number, enableLimit: number, lowerTranslation: number, upperTranslation: number, enableMotor: number, maxMotorTorque: number, motorSpeed: number, collideConnected: number) => number
_physics_destroyBody: (entityId: number) => void
_physics_destroyJoint: (entityId: number) => void
_physics_destroyMouseJoint: () => void
_physics_enableDistanceJointLimit: (entityId: number, enable: number) => void
_physics_enableDistanceJointMotor: (entityId: number, enable: number) => void
_physics_enableDistanceJointSpring: (entityId: number, enable: number) => void
_physics_enablePrismaticJointLimit: (entityId: number, enable: number) => void
_physics_enablePrismaticJointMotor: (entityId: number, enable: number) => void
_physics_enablePrismaticJointSpring: (entityId: number, enable: number) => void
_physics_enableRevoluteLimit: (entityId: number, enable: number) => void
_physics_enableRevoluteMotor: (entityId: number, enable: number) => void
_physics_enableWheelJointLimit: (entityId: number, enable: number) => void
_physics_enableWheelJointMotor: (entityId: number, enable: number) => void
_physics_enableWheelJointSpring: (entityId: number, enable: number) => void
_physics_getAngularVelocity: (entityId: number) => number
_physics_getBodyCenterOfMass: (entityId: number) => number
_physics_getBodyCount: () => number
_physics_getBodyInertia: (entityId: number) => number
_physics_getBodyMass: (entityId: number) => number
_physics_getCollisionEnterBuffer: () => number
_physics_getCollisionEnterCount: () => number
_physics_getCollisionExitBuffer: () => number
_physics_getCollisionExitCount: () => number
_physics_getDistanceJointCurrentLength: (entityId: number) => number
_physics_getDistanceJointLength: (entityId: number) => number
_physics_getDistanceJointMotorForce: (entityId: number) => number
_physics_getDynamicBodyCount: () => number
_physics_getDynamicBodyTransforms: () => number
_physics_getGravity: () => number
_physics_getHitEventBuffer: () => number
_physics_getHitEventCount: () => number
_physics_getInterpolatedCount: () => number
_physics_getInterpolatedTransforms: (alpha: number) => number
_physics_getJointCount: () => number
_physics_getLinearVelocity: (entityId: number) => number
_physics_getMoveCharacterBuffer: () => number
_physics_getOverlapBuffer: () => number
_physics_getPrismaticJointMotorForce: (entityId: number) => number
_physics_getPrismaticJointSpeed: (entityId: number) => number
_physics_getPrismaticJointTranslation: (entityId: number) => number
_physics_getRaycastBuffer: () => number
_physics_getRevoluteAngle: (entityId: number) => number
_physics_getRevoluteMotorTorque: (entityId: number) => number
_physics_getSensorEnterBuffer: () => number
_physics_getSensorEnterCount: () => number
_physics_getSensorExitBuffer: () => number
_physics_getSensorExitCount: () => number
_physics_getShapeCastBuffer: () => number
_physics_getShapeCount: () => number
_physics_getTrackingRows: () => number
_physics_getWheelJointMotorTorque: (entityId: number) => number
_physics_hasBody: (entityId: number) => number
_physics_hasJoint: (entityId: number) => number
_physics_hasMouseJoint: () => number
_physics_init: (gx: number, gy: number, timestep: number, substeps: number, contactHertz: number, contactDampingRatio: number, contactSpeed: number) => void
_physics_isAwake: (entityId: number) => number
_physics_moveCharacter: (px: number, py: number, c1x: number, c1y: number, c2x: number, c2y: number, radius: number, velX: number, velY: number, dt: number, upX: number, upY: number, floorCos: number, maskBits: number, selfEntity: number, skinWidth: number, maxSlides: number, snapLength: number, slideOnCeiling: number) => number
_physics_overlapAABB: (minX: number, minY: number, maxX: number, maxY: number, maskBits: number) => number
_physics_overlapCircle: (centerX: number, centerY: number, radius: number, maskBits: number) => number
_physics_raycast: (originX: number, originY: number, dirX: number, dirY: number, maxDistance: number, maskBits: number) => number
_physics_setAngularVelocity: (entityId: number, omega: number) => void
_physics_setAwake: (entityId: number, awake: number) => void
_physics_setBodyEnabled: (entityId: number, enabled: number) => void
_physics_setBodyTargetTransform: (entityId: number, x: number, y: number, angle: number, dt: number) => void
_physics_setBodyTransform: (entityId: number, x: number, y: number, angle: number) => void
_physics_setDistanceJointLength: (entityId: number, length: number) => void
_physics_setDistanceJointLimits: (entityId: number, minLength: number, maxLength: number) => void
_physics_setDistanceJointMaxMotorForce: (entityId: number, force: number) => void
_physics_setDistanceJointMotorSpeed: (entityId: number, speed: number) => void
_physics_setGravity: (gx: number, gy: number) => void
_physics_setLinearVelocity: (entityId: number, vx: number, vy: number) => void
_physics_setMotorJointAngularVelocity: (entityId: number, omega: number) => void
_physics_setMotorJointLinearVelocity: (entityId: number, vx: number, vy: number) => void
_physics_setMotorJointMaxVelocityForce: (entityId: number, force: number) => void
_physics_setMotorJointMaxVelocityTorque: (entityId: number, torque: number) => void
_physics_setMouseTarget: (targetX: number, targetY: number) => void
_physics_setOneWayPlatform: (entityId: number, nx: number, ny: number, enable: number) => void
_physics_setPrismaticJointLimits: (entityId: number, lower: number, upper: number) => void
_physics_setPrismaticJointMaxMotorForce: (entityId: number, force: number) => void
_physics_setPrismaticJointMotorSpeed: (entityId: number, speed: number) => void
_physics_setRevoluteLimits: (entityId: number, lower: number, upper: number) => void
_physics_setRevoluteMaxMotorTorque: (entityId: number, torque: number) => void
_physics_setRevoluteMotorSpeed: (entityId: number, speed: number) => void
_physics_setWheelJointLimits: (entityId: number, lower: number, upper: number) => void
_physics_setWheelJointMaxMotorTorque: (entityId: number, torque: number) => void
_physics_setWheelJointMotorSpeed: (entityId: number, speed: number) => void
_physics_setWorldConfig: (enableSleep: number, enableContinuous: number, restitutionThreshold: number, maxLinearSpeed: number) => void
_physics_shapeCastBox: (centerX: number, centerY: number, halfW: number, halfH: number, angle: number, translationX: number, translationY: number, maskBits: number) => number
_physics_shapeCastCapsule: (center1X: number, center1Y: number, center2X: number, center2Y: number, radius: number, translationX: number, translationY: number, maskBits: number) => number
_physics_shapeCastCircle: (centerX: number, centerY: number, radius: number, translationX: number, translationY: number, maskBits: number) => number
_physics_shutdown: () => void
_physics_step: (dt: number) => void
_physics_updateBodyProperties: (entityId: number, bodyType: number, gravityScale: number, linearDamping: number, angularDamping: number, fixedRotation: number, bullet: number) => void
```

## PickableWorld — type @experimental
```
Pick<World, 'getCppRegistry' | 'getWasmModule'>
```

## PlatformAdapter — interface @experimental
```
bindInputEvents: (callbacks: InputEventCallbacks, target?: unknown) => void
canPay: (() => boolean) | undefined
canSignIn: (() => boolean) | undefined
checkSession: (() => Promise<boolean>) | undefined
clearStorage: (prefix: string) => void
createAudioBackend: (() => PlatformAudioBackend) | undefined
createCanvas: (width: number, height: number) => PlatformCanvas
createImage: () => PlatformImage
createInterstitialAd: ((adUnitId: string) => PlatformInterstitialAd | null) | undefined
createRewardedAd: ((adUnitId: string) => PlatformRewardedAd | null) | undefined
createSocket: ((options: PlatformSocketOptions) => PlatformSocket) | undefined
createTextEditor: (() => PlatformTextEditor | null) | undefined
createVideoBackend: ((ctx: VideoBackendContext) => PlatformVideoBackend) | undefined
devicePixelRatio: () => number
family: "minigame" | undefined
fetch: (url: string, options?: PlatformRequestOptions) => Promise<PlatformResponse>
fileExists: (path: string) => Promise<boolean>
getStorageItem: (key: string) => string | null
hasTouch: (() => boolean) | undefined
instantiateWasm: (pathOrBuffer: string | ArrayBuffer, imports: WebAssembly.Imports) => Promise<WasmInstantiateResult>
language: (() => string) | undefined
loadImagePixels: (path: string) => Promise<ImageLoadResult>
loadSubpackage: ((name: string) => Promise<void>) | undefined
login: (() => Promise<string>) | undefined
name: PlatformName
now: () => number
onAppHide: ((callback: () => void) => () => void) | undefined
onAppShow: ((callback: () => void) => () => void) | undefined
onContextLost: ((callback: () => void) => () => void) | undefined
onMemoryWarning: ((callback: () => void) => () => void) | undefined
onShareRequest: ((provide: () => PlatformShareOptions) => void) | undefined
onStoreOverlay: ((listener: (covered: boolean) => void) => () => void) | undefined
onUnhandledError: ((callback: (error: unknown) => void) => () => void) | undefined
openDataCanvas: (() => PlatformCanvas | null) | undefined
openDataPostMessage: ((message: Record<string, unknown>) => void) | undefined
pollGamepads: (() => GamepadSnapshot[]) | undefined
rasterizeGlyph: ((request: PlatformGlyphRequest) => PlatformGlyph | null) | undefined
readCacheFile: ((key: string) => Promise<ArrayBuffer | null>) | undefined
readFile: (path: string) => Promise<ArrayBuffer>
readTextFile: (path: string) => Promise<string>
registerFont: ((family: string, bytes: ArrayBuffer) => Promise<void>) | undefined
removeStorageItem: (key: string) => void
requestPayment: ((request: PlatformPaymentRequest) => Promise<void>) | undefined
setCloudKeyValues: ((entries: Readonly<Record<string, string>>) => boolean) | undefined
setStorageItem: (key: string, value: string) => void
setWindowSize: ((width: number, height: number) => void) | undefined
share: ((options: PlatformShareOptions) => void) | undefined
steamAchievements: ((appId: number) => AchievementProvider | null) | undefined
steamIdentity: (() => { id: string; name: string; } | null) | undefined
unbindInputEvents: (() => void) | undefined
writeCacheFile: ((key: string, bytes: ArrayBuffer) => Promise<void>) | undefined
```

## PlatformAudioBackend — interface @experimental
```
dispose: () => void
ensureResumed: () => Promise<void>
getFrequencyData: ((out: Uint8Array) => boolean) | undefined
initialize: (options?: AudioBackendInitOptions) => Promise<void>
isReady: boolean
loadBuffer: (url: string) => Promise<AudioBufferHandle>
loadBufferFromData: (url: string, data: ArrayBuffer) => Promise<AudioBufferHandle>
mixer: AudioMixer | null
name: string
play: (buffer: AudioBufferHandle, config: PlayConfig) => AudioHandle
resume: () => void
suspend: () => void
unloadBuffer: (handle: AudioBufferHandle) => void
```

## PlatformCanvas — interface @experimental
```
getContext: (contextId: "2d", options?: PlatformCanvasContextAttributes) => PlatformCanvas2DContext | null
height: number
width: number
```

## PlatformCanvas2DContext — interface @experimental
```
clearRect: (x: number, y: number, w: number, h: number) => void
drawImage: (image: PlatformImageSource, dx: number, dy: number) => void
fillStyle: PlatformCanvasPaint
fillText: (text: string, x: number, y: number) => void
font: string
getImageData: (sx: number, sy: number, sw: number, sh: number) => PlatformImageData
measureText: (text: string) => PlatformTextMetrics
textAlign: string
textBaseline: string
```

## PlatformImage — interface @experimental
```
crossOrigin: string | null | undefined
height: number
onerror: PlatformImageEventHandler
onload: PlatformImageEventHandler
src: string
width: number
```

## PlatformRequestOptions — interface @experimental
```
body: string | ArrayBuffer | undefined
headers: Record<string, string> | undefined
method: "GET" | "POST" | "PUT" | "DELETE" | "HEAD" | "OPTIONS" | undefined
responseType: "json" | "text" | "arraybuffer" | undefined
timeout: number | undefined
```

## PlatformResponse — interface @experimental
```
arrayBuffer: () => Promise<ArrayBuffer>
headers: Record<string, string>
json: <T = unknown>() => Promise<T>
ok: boolean
status: number
statusText: string
text: () => Promise<string>
```

## PlatformSocket — interface @experimental
```
close: (code?: number, reason?: string) => void
connect: () => void
on: <K extends keyof PlatformSocketEvents>(event: K, handler: (...args: PlatformSocketEvents[K]) => void) => () => void
readyState: PlatformSocketReadyState
send: (data: string | ArrayBuffer) => void
```

## PlatformSocketEvents — interface @experimental
```
close: [code: number, reason: string]
error: [error: unknown]
message: [data: string | ArrayBuffer]
open: []
```

## PlatformSocketOptions — interface @experimental
```
protocols: string | string[] | undefined
url: string
```

## PlatformSocketReadyState — type @experimental
```
'connecting' | 'open' | 'closing' | 'closed'
```

## PlatformType — type @experimental @deprecated
```
PlatformName
```

## PlatformVideoBackend — interface @experimental
```
createStream: (url: string, options: VideoStreamOptions) => VideoStreamHandle
dispose: () => void
name: string
```

## PlayConfig — interface @experimental
```
bus: string | undefined
loop: boolean | undefined
pan: number | undefined
playbackRate: number | undefined
priority: number | undefined
startOffset: number | undefined
volume: number | undefined
```

## PlayRealmRuntimeConfig — interface @experimental
```
achievements: string[] | undefined
app: App
assetBaseUrl: string | undefined
assetManifest: Record<string, string>
assetPathMap: Record<string, string> | undefined
audioConfig: AudioProjectConfig | undefined
canvas: HTMLCanvasElement
catalogData: CatalogData | null | undefined
enableStats: boolean | undefined
entrySceneName: string | undefined
extraScenes: { name: string; data?: SceneData; path?: string; }[] | undefined
manifest: AddressableManifest | null | undefined
module: ESEngineModule
persistUpdateKey: string | undefined
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
remoteRoot: string | undefined
sceneData: SceneData
uiTheme: "dark" | "light" | undefined
uiThemeOverrides: ThemeOverrides | undefined
wasmBaseUrl: string | undefined
```

## PlayableAdBridge — interface @experimental
```
cta: (() => void) | undefined
```

## PlayableRuntimeConfig — interface @experimental
```
app: App
assetPathMap: Record<string, string> | undefined
assets: Record<string, string>
audioConfig: AudioProjectConfig | undefined
canvas: HTMLCanvasElement
depthLayers: number | undefined
firstScene: string
manifest: AddressableManifest | null | undefined
module: ESEngineModule
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
scenes: { name: string; data: SceneData; }[]
uiTheme: "dark" | "light" | undefined
uiThemeOverrides: ThemeOverrides | undefined
ySortLayers: number | undefined
```

## Plugin — interface @experimental
```
after: string[] | undefined
before: string[] | undefined
build: (app: App) => void
cleanup: ((app?: App) => void) | undefined
dependencies: PluginDependency[] | undefined
finish: ((app: App) => void) | undefined
name: string | undefined
```

## PluginDependency — type @experimental
```
string | ResourceDef<any>
```

## PluralCategory — type @experimental
```
'zero' | 'one' | 'two' | 'few' | 'many' | 'other'
```

## PluralForms — interface @experimental
```
few: string | undefined
many: string | undefined
one: string | undefined
other: string
two: string | undefined
zero: string | undefined
```

## PluralSelector — type @experimental
```
(count: number) => PluralCategory
```

## PolygonCollider — const @experimental
```
ComponentDef<PolygonColliderData>
```

## PooledAudioNode — interface @experimental
```
gain: GainNode
inUse: boolean
panner: StereoPannerNode
source: AudioBufferSourceNode | null
startTime: number
```

## PostProcess — const @experimental
```
ResourceDef<PostProcessAPI>
```

## PostProcessAPI — class @experimental
```
_applyForCamera: (camera: Entity) => void
_applyScreenStack: () => void
_beginScreenCapture: () => void
_endScreenCapture: () => void
_executeScreenPasses: () => void
_resetAfterCamera: () => void
begin: () => void
bind: (camera: Entity, stack: PostProcessStack) => void
createStack: () => PostProcessStack
end: () => void
getStack: (camera: Entity) => PostProcessStack | null
init: (width: number, height: number) => boolean
isInitialized: () => boolean
resize: (width: number, height: number) => void
screenStack: PostProcessStack | null
setBypass: (bypass: boolean) => void
setOutputViewport: (x: number, y: number, w: number, h: number) => void
setScreenStack: (stack: PostProcessStack | null) => void
shutdown: () => void
state: PostProcessState
unbind: (camera: Entity) => void
volumeShaders: Map<string, number>
volumeStacks: Map<number, PostProcessStack>
static new (): PostProcessAPI
static prototype: PostProcessAPI
```

## PostProcessPlugin — class @experimental
```
build: (app: App) => void
cleanup: (app?: App) => void
name: string
static new (): PostProcessPlugin
static prototype: PostProcessPlugin
```

## PostProcessStack — class @experimental
```
addPass: (name: string, shader: ShaderHandle) => PostProcessStack
clearDirty: () => void
clearPasses: () => PostProcessStack
destroy: () => void
enabledPassCount: number
id: number
isDestroyed: boolean
isDirty: boolean
passCount: number
passes: readonly PassConfig[]
removePass: (name: string) => PostProcessStack
setAllPassesEnabled: (enabled: boolean) => void
setEnabled: (name: string, enabled: boolean) => PostProcessStack
setTexture: (passName: string, uniform: string, textureHandle: number) => PostProcessStack
setUniform: (passName: string, uniform: string, value: number) => PostProcessStack
setUniformVec4: (passName: string, uniform: string, value: Vec4) => PostProcessStack
static new (state: PostProcessState): PostProcessStack
static prototype: PostProcessStack
```

## PostProcessVolume — const @experimental
```
ComponentDef<PostProcessVolumeData>
```

## PostProcessVolumeData — interface @experimental
```
blendDistance: number
effects: { type: string; enabled: boolean; uniforms: Record<string, number>; textures?: Record<string, string>; }[]
isGlobal: boolean
priority: number
shape: "box" | "sphere"
size: { x: number; y: number; }
weight: number
```

## PreSceneDrawCallback — type @experimental
```
(info: PreSceneDrawInfo) => void
```

## PreSceneDrawInfo — interface @experimental
```
elapsed: number
height: number
width: number
```

## PredictionOptions — interface @experimental
```
apply: (world: World, entity: Entity, actions: Record<string, unknown>, dt: number) => void
maxPendingInputs: number | undefined
smoothing: PredictionSmoothing | undefined
```

## PredictionSmoothing — interface @experimental
```
halfLife: number
maxError: number | undefined
```

## PrefabComponentData — interface @experimental
```
data: Record<string, unknown>
type: string
```

## PrefabData — interface @experimental
```
basePrefab: string | undefined
entities: PrefabEntityData[]
name: string
overrides: PrefabOverride[] | undefined
rootEntityId: string
version: string
```

## PrefabDiagnostic — interface @experimental
```
code: string
entityId: string | undefined
field: string | undefined
message: string
severity: PrefabDiagnosticSeverity
```

## PrefabDiagnosticSeverity — type @experimental
```
'error' | 'warning'
```

## PrefabEntityData — interface @experimental
```
children: string[]
components: ComponentData[]
metadata: Record<string, unknown> | undefined
name: string
nestedPrefab: NestedPrefabRef | undefined
parent: string | null
prefabEntityId: string
visible: boolean
```

## PrefabEntityId — type @experimental
```
string
```

## PrefabInstanceDelta — interface @experimental
```
added: AddedEntity[]
overrides: PrefabOverride[]
prefab: string
removed: string[]
```

## PrefabInstanceEntry — interface @experimental
```
added: AddedEntity[]
id: number
overrides: PrefabOverride[]
parent: number | null
prefab: string
removed: string[]
```

## PrefabOverride — interface @beta
```
componentData: ComponentData | undefined
componentType: string | undefined
metadataKey: string | undefined
prefabEntityId: string
propertyName: string | undefined
type: "property" | "component_added" | "component_replaced" | "component_removed" | "name" | "visibility" | "parent" | "metadata_set" | "metadata_removed"
value: unknown
```

## PrefabServer — class @beta
```
instantiate: (pathOrAddress: string, options?: { baseUrl?: string; parent?: Entity; overrides?: SpawnOverride[]; scene?: boolean; }) => Promise<InstantiatePrefabResult>
static new (world: World, getAssets: () => AssetsClass, getScenes?: () => SceneManagerState | null): PrefabServer
static prototype: PrefabServer
```

## Prefabs — const @beta
```
ResourceDef<PrefabServer>
```

## PrefabsPlugin — class @experimental
```
build: (app: App) => void
dependencies: ResourceDef<AssetsClass>[]
name: string
static new (): PrefabsPlugin
static prototype: PrefabsPlugin
```

## PreviewPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
waitForReady: () => Promise<void>
static new (sceneUrl: string, baseUrl?: string): PreviewPlugin
static prototype: PreviewPlugin
```

## PrismaticJoint — const @experimental
```
ComponentDef<PrismaticJointData>
```

## ProbedRegistrations — interface @experimental
```
fingerprint: string
pending: PendingSystemEntry[]
```

## ProcessedEntity — interface @experimental
```
children: number[]
components: ComponentData[]
id: number
metadata: Record<string, unknown> | undefined
name: string
parent: number | null
prefabEntityId: string
visible: boolean
```

## ProgressHandle — interface @experimental
```
dispose: () => void
entity: number
fillEntity: number
getValue: () => number
setValue: (v: number) => void
```

## ProgressOptions — interface @experimental
```
background: UIVisualInit | undefined
direction: LinearFillDirection | undefined
fill: { color?: Color; sprite?: number; } | undefined
node: UINodeInit | undefined
parent: number | undefined
radial: boolean | undefined
value: number | undefined
world: World
```

## ProjectionType — enum @experimental
```
Orthographic = 1
Perspective = 0
```

## PropertyChannel — interface @experimental
```
keyframes: Keyframe[]
property: string
```

## PropertyTrack — interface @experimental
```
channels: PropertyChannel[]
childPath: string
component: string
name: string
type: "property"
```

## PtrLayout — interface @experimental
```
fields: readonly PtrFieldDesc[]
ptrFn: string
```

## Quat — interface @public
```
w: number
x: number
y: number
z: number
```

## Query — function @public
```
<C extends QueryArg[]>(...components: C): QueryBuilder<C>
```

## QueryArg — type @public
```
AnyComponentDef | MutWrapper<AnyComponentDef> | AddedWrapper<AnyComponentDef> | ChangedWrapper<AnyComponentDef>
```

## QueryBuilder — interface @public
```
@internal _addedFilters: { index: number; component: AnyComponentDef; }[]
@internal _changedFilters: { index: number; component: AnyComponentDef; }[]
@internal _components: C
@internal _filter: FilterExpr | null
@internal _mutIndices: number[]
@internal _type: "query"
@internal _with: AnyComponentDef[]
@internal _without: AnyComponentDef[]
filter: (expr: FilterExpr) => QueryBuilder<C>
with: (...components: AnyComponentDef[]) => QueryBuilder<C>
without: (...components: AnyComponentDef[]) => QueryBuilder<C>
```

## QueryDescriptor — interface @public
```
@internal _addedFilters: { index: number; component: AnyComponentDef; }[]
@internal _changedFilters: { index: number; component: AnyComponentDef; }[]
@internal _components: C
@internal _filter: FilterExpr | null
@internal _mutIndices: number[]
@internal _type: "query"
@internal _with: AnyComponentDef[]
@internal _without: AnyComponentDef[]
```

## QueryInstance — class @public
```
@internal resetTick: (tick: number) => void
[Symbol.iterator]: () => Iterator<QueryResult<C>>
count: () => number
forEach: (callback: (entity: Entity, ...components: ComponentsData<C>) => void) => void
isEmpty: () => boolean
single: () => QueryResult<C> | null
toArray: () => QueryResult<C>[]
static new <C extends readonly QueryArg[]>(world: World, descriptor: QueryDescriptor<C>, lastRunTick?: number): QueryInstance<C>
static prototype: QueryInstance<any>
```

## QueryResult — type @public
```
[
    Entity,
    ...ComponentsData<C>
]
```

## REPLICATION_CHANNEL — const @experimental
```
1
```

## REPLICATION_PROTOCOL_VERSION — const @experimental
```
2
```

## RETIRED_COMPONENT_TYPES — const @experimental
```
ReadonlySet<string>
```

## RadiusInterestOptions — interface @experimental
```
position: ((world: World, entity: Entity) => { x: number; y: number; } | null) | undefined
```

## ReadonlySignal — interface @experimental
```
get: () => T
subscribe: (fn: (value: T) => void) => () => void
```

## Removed — function @experimental
```
<T extends AnyComponentDef>(component: T): RemovedQueryDescriptor<T>
```

## RemovedQueryDescriptor — interface @public
```
@internal _component: T
@internal _type: "removed"
```

## RemovedQueryInstance — class @public
```
@internal resetTick: (tick: number) => void
[Symbol.iterator]: () => Iterator<Entity>
isEmpty: () => boolean
toArray: () => Entity[]
static new <T extends AnyComponentDef>(world: World, component: T, lastRunTick: number): RemovedQueryInstance<T>
static prototype: RemovedQueryInstance<any>
```

## RenderParams — interface @experimental
```
clearColor: { x: number; y: number; z: number; w: number; } | undefined
elapsed: number
height: number
registry: { _cpp: CppRegistry; }
viewProjection: Float32Array<ArrayBufferLike>
width: number
```

## RenderPipeline — class @experimental
```
addPreFlushCallback: (cb: (registry: { _cpp: CppRegistry; }) => void) => void
beginFrame: (elapsedSec?: number) => void
beginScreenCapture: () => void
endScreenCapture: () => void
render: (params: RenderParams) => void
renderCamera: (params: CameraRenderParams) => void
runPreFlushCallbacks: (registry: { _cpp: CppRegistry; }) => void
setActiveScenes: (scenes: Set<string> | null) => void
setPostProcess: (pp: PostProcessAPI | null) => void
submitScene: (registry: { _cpp: CppRegistry; }, viewProjection: Float32Array, viewport: Viewport, _elapsed: number) => void
static new (): RenderPipeline
static prototype: RenderPipeline
```

## RenderStage — enum @experimental
```
Background = 0
Opaque = 1
Overlay = 3
Transparent = 2
```

## RenderStats — interface @experimental
```
culled: number
drawCalls: number
meshes: number
spine: number
sprites: number
text: number
triangles: number
```

## RenderSurfaceSource — type @experimental
```
| { readonly kind: 'gl-context'; readonly handle: number }
    | { readonly kind: 'webgpu'; readonly canvasSelector?: string }
    | { readonly kind: 'default' }
```

## RenderTargetHandle — type @experimental
```
number
```

## RenderTexture — const @experimental
```
{ create(options: RenderTextureOptions): RenderTextureHandle; release(rt: RenderTextureHandle): void; resize(rt: RenderTextureHandle, width: number, height: number): RenderTextureHandle; begin(rt: RenderTextureHandle, viewProjection: Float32Array): void; end(): void; getDepthTexture(rt: RenderTextureHandle): number; }
```

## RenderTextureHandle — interface @experimental
```
_depth: boolean
_filter: "linear" | "nearest"
_handle: number
height: number
texture: number
textureId: number
width: number
```

## RenderTextureOptions — interface @experimental
```
depth: boolean | undefined
filter: "linear" | "nearest" | undefined
height: number
width: number
```

## RenderType — enum @experimental
```
ExternalMesh = 3
Mesh = 2
Particle = 5
Shape = 6
Spine = 1
Sprite = 0
Text = 4
UIElement = 7
```

## Renderer — const @experimental
```
{ init(width: number, height: number): void; resize(width: number, height: number): void; beginFrame(elapsedSec?: number): void; updateTransforms(registry: { _cpp: CppRegistry; }): void; begin(viewProjection: Float32Array, target?: RenderTargetHandle, clearFlags?: number, clearColor?: { x: number; y: number; z: number; w: number; }, clearRect?: { x: number; y: number; w: number; h: number; }): void; flush(): void; end(): void; submitAll(registry: { _cpp: CppRegistry; }, skipFlags: number, vpX: number, vpY: number, vpW: number, vpH: number): void; setStage(stage: RenderStage): void; createRenderTarget(width: number, height: number, flags?: number): RenderTargetHandle; releaseRenderTarget(handle: RenderTargetHandle): void; getTargetTexture(handle: RenderTargetHandle): number; getTargetDepthTexture(handle: RenderTargetHandle): number; setClearColor(r: number, g: number, b: number, a: number): void; setViewport(x: number, y: number, w: number, h: number): void; setYSortLayers(mask: number): void; setDepthLayers(mask: number): void; setCullingMask(mask: number): void; setTextureParams(textureId: number, minFilter: number, magFilter: number, wrapS: number, wrapT: number): void; measureBitmapText(fontHandle: number, text: string, fontSize: number, spacing: number): { width: number; height: number; }; getStats(): RenderStats; captureNextFrame(): void; getCapturedData(): FrameCaptureData | null; hasCapturedData(): boolean; replayToDrawCall(drawCallIndex: number): void; getSnapshotImageData(): Promise<ImageData | null>; }
```

## Replicated — const @experimental
```
ComponentDef<ReplicatedData>
```

## ReplicatedData — interface @experimental
```
netId: number
owner: number
```

## ReplicationClient — class @experimental
```
@internal setFixedDelta: (dt: number) => void
applyPending: () => void
connect: (transport: NetTransport) => Promise<void>
connected: boolean
connectionId: number
disconnect: () => void
enablePrediction: (options: PredictionOptions) => void
netIds: NetIds
ownsEntity: (entity: Entity) => boolean
predictionEnabled: boolean
sampleInterpolation: (deltaTicks: number) => void
sendInput: (actions: Record<string, unknown>) => void
serverTick: number
table: ReplicationTable
static new (world: World, options?: ReplicationClientOptions): ReplicationClient
static prototype: ReplicationClient
```

## ReplicationPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): ReplicationPlugin
static prototype: ReplicationPlugin
```

## ReplicationServer — class @experimental
```
attachConnection: (transport: NetTransport) => number
beginTick: (fixedDelta: number) => void
clientIds: number[]
connectionCount: number
detachConnection: (id: number) => void
inputOf: (connectionId: number) => ReplInputMsg | null
netIds: NetIds
sample: (tick: number) => void
setInterestPolicy: (policy: InterestPolicy | null) => void
table: ReplicationTable
tickInputOf: (connectionId: number) => ReplInputMsg | null
static new (world: World): ReplicationServer
static prototype: ReplicationServer
```

## RequestHandler — type @experimental
```
(payload: Req) => Res | Promise<Res>
```

## Res — function @public
```
<T>(resource: ResourceDef<T>): ResDescriptor<T>
```

## ResDescriptor — interface @public
```
@internal _resource: ResourceDef<T>
@internal _type: "res"
```

## ResMut — function @public
```
<T>(resource: ResourceDef<T>): ResMutDescriptor<T>
```

## ResMutDescriptor — interface @public
```
@internal _resource: ResourceDef<T>
@internal _type: "res_mut"
```

## ResMutInstance — class @public
```
@internal updateValue: (value: T) => void
get: () => T
modify: (fn: (value: T) => void) => void
set: (value: T) => void
static new <T>(value: T, setter: (v: T) => void): ResMutInstance<T>
static prototype: ResMutInstance<any>
```

## ResolvedAssetGroup — interface @experimental
```
alwaysInclude: boolean
delivery: BundleMode
name: string
```

## ResolvedAtlas — interface @experimental
```
folder: string
name: string
```

## ResolvedTileCollision — interface @experimental
```
density: number | undefined
friction: number | undefined
oneWay: { nx: number; ny: number; } | undefined
restitution: number | undefined
sensor: boolean | undefined
shape: { type: "box"; } | { type: "polygon"; points: [number, number][]; } | { type: "circle"; cx: number; cy: number; r: number; }
```

## ResolvedTileset — interface @experimental
```
asset: TilesetAsset
textureHandle: number
textureHeight: number | undefined
textureWidth: number | undefined
```

## ResourceDef — interface @public
```
@internal _default: T
@internal _id: symbol
@internal _name: string
```

## ResourceStats — interface @experimental
```
cacheHits: number
cacheMisses: number
indexBufferCount: number
shaderCount: number
textureBudget: number
textureBytes: number
textureCount: number
textureEvictableCount: number
vertexBufferCount: number
```

## ReverbEffectDef — interface @experimental
```
decay: number | undefined
seconds: number | undefined
type: "reverb"
wet: number | undefined
```

## RevoluteJoint — const @experimental
```
ComponentDef<RevoluteJointData>
```

## RichTextRun — type @experimental
```
TextSegment | ImageSegment
```

## RigidBody — const @experimental
```
BuiltinComponentDef<RigidBodyData>
```

## RigidBodyData — interface @experimental
```
angularDamping: number
bodyType: number
bullet: boolean
enabled: boolean
fixedRotation: boolean
gravityScale: number
linearDamping: number
```

## RunCondition — type @public
```
() => boolean
```

## RuntimeAssetSource — interface @experimental
```
backend: Backend
decodePixels: (path: string, flip: boolean) => Promise<{ width: number; height: number; pixels: Uint8Array; }>
listAssetPaths: (() => string[]) | undefined
resolveAddress: ((ref: string) => string | null) | undefined
resolveRef: ((ref: string) => string) | undefined
textureImportSettings: ((ref: string) => ParsedTextureImportSettings | undefined) | undefined
```

## RuntimeBuildConfig — interface @experimental
```
assetFailureCooldown: number | undefined
assetLoadTimeout: number | undefined
audioCacheBudget: number | undefined
canvasMatchWidthOrHeight: number | undefined
canvasScaleMode: string | undefined
defaultFontFamily: string | undefined
maxDeltaTime: number | undefined
maxFixedSteps: number | undefined
sceneTransitionColor: string | undefined
sceneTransitionDuration: number | undefined
textCanvasSize: number | undefined
textureCacheBudget: number | undefined
```

## RuntimeConfig — const @experimental
```
{ sceneTransitionDuration: number; sceneTransitionColor: { r: number; g: number; b: number; a: number; }; defaultFontFamily: string; canvasScaleMode: number; canvasMatchWidthOrHeight: number; maxDeltaTime: number; maxFixedSteps: number; textCanvasSize: number; assetLoadTimeout: number; assetFailureCooldown: number; textureCacheBudget: number; audioCacheBudget: number; }
```

## RuntimeInitConfig — interface @experimental
```
achievements: string[] | undefined
app: App
aspectRatio: number | undefined
audioConfig: AudioProjectConfig | undefined
catalog: Catalog | undefined
firstScene: string
manifest: AddressableManifest | ManifestModel | null | undefined
module: ESEngineModule | null
persistUpdateKey: string | undefined
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
physicsModule: PhysicsWasmModule | null | undefined
remoteRoot: string | undefined
scenes: { name: string; data?: SceneData; path?: string; }[]
screenFit: { designWidth: number; designHeight: number; scaleMode: number; matchWidthOrHeight: number; } | undefined
source: RuntimeAssetSource
spineManager: SpineManager | null | undefined
spineModule: SpineWasmModule | null | undefined
steamAppId: number | undefined
uiTheme: "dark" | "light" | undefined
uiThemeOverrides: ThemeOverrides | undefined
```

## RuntimeOnly — const @public
```
ComponentDef<{}>
```

## SCENE_FORMAT_VERSION — const @experimental
```
1
```

## SCREEN_FIT_OFF — const @experimental
```
-1
```

## SIDE_MODULES — const @experimental
```
Record<BuiltinSideModuleId, SideModuleDescriptor>
```

## SPINE_VERSIONS — const @experimental
```
readonly SpineVersion[]
```

## STATE_PATH_SEP — const @experimental
```
"/"
```

## SafeArea — const @experimental
```
ComponentDef<SafeAreaData>
```

## SafeAreaData — interface @experimental
```
applyBottom: boolean
applyLeft: boolean
applyRight: boolean
applyTop: boolean
```

## SafeAreaPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
dependencies: "uiLayout"[]
name: "safeArea"
static new (): SafeAreaPlugin
static prototype: SafeAreaPlugin
```

## SampleDeps — interface @experimental
```
getComponent: (name: string) => AnyComponentDef | undefined
resolveChild: (root: Entity, childPath: string) => Entity | null
world: SampleWorld
```

## SampleOptions — interface @experimental
```
skipChannel: ((childPath: string, component: string, property: string) => boolean) | undefined
```

## SampleWorld — interface @experimental
```
get: (entity: Entity, def: AnyComponentDef) => Record<string, unknown>
has: (entity: Entity, def: AnyComponentDef) => boolean
set: (entity: Entity, def: AnyComponentDef, data: Record<string, unknown>) => void
```

## SaveEnvelope — interface @experimental
```
data: T
savedAt: number
version: number
```

## SaveManager — class @experimental
```
has: (slot: string) => boolean
load: <T>(slot: string) => T | null
remove: (slot: string) => void
save: <T>(slot: string, data: T) => void
savedAt: (slot: string) => number | null
static new (opts: SaveManagerOptions): SaveManager
static prototype: SaveManager
```

## SaveManagerOptions — interface @experimental
```
keyPrefix: string | undefined
migrations: Record<number, SaveMigration> | undefined
now: (() => number) | undefined
storage: SaveStorage | undefined
version: number
```

## SaveMigration — type @experimental
```
(data: unknown) => unknown
```

## SaveStorage — interface @experimental
```
getJSON: <T>(key: string, defaultValue?: T) => T | undefined
has: (key: string) => boolean
remove: (key: string) => void
setJSON: <T>(key: string, value: T) => void
```

## ScaleMode — type @experimental
```
(typeof ScaleMode)[keyof typeof ScaleMode]
```

## SceneAssetRefs — interface @experimental
```
byType: Map<string, Set<string>>
dragonBones: SpineAssetRef[]
rawByType: Map<string, Set<string>>
rawFor: Map<string, string>
spines: SpineAssetRef[]
unresolved: string[]
```

## SceneComponentCodec — interface @experimental
```
exportData: ((entity: number, data: Record<string, unknown>) => void) | undefined
importData: ((entity: number, outOfBand: Record<string, unknown>) => void) | undefined
outOfBandFields: readonly string[] | undefined
```

## SceneComponentData — interface @experimental
```
data: Record<string, unknown>
type: string
```

## SceneConfig — interface @beta
```
cleanup: ((ctx: SceneContext) => void) | undefined
data: SceneData | undefined
name: string
path: string | undefined
setup: ((ctx: SceneContext) => void | Promise<void>) | undefined
systems: { schedule: Schedule; system: SystemDef; }[] | undefined
```

## SceneContext — interface @beta
```
adopt: (entity: Entity) => void
bindPostProcess: (camera: Entity, stack: PostProcessStack) => void
despawn: (entity: Entity) => void
entities: ReadonlySet<number>
name: string
registerDrawCallback: (id: string, fn: DrawCallback) => void
setPersistent: (entity: Entity, persistent: boolean) => void
spawn: () => Entity
trackAssets: (byType: ReadonlyMap<string, ReadonlySet<string>>) => void
unbindPostProcess: (camera: Entity) => void
```

## SceneData — interface @experimental
```
entities: SceneEntityData[]
generator: string | undefined
name: string
textureMetadata: Record<string, TextureMetadata> | undefined
version: string | number
```

## SceneEntityData — interface @experimental
```
children: number[]
components: SceneComponentData[]
id: number
name: string
parent: number | null
visible: boolean | undefined
```

## SceneLoadCancelled — class @experimental
```
static new (name: string): SceneLoadCancelled
static prototype: SceneLoadCancelled
```

## SceneLoadOptions — interface @experimental
```
abortOnMissingAssets: boolean | undefined
assetBaseUrl: string | undefined
assets: Assets | undefined
collectAssets: LoadedSceneAssets | undefined
onMissingAssets: MissingAssetCallback | undefined
onProgress: SceneLoadProgressCallback | undefined
```

## SceneLoadProgressCallback — type @beta
```
(loaded: number, total: number) => void
```

## SceneManager — const @beta
```
ResourceDef<SceneManagerState>
```

## SceneManagerState — class @beta
```
bringToTop: (name: string) => void
getActive: () => string | null
getActiveScenes: () => string[]
getInitial: () => string | null
getLoadOrder: () => string[]
getLoaded: () => string[]
getScene: (name: string) => SceneContext | null
getSceneStatus: (name: string) => SceneStatus | null
isActive: (name: string) => boolean
isLoaded: (name: string) => boolean
isPaused: (name: string) => boolean
isSleeping: (name: string) => boolean
isTransitioning: () => boolean
load: (name: string, onProgress?: SceneLoadProgressCallback) => Promise<SceneContext>
loadAdditive: (name: string, onProgress?: SceneLoadProgressCallback) => Promise<SceneContext>
pause: (name: string) => void
register: (config: SceneConfig) => void
reload: (options?: TransitionOptions) => Promise<void>
reset: () => void
resume: (name: string) => void
setInitial: (name: string) => void
sleep: (name: string) => void
switchTo: (name: string, options?: TransitionOptions) => Promise<void>
unload: (name: string, options?: TransitionOptions) => Promise<void>
updateTransition: (dt: number) => void
wake: (name: string) => void
static new (app: App): SceneManagerState
static prototype: SceneManagerState
```

## SceneMigrationResult — interface @experimental
```
data: SceneData
fromVersion: number
migrated: boolean
toVersion: number
```

## SceneOwner — const @experimental
```
ComponentDef<SceneOwnerData>
```

## SceneOwnerData — interface @experimental
```
persistent: boolean
scene: string
```

## SceneStatus — type @beta
```
'loading' | 'running' | 'paused' | 'sleeping' | 'unloading'
```

## SceneStreamHost — interface @experimental
```
isLoaded: (name: string) => boolean
isSleeping: (name: string) => boolean
loadAdditive: (name: string) => Promise<unknown>
sleep: (name: string) => void
unload: (name: string) => Promise<void>
wake: (name: string) => void
```

## SceneStreaming — const @experimental
```
ResourceDef<SceneStreamingController>
```

## SceneStreamingConfig — interface @experimental
```
loadRadius: number
policy: StreamPolicy | undefined
unloadRadius: number
```

## SceneStreamingController — class @experimental
```
clear: () => void
configure: (config: SceneStreamingConfig) => void
getActive: () => string[]
getFocusEntity: () => Entity | null
register: (cell: StreamCell) => void
setFocus: (x: number, y: number) => void
setFocusEntity: (entity: Entity | null) => void
unregister: (scene: string) => void
update: () => void
static new (host: SceneStreamHost, config?: SceneStreamingConfig): SceneStreamingController
static prototype: SceneStreamingController
```

## Schedule — enum @public
```
First = 1
FixedPostUpdate = 12
FixedPreUpdate = 10
FixedUpdate = 11
Last = 5
PostUpdate = 4
PreUpdate = 2
Startup = 0
Update = 3
```

## ScreenInfo — class @experimental
```
dpr: number
height: number
on: <K extends keyof ScreenInfoEvents>(event: K, handler: (...args: ScreenInfoEvents[K]) => void) => () => void
orientation: ScreenOrientation
update: (width: number, height: number, dpr?: number) => void
width: number
static new (): ScreenInfo
static prototype: ScreenInfo
```

## ScreenInfoEvents — interface @experimental
```
orientationchange: [orientation: ScreenOrientation]
resize: [width: number, height: number]
```

## ScreenOrientation — enum @experimental
```
Landscape = "landscape"
Portrait = "portrait"
```

## ScreenRect — interface @experimental
```
h: number
w: number
x: number
y: number
```

## ScreenScaling — const @experimental
```
ResourceDef<ScreenScalingData>
```

## ScreenScalingData — interface @experimental
```
designHeight: number
designWidth: number
matchWidthOrHeight: number
scaleMode: number
```

## ScrollContainer — class @experimental
```
dispose: () => void
getContentSize: () => Vec2
getDecelerationRate: () => number
getDragScroll: () => boolean
getMaxOffset: () => Vec2
getOffset: () => Vec2
getShowScrollbar: () => boolean
getViewportSize: () => Vec2
getWheelSpeed: () => number
onScroll: (listener: ScrollListener) => () => void
scrollBy: (delta: Vec2) => void
setContentSize: (size: Vec2) => void
setOffset: (offset: Vec2) => void
setViewportSize: (size: Vec2) => void
static new (opts: ScrollContainerOptions): ScrollContainer
static prototype: ScrollContainer
```

## ScrollViewHandle — interface @experimental
```
content: number
dispose: () => void
entity: number
scroll: ScrollContainer
scrollTo: (offset: Vec2) => void
setContentSize: (size: Vec2) => void
```

## ScrollViewOptions — interface @experimental
```
background: UIVisualInit | undefined
contentSize: Vec2
decelerationRate: number | undefined
direction: "vertical" | "horizontal" | "both" | undefined
dragScroll: boolean | undefined
host: ListViewHost
node: UINodeInit | undefined
onScroll: ((offset: Vec2) => void) | undefined
parent: number | undefined
viewportSize: Vec2
wheelSpeed: number | undefined
world: World
```

## SegmentCollider — const @experimental
```
BuiltinComponentDef<SegmentColliderData>
```

## SenseResult — interface @experimental
```
dirX: number
dirY: number
distance: number
visible: boolean
```

## SensorEvent — interface @experimental
```
sensorEntity: number
visitorEntity: number
```

## ServicesPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): ServicesPlugin
static prototype: ServicesPlugin
```

## ShaderHandle — type @experimental
```
number
```

## ShaderParam — interface @experimental
```
default: number[]
defaultTexture: string | undefined
displayName: string
name: string
range: { min: number; max: number; } | undefined
type: ShaderParamType
ui: string | undefined
```

## ShaderParamType — type @experimental
```
'float' | 'vec2' | 'vec3' | 'vec4' | 'color' | 'int' | 'texture'
```

## ShaderReflection — interface @experimental
```
domain: string
params: ShaderParam[]
```

## ShaderSources — const @experimental
```
{ SPRITE_VERTEX: string; SPRITE_FRAGMENT: string; COLOR_VERTEX: string; COLOR_FRAGMENT: string; }
```

## ShadowCaster2D — const @experimental
```
BuiltinComponentDef<ShadowCaster2DData>
```

## ShadowCaster2DData — interface @experimental
```
enabled: boolean
size: Vec2
```

## ShapeRenderer — const @experimental
```
BuiltinComponentDef<ShapeRendererData>
```

## ShapeRendererData — interface @experimental
```
color: Color
cornerRadius: number
enabled: boolean
layer: number
parallax: Vec2
shapeType: number
size: Vec2
```

## ShapeType — enum @experimental
```
Capsule = 1
Circle = 0
RoundedRect = 2
```

## Share — const @experimental
```
ResourceDef<ShareAPI>
```

## ShareAPI — class @experimental
```
available: boolean
setShareCard: (card: ShareCard | (() => ShareCard)) => void
share: (card?: ShareCard) => boolean
static new (): ShareAPI
static prototype: ShareAPI
```

## ShareCard — type @experimental
```
imageUrl: string | undefined
query: string | undefined
title: string | undefined
```

## SideModuleDescriptor — interface @experimental
```
file: string
globalName: string | undefined
```

## SideModuleHost — interface @experimental
```
acquire: (id: SideModuleId) => Promise<SideModule | null>
```

## SideModuleId — type @experimental
```
BuiltinSideModuleId | (string & {})
```

## Signal — interface @experimental
```
get: () => T
set: (value: T) => void
subscribe: (fn: (value: T) => void) => () => void
update: (fn: (prev: T) => T) => void
```

## SimulationSpace — enum @experimental
```
Local = 1
World = 0
```

## SkeletalFieldMeta — interface @public
```
atlasField: string
runtime: string
skeletonField: string
```

## SliceBorder — interface @experimental
```
bottom: number
left: number
right: number
top: number
```

## SliderHandle — interface @experimental
```
dispose: () => void
entity: number
fillEntity: number
getValue: () => number
handleEntity: number
setValue: (value: number) => void
trackEntity: number
```

## SliderOptions — interface @experimental
```
disabled: boolean | undefined
events: UIEventQueue
fillVisual: UIVisualInit | undefined
focusable: boolean | undefined
handleVisual: UIVisualInit | undefined
handleWidth: number | undefined
max: number | undefined
min: number | undefined
node: UINodeInit | undefined
onChange: ((value: number, entity: Entity) => void) | undefined
parent: number | undefined
step: number | undefined
tabIndex: number | undefined
trackVisual: UIVisualInit | undefined
value: number | undefined
world: World
```

## SocketReadyState — type @experimental
```
PlatformSocketReadyState
```

## SourceDelta — interface @experimental
```
added: readonly AddedEntity[]
overrides: readonly PrefabOverride[]
removed: readonly string[]
```

## SpatialAudioConfig — interface @experimental
```
maxDistance: number
model: AttenuationModel
refDistance: number
rolloff: number
```

## SpawnOverride — type @beta
```
Omit<PrefabOverride, 'prefabEntityId'> & { prefabEntityId?: string }
```

## Spine — const @beta
```
ResourceDef<SpineManager>
```

## SpineAnimation — const @beta
```
BuiltinComponentDef<SpineAnimationData>
```

## SpineAnimationData — interface @experimental
```
animation: string
atlasPath: string
color: Color
enabled: boolean
flipX: boolean
flipY: boolean
layer: number
loop: boolean
material: number
playing: boolean
skeletonPath: string
skeletonScale: number
skin: string
timeScale: number
```

## SpineAnimationDriver — interface @experimental
```
setAnimation: (entity: Entity, animation: string, loop: boolean) => void
```

## SpineEvents — const @experimental
```
ResourceDef<SpineEventsData>
```

## SpineLoadResult — interface @experimental
```
error: string | undefined
success: boolean
```

## SpinePlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
finish: (app: App) => void
name: string
setSpineManager: (manager: SpineManager) => void
spineManager: SpineManager | null
static new (manager?: SpineManager): SpinePlugin
static prototype: SpinePlugin
```

## SpineTrack — interface @experimental
```
blendIn: number
childPath: string
clips: SpineClip[]
name: string
type: "spine"
```

## Sprite — const @public
```
BuiltinComponentDef<SpriteData>
```

## SpriteAnimClip — interface @experimental
```
events: SpriteAnimEvent[] | undefined
fps: number
frames: SpriteAnimFrame[]
labels: Record<string, number> | undefined
loop: boolean
name: string
```

## SpriteAnimEvent — interface @experimental
```
data: unknown
frame: number
name: string
```

## SpriteAnimEventHandler — type @experimental
```
(event: SpriteAnimEvent, entity: Entity) => void
```

## SpriteAnimFrame — interface @experimental
```
duration: number | undefined
pivot: { x: number; y: number; } | undefined
texture: number
uvOffset: { x: number; y: number; } | undefined
uvScale: { x: number; y: number; } | undefined
```

## SpriteAnimTrack — interface @experimental
```
childPath: string
clip: string
name: string
startTime: number
type: "spriteAnim"
```

## SpriteAnimation — const @experimental
```
ResourceDef<SpriteAnimationAPI>
```

## SpriteAnimationAPI — class @experimental
```
aliasClip: (name: string, clip: SpriteAnimClip) => void
clearClips: () => void
getClip: (name: string) => SpriteAnimClip | undefined
gotoFrame: (animator: SpriteAnimatorData, frameIndex: number, andPlay?: boolean) => void
gotoLabel: (animator: SpriteAnimatorData, label: string, andPlay?: boolean) => void
onEvent: (entity: Entity, handler: SpriteAnimEventHandler) => () => void
onEventGlobal: (handler: SpriteAnimEventHandler) => () => void
registerClip: (clip: SpriteAnimClip) => void
removeEntityListeners: (entity: Entity) => void
unregisterClip: (name: string) => void
update: (world: World, deltaTime: number) => void
static new (): SpriteAnimationAPI
static prototype: SpriteAnimationAPI
```

## SpriteAnimator — const @experimental
```
ComponentDef<SpriteAnimatorData>
```

## SpriteAnimatorData — interface @experimental
```
clip: string
currentFrame: number
enabled: boolean
finished: boolean
frameTimer: number
loop: boolean
playing: boolean
speed: number
```

## SpriteData — interface @public
```
color: Color
enabled: boolean
flipX: boolean
flipY: boolean
layer: number
lit: boolean
material: number
parallax: Vec2
pivot: Vec2
size: Vec2
texture: number
tileSize: Vec2
tileSpacing: Vec2
uvOffset: Vec2
uvScale: Vec2
```

## SpriteFilter — const @experimental
```
{ createOutline(options?: OutlineFilterOptions): MaterialHandle; createGlow(options?: OutlineFilterOptions): MaterialHandle; createDropShadow(options?: DropShadowFilterOptions): MaterialHandle; setOutlineColor(material: MaterialHandle, color: { r: number; g: number; b: number; a: number; }): void; setOutlineWidth(material: MaterialHandle, width: number): void; setShadowOffset(material: MaterialHandle, x: number, y: number): void; setShadowBlur(material: MaterialHandle, blur: number): void; }
```

## StaleOverride — interface @experimental
```
nestedAt: string | undefined
override: PrefabOverride
reason: string
site: "variant" | "nested" | "instance"
```

## StateMachineAgent — const @experimental
```
ComponentDef<StateMachineAgentData>
```

## StateMachineAgentData — interface @experimental
```
current: string
fsm: string
```

## StateMachines — class @experimental
```
blackboard: (entity: Entity) => Blackboard
fire: (entity: Entity, trigger: string) => void
state: (entity: Entity) => string | null
static new (states: Map<Entity, AgentState>): StateMachines
static prototype: StateMachines
```

## Stats — const @experimental
```
ResourceDef<FrameStats>
```

## StatsCollector — class @experimental
```
getFps: () => number
getFrameTimeMs: () => number
pushFrame: (deltaSeconds: number) => void
reset: () => void
static new (): StatsCollector
static prototype: StatsCollector
```

## StatsOverlay — class @experimental
```
dispose: () => void
hide: () => void
show: () => void
update: (stats: FrameStats) => void
static new (container: HTMLElement, position?: StatsPosition): StatsOverlay
static prototype: StatsOverlay
```

## StatsPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: "stats"
static new (options?: StatsPluginOptions): StatsPlugin
static prototype: StatsPlugin
```

## StatsPluginOptions — interface @experimental
```
container: HTMLElement | undefined
overlay: boolean | undefined
position: StatsPosition | undefined
```

## StatsPosition — type @experimental
```
'top-left' | 'top-right' | 'bottom-left' | 'bottom-right'
```

## Status — enum @experimental
```
Failure = "failure"
Running = "running"
Success = "success"
```

## Stick — const @public
```
(stick: "left" | "right", pad?: number) => Binding
```

## Storage — const @experimental
```
{ getString(key: string, defaultValue?: string): string | undefined; setString(key: string, value: string): void; getNumber(key: string, defaultValue?: number): number | undefined; setNumber(key: string, value: number): void; getBoolean(key: string, defaultValue?: boolean): boolean | undefined; setBoolean(key: string, value: boolean): void; getJSON<T>(key: string, defaultValue?: T): T | undefined; setJSON<T>(key: string, value: T): void; remove(key: string): void; has(key: string): boolean; clear(): void; }
```

## StreamCell — interface @experimental
```
radius: number
scene: string
x: number
y: number
```

## StreamDecision — interface @experimental
```
toActivate: string[]
toDeactivate: string[]
```

## StreamPolicy — type @experimental
```
'unload' | 'sleep'
```

## SubEmitterTrigger — enum @experimental
```
Birth = 1
Death = 0
```

## SubmitSkipFlags — const @experimental
```
{ readonly None: 0; readonly Spine: 1; readonly Particles: 2; }
```

## SubsystemActivity — type @experimental
```
'stepping' | 'idle' | 'inactive'
```

## SubsystemEvent — interface @experimental
```
atMs: number
detail: string | undefined
id: string
phase: SubsystemPhase
seq: number
```

## SubsystemPhase — type @experimental
```
'registered' | 'initializing' | 'ready' | 'error'
```

## SubsystemRegistry — class @experimental
```
getStatuses: () => SubsystemStatus[]
markError: (id: string, reason?: string) => void
markStepped: (id: string) => void
phaseOf: (id: string) => SubsystemPhase | undefined
recentEvents: (limit?: number) => SubsystemEvent[]
register: (id: string, opts?: { displayName?: string; dependsOn?: string[]; }) => void
subscribe: (fn: () => void) => () => void
transition: (id: string, phase: SubsystemPhase, detail?: string) => void
static new (): SubsystemRegistry
static prototype: SubsystemRegistry
```

## SubsystemStatus — interface @experimental
```
activity: SubsystemActivity
dependsOn: readonly string[]
detail: string | undefined
displayName: string
id: string
lastError: string | undefined
phase: SubsystemPhase
phaseAgeMs: number
```

## SwipeDirection — type @experimental
```
'left' | 'right' | 'up' | 'down'
```

## SyncPrefabResolver — type @experimental
```
(path: string) => PrefabData | null
```

## SystemDef — interface @public
```
@internal _fn: (...args: never[]) => void | Promise<void>
@internal _id: symbol
@internal _name: string
@internal _params: readonly SystemParam[]
@internal _runAfter: readonly string[] | undefined
@internal _runBefore: readonly string[] | undefined
```

## SystemOptions — interface @public
```
name: string | undefined
runAfter: string[] | undefined
runBefore: string[] | undefined
```

## SystemParam — type @public
```
| QueryDescriptor<readonly QueryArg[]>
    | ResDescriptor<unknown>
    | ResMutDescriptor<unknown>
    | CommandsDescriptor
    | EventWriterDescriptor<unknown>
    | EventReaderDescriptor<unknown>
    | RemovedQueryDescriptor<AnyComponentDef>
    | GetWorldDescriptor
```

## SystemRunner — class @experimental
```
clearTimings: () => void
evict: (systemId: symbol) => void
getTimings: () => ReadonlyMap<string, number> | null
reset: () => void
run: (system: SystemDef) => void | Promise<void>
setTimingEnabled: (enabled: boolean) => void
static new (world: World, resources: ResourceStorage, eventRegistry?: EventRegistry): SystemRunner
static prototype: SystemRunner
```

## SystemSet — interface @public
```
@internal _kind: "set"
@internal _name: string
@internal _runAfter: readonly string[] | undefined
@internal _runBefore: readonly string[] | undefined
@internal _runIf: RunCondition | undefined
@internal _systems: readonly SystemDef[]
```

## SystemSetOptions — interface @public
```
runAfter: string[] | undefined
runBefore: string[] | undefined
runIf: RunCondition | undefined
systems: SystemDef[]
```

## TB_E — const @experimental
```
2
```

## TB_N — const @experimental
```
1
```

## TB_NE — const @experimental
```
16
```

## TB_NW — const @experimental
```
128
```

## TB_S — const @experimental
```
4
```

## TB_SE — const @experimental
```
32
```

## TB_SW — const @experimental
```
64
```

## TB_W — const @experimental
```
8
```

## TERRAIN_NEIGHBORS — const @experimental
```
readonly { dx: number; dy: number; bit: number; }[]
```

## THEME_COLOR_ROLES — const @experimental
```
readonly (keyof ThemeColors)[]
```

## TILESET_FORMAT_VERSION — const @experimental
```
"1"
```

## TILE_FLAGS_MASK — const @experimental
```
number
```

## TILE_FLIP_D — const @experimental
```
32768
```

## TILE_FLIP_H — const @experimental
```
8192
```

## TILE_FLIP_V — const @experimental
```
16384
```

## TILE_ID_MASK — const @experimental
```
8191
```

## TParams — type @experimental
```
Record<string, string | number>
```

## Takeover — interface @experimental
```
active: boolean
around: <T>(show: () => Promise<T>) => Promise<T>
begin: () => void
end: () => void
```

## TakeoverHost — interface @experimental
```
isPaused: () => boolean
resumeAudio: () => void
setPaused: (paused: boolean) => void
suspendAudio: () => void
```

## TerrainColor — interface @experimental
```
color: string
name: string
```

## TerrainIndex — interface @experimental
```
byMask: Map<number, number>
mode: TerrainMode
```

## TerrainIndices — interface @experimental
```
sets: Map<number, TerrainIndex>
tileTerrain: Map<number, number>
```

## TerrainMode — type @experimental
```
'edge' | 'corner' | 'wang'
```

## Text — const @public
```
ComponentDef<TextData>
```

## TextAlign — type @public
```
(typeof TextAlign)[keyof typeof TextAlign]
```

## TextData — interface @public
```
align: TextAlign
bold: boolean
color: Color
content: string
enabled: boolean
font: number
fontFamily: string
fontSize: number
i18nKey: string
italic: boolean
layer: number
lineHeight: number
overflow: TextOverflow
renderMode: TextRenderMode
richText: boolean
shadowBlur: number
shadowColor: Color
shadowOffsetX: number
shadowOffsetY: number
strokeColor: Color
strokeWidth: number
verticalAlign: TextVerticalAlign
wordWrap: boolean
```

## TextInit — interface @experimental
```
align: TextAlign | undefined
bold: boolean | undefined
color: Color | undefined
content: string | undefined
enabled: boolean | undefined
font: number | undefined
fontFamily: string | undefined
fontSize: number | undefined
i18nKey: string | undefined
italic: boolean | undefined
layer: number | undefined
lineHeight: number | undefined
overflow: TextOverflow | undefined
renderMode: TextRenderMode | undefined
richText: boolean | undefined
shadowBlur: number | undefined
shadowColor: Color | undefined
shadowOffsetX: number | undefined
shadowOffsetY: number | undefined
strokeColor: Color | undefined
strokeWidth: number | undefined
verticalAlign: TextVerticalAlign | undefined
wordWrap: boolean | undefined
```

## TextInput — const @experimental
```
ComponentDef<TextInputData>
```

## TextInputData — interface @experimental
```
backgroundColor: Color
color: Color
cursorPos: number
dirty: boolean
focused: boolean
font: number
fontFamily: string
fontSize: number
maxLength: number
multiline: boolean
padding: number
password: boolean
placeholder: string
placeholderColor: Color
readOnly: boolean
renderMode: TextRenderMode
textAlign: TextAlign
value: string
```

## TextInputHandle — interface @experimental
```
dispose: () => void
entity: number
getValue: () => string
setValue: (value: string) => void
```

## TextInputOptions — interface @experimental
```
backgroundColor: Color | undefined
color: Color | undefined
disabled: boolean | undefined
events: UIEventQueue
fontFamily: string | undefined
fontSize: number | undefined
maxLength: number | undefined
multiline: boolean | undefined
node: UINodeInit | undefined
onChange: ((value: string, entity: Entity) => void) | undefined
onSubmit: ((value: string, entity: Entity) => void) | undefined
padding: number | undefined
parent: number | undefined
password: boolean | undefined
placeholder: string | undefined
placeholderColor: Color | undefined
readOnly: boolean | undefined
renderMode: TextRenderMode | undefined
tabIndex: number | undefined
value: string | undefined
world: World
```

## TextInputPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
dependencies: "focus"[]
name: "textInput"
static new (): TextInputPlugin
static prototype: TextInputPlugin
```

## TextMetrics — interface @experimental
```
height: number
lineCount: number
width: number
```

## TextOverflow — type @experimental
```
(typeof TextOverflow)[keyof typeof TextOverflow]
```

## TextPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): TextPlugin
static prototype: TextPlugin
```

## TextRenderMode — type @public
```
(typeof TextRenderMode)[keyof typeof TextRenderMode]
```

## TextSegment — interface @experimental
```
bold: boolean
color: Color | null
fontSize: number | null
italic: boolean
text: string
type: "text"
```

## TextVerticalAlign — type @public
```
(typeof TextVerticalAlign)[keyof typeof TextVerticalAlign]
```

## TextureDimensions — interface @experimental
```
height: number
width: number
```

## TextureFilter — type @experimental
```
(typeof TextureFilter)[keyof typeof TextureFilter]
```

## TextureHandle — type @experimental
```
number
```

## TextureInfo — interface @experimental
```
handle: number
height: number
width: number
```

## TextureRef — interface @experimental
```
slot: number | undefined
textureId: number
```

## TextureWrap — type @experimental
```
(typeof TextureWrap)[keyof typeof TextureWrap]
```

## ThemeColors — interface @beta
```
backdrop: Color
control: Color
controlActive: Color
controlFocus: Color
controlHover: Color
onPrimary: Color
primary: Color
primaryActive: Color
primaryHover: Color
surface: Color
surfaceElevated: Color
text: Color
track: Color
```

## ThemeOverrides — interface @experimental
```
colors: Partial<ThemeColors> | undefined
type: Partial<ThemeType> | undefined
```

## ThemeStyle — const @experimental
```
ComponentDef<ThemeStyleData>
```

## ThemeStyleData — interface @experimental
```
input: { background?: ColorRole; text?: ColorRole; placeholder?: ColorRole; } | undefined
states: Record<string, keyof ThemeColors> | undefined
text: keyof ThemeColors | undefined
visual: keyof ThemeColors | undefined
```

## ThemeTokens — interface @experimental
```
colors: ThemeColors
type: ThemeType
```

## ThemeType — interface @experimental
```
body: number
label: number
title: number
```

## TileCollisionPiece — interface @experimental
```
center: Vec2
circles: { c: Vec2; r: number; }[]
oneWay: { nx: number; ny: number; } | null
polylines: Vec2[][]
sensor: boolean
```

## TileCollisionShape — type @experimental
```
| { type: 'box' }
    | { type: 'polygon'; points: [number, number][] }
    | { type: 'circle'; cx: number; cy: number; r: number }
```

## TileFlags — interface @experimental
```
flipD: boolean
flipH: boolean
flipV: boolean
```

## TileGridParams — interface @experimental
```
hexSideLength: number | undefined
orientation: number
staggerAxisX: boolean | undefined
staggerIndexEven: boolean | undefined
tileHeight: number
tileWidth: number
```

## TileOrientation — enum @experimental
```
Hexagonal = 3
Isometric = 1
Orthogonal = 0
Staggered = 2
```

## TileStamp — interface @experimental
```
cells: number[]
h: number
w: number
```

## TiledLayerData — interface @experimental
```
chunks: TiledChunkData[]
height: number
infinite: boolean
name: string
opacity: number
parallaxX: number
parallaxY: number
tiles: Uint16Array<ArrayBufferLike>
tintColor: { r: number; g: number; b: number; a: number; }
visible: boolean
width: number
```

## TiledMapData — interface @experimental
```
collisionTileIds: number[]
height: number
hexSideLength: number
layers: TiledLayerData[]
objectGroups: TiledObjectGroupData[]
orientation: string
staggerAxis: string
staggerIndex: string
tileAnimations: Map<number, TiledAnimFrame[]>
tileHeight: number
tileProperties: Map<number, Map<string, string>>
tileShapes: Map<number, ResolvedTileCollision>
tileWidth: number
tilesets: TiledTilesetData[]
width: number
```

## TiledTilesetData — interface @experimental
```
collectionTiles: TiledCollectionTile[] | undefined
columns: number
firstGid: number
image: string
margin: number | undefined
name: string
spacing: number | undefined
tileCount: number
tileHeight: number
tileWidth: number
```

## Tilemap — const @experimental
```
ComponentDef<TilemapData>
```

## TilemapAPI — const @experimental
```
{ initLayer(entity: number, width: number, height: number, tileWidth: number, tileHeight: number): void; destroyLayer(entity: number): void; setTile(entity: number, x: number, y: number, tileId: number): void; getTile(entity: number, x: number, y: number): number; fillRect(entity: number, x: number, y: number, w: number, h: number, tileId: number): void; setTiles(entity: number, tiles: Uint16Array): void; setTilesets(entity: number, slots: { firstId: number; textureHandle: number; columns: number; margin?: number; spacing?: number; }[]): void; hasLayer(entity: number): boolean; setRenderProps(entity: number, textureHandle: number, tilesetColumns: number, uvTileW: number, uvTileH: number, sortLayer: number, depth: number, parallaxX: number, parallaxY: number): void; setTint(entity: number, r: number, g: number, b: number, a: number, opacity: number): void; setVisible(entity: number, visible: boolean): void; setOriginEntity(layerKey: number, originEntity: number): void; setTileAnimation(entity: number, tileId: number, frames: { tileId: number; duration: number; }[]): void; clearTileAnimations(entity: number): void; advanceAnimations(entity: number, dtMs: number): void; setTileProperty(entity: number, tileId: number, key: string, value: string): void; getTileProperty(entity: number, x: number, y: number, key: string): string; flipTile(entity: number, x: number, y: number, flipH: boolean, flipV: boolean, flipD: boolean): void; rotateTile(entity: number, x: number, y: number, degrees: number): void; initInfiniteLayer(entity: number, tileWidth: number, tileHeight: number): void; setChunkTiles(entity: number, chunkX: number, chunkY: number, tiles: Uint16Array, width: number, height: number): void; setGridType(entity: number, type: number): void; setHexParams(entity: number, sideLength: number, staggerAxisX: boolean, staggerIndexEven: boolean): void; tileToWorld(entity: number, tx: number, ty: number, originX: number, originY: number): { x: number; y: number; }; worldToTile(entity: number, wx: number, wy: number, originX: number, originY: number): { x: number; y: number; }; exportChunks(entity: number): string; importChunks(entity: number, encoded: string): boolean; }
```

## TilemapData — interface @experimental
```
source: string
```

## TilemapLayer — const @experimental
```
BuiltinComponentDef<TilemapLayerData>
```

## TilemapLayerData — interface @experimental
```
cellSize: Vec2
hexSideLength: number
opacity: number
orientation: number
originOffset: Vec2
parallaxFactor: Vec2
renderLayer: number
staggerAxis: number
staggerIndex: number
tileset: number
tilesetColumns: number
tilesetRows: number
tintColor: Color
visible: boolean
```

## TilemapLiveSync — const @experimental
```
{ _bind(fn: ApplyTilesetRefs | null): void; setLayerTilesets(entity: number, refs: readonly string[]): void; }
```

## TilemapPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
resetLayers: () => void
static new (): TilemapPlugin
static prototype: TilemapPlugin
```

## Tilemaps — const @experimental
```
ResourceDef<{ initLayer(entity: number, width: number, height: number, tileWidth: number, tileHeight: number): void; destroyLayer(entity: number): void; setTile(entity: number, x: number, y: number, tileId: number): void; getTile(entity: number, x: number, y: number): number; fillRect(entity: number, x: number, y: number, w: number, h: number, tileId: number): void; setTiles(entity: number, tiles: Uint16Array): void; setTilesets(entity: number, slots: { firstId: number; textureHandle: number; columns: number; margin?: number; spacing?: number; }[]): void; hasLayer(entity: number): boolean; setRenderProps(entity: number, textureHandle: number, tilesetColumns: number, uvTileW: number, uvTileH: number, sortLayer: number, depth: number, parallaxX: number, parallaxY: number): void; setTint(entity: number, r: number, g: number, b: number, a: number, opacity: number): void; setVisible(entity: number, visible: boolean): void; setOriginEntity(layerKey: number, originEntity: number): void; setTileAnimation(entity: number, tileId: number, frames: { tileId: number; duration: number; }[]): void; clearTileAnimations(entity: number): void; advanceAnimations(entity: number, dtMs: number): void; setTileProperty(entity: number, tileId: number, key: string, value: string): void; getTileProperty(entity: number, x: number, y: number, key: string): string; flipTile(entity: number, x: number, y: number, flipH: boolean, flipV: boolean, flipD: boolean): void; rotateTile(entity: number, x: number, y: number, degrees: number): void; initInfiniteLayer(entity: number, tileWidth: number, tileHeight: number): void; setChunkTiles(entity: number, chunkX: number, chunkY: number, tiles: Uint16Array, width: number, height: number): void; setGridType(entity: number, type: number): void; setHexParams(entity: number, sideLength: number, staggerAxisX: boolean, staggerIndexEven: boolean): void; tileToWorld(entity: number, tx: number, ty: number, originX: number, originY: number): { x: number; y: number; }; worldToTile(entity: number, wx: number, wy: number, originX: number, originY: number): { x: number; y: number; }; exportChunks(entity: number): string; importChunks(entity: number, encoded: string): boolean; }>
```

## TilesetAnimFrame — interface @experimental
```
durationMs: number
tile: number
```

## TilesetAsset — interface @experimental
```
columns: number
margin: number
spacing: number
terrains: TilesetTerrain[] | undefined
texture: string
tileCount: number | undefined
tileHeight: number
tileWidth: number
tiles: Record<number, TilesetTile>
version: string
```

## TilesetCollision — type @experimental
```
TileCollisionShape & {
    /** One-way (jump-through) platform: contacts from behind this normal pass through. */
    oneWay?: { nx: number; ny: number };
    /** Non-solid trigger volume (fires events, no physical response). */
    sensor?: boolean;
    /** Physics material overrides (absent = engine defaults). */
    density?: number;
    friction?: number;
    restitution?: number;
}
```

## TilesetModel — interface @experimental
```
animations: Map<number, { tileId: number; duration: number; }[]>
collidableTileIds: number[]
slots: TilesetModelSlot[]
tileShapes: Map<number, ResolvedTileCollision>
```

## TilesetTerrain — interface @experimental
```
color: string | undefined
colors: TerrainColor[] | undefined
mode: TerrainMode
name: string
```

## TilesetTile — interface @experimental
```
animation: TilesetAnimFrame[] | undefined
collision: TilesetCollision | undefined
probability: number | undefined
properties: Record<string, string> | undefined
terrain: TilesetTileTerrain | undefined
```

## TilesetTileTerrain — interface @experimental
```
corners: number[] | undefined
mask: number | undefined
set: number
```

## Time — const @public
```
ResourceDef<TimeData>
```

## TimeData — interface @public
```
delta: number
elapsed: number
fixedAlpha: number
fixedDelta: number
fixedTick: number
frameCount: number
scale: number
unscaledDelta: number
```

## Timeline — const @experimental
```
ResourceDef<TimelineAPI>
```

## TimelineAPI — class @experimental
```
@internal clearStates: () => void
@internal ensureState: (entity: Entity, wrapMode: WrapMode, speed: number) => TimelineState
@internal removeState: (entity: Entity) => void
getCurrentTime: (entity: Entity) => number
getState: (entity: Entity) => TimelineState | undefined
isPlaying: (entity: Entity) => boolean
pause: (entity: Entity) => void
play: (entity: Entity) => void
setTime: (entity: Entity, time: number) => void
stop: (entity: Entity) => void
static new (flags_?: PlayerFlagChannel | null): TimelineAPI
static prototype: TimelineAPI
```

## TimelineAsset — interface @experimental
```
duration: number
tracks: Track[]
type: "timeline"
version: string
wrapMode: WrapMode
```

## TimelinePlayer — const @experimental
```
ComponentDef<TimelinePlayerData>
```

## TimelinePlayerData — interface @experimental
```
finished: boolean
playing: boolean
speed: number
timeline: string
wrapMode: string
```

## TimelinePlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
clearHandles: () => void
getAsset: (path: string) => TimelineAsset | undefined
getTextureHandle: (timelinePath: string, textureUuid: string) => number
name: string
registerAsset: (path: string, asset: TimelineAsset) => void
registerTextureHandles: (path: string, handles: Map<string, number>) => void
static new (): TimelinePlugin
static prototype: TimelinePlugin
```

## TimerHandle — class @experimental
```
cancel: () => void
elapsed: number
id: number
isActive: boolean
pause: () => TimerHandle
repeatCount: number
reset: () => TimerHandle
resume: () => TimerHandle
static new (manager: TimerManager, id: number): TimerHandle
static prototype: TimerHandle
```

## TimerManager — class @experimental
```
activeCount: number
cancel: (id: number) => void
cancelAll: () => void
delay: (seconds: number, callback: (timer: TimerHandle) => void) => TimerHandle
getElapsed: (id: number) => number
getRepeatCount: (id: number) => number
has: (id: number) => boolean
interval: (seconds: number, callback: (timer: TimerHandle) => void, maxRepeat?: number) => TimerHandle
pause: (id: number) => void
reset: (id: number) => void
resume: (id: number) => void
tick: (dt: number) => void
timeScale: number
static new (): TimerManager
static prototype: TimerManager
```

## TimerRes — const @experimental
```
ResourceDef<TimerManager>
```

## ToggleHandle — interface @experimental
```
dispose: () => void
entity: number
getValue: () => boolean
setDisabled: (disabled: boolean) => void
setValue: (value: boolean) => void
```

## ToggleOptions — interface @experimental
```
background: UIVisualInit | undefined
check: { node?: UINodeInit; color?: Color; sprite?: number; } | undefined
disabled: boolean | undefined
events: UIEventQueue
focusable: boolean | undefined
interactionStates: Record<string, ButtonStateVisual> | undefined
isOn: boolean | undefined
node: UINodeInit | undefined
onChange: ((isOn: boolean, entity: Entity) => void) | undefined
parent: number | undefined
tabIndex: number | undefined
world: World
```

## TouchPoint — interface @beta
```
id: number
x: number
y: number
```

## Track — type @experimental
```
PropertyTrack | SpineTrack | SpriteAnimTrack | AudioTrack | ActivationTrack | MarkerTrack | CustomEventTrack | AnimFramesTrack
```

## TrackType — type @experimental
```
(typeof TrackType)[keyof typeof TrackType]
```

## Trail — const @experimental
```
ResourceDef<TrailAPI>
```

## TrailAPI — class @experimental
```
clear: (entity: Entity) => void
update: (dt: number) => void
static new (module: NonNullable<EngineApi>, registry: CppRegistry): TrailAPI
static prototype: TrailAPI
```

## TrailPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
static new (): TrailPlugin
static prototype: TrailPlugin
```

## TrailRenderer — const @experimental
```
BuiltinComponentDef<TrailRendererData>
```

## TrailRendererData — interface @experimental
```
blendMode: number
emitting: boolean
enabled: boolean
endColor: Color
endWidth: number
layer: number
material: number
minVertexDistance: number
startColor: Color
startWidth: number
texture: number
time: number
```

## Transaction — class @experimental
```
add: (op: TransactionOp) => void
addDeferred: (op: TransactionOp) => void
id: string
label: string
opCount: number
redo: () => void
timestamp: number
undo: () => void
static new (label: string, id?: string): Transaction
static prototype: Transaction
```

## TransactionManager — class @experimental
```
begin: (label: string) => Transaction
canRedo: () => boolean
canUndo: () => boolean
clear: () => void
commit: (tx: Transaction) => void
peekRedo: () => Transaction | null
peekUndo: () => Transaction | null
redo: () => Transaction | null
rollback: (tx: Transaction) => void
undo: () => Transaction | null
static new (options?: TransactionManagerOptions): TransactionManager
static prototype: TransactionManager
```

## TransactionManagerOptions — interface @experimental
```
historyLimit: number | undefined
```

## TransactionOp — interface @experimental
```
forward: () => void
reverse: () => void
```

## Transform — const @public
```
BuiltinComponentDef<TransformData>
```

## TransformData — interface @public
```
position: Vec3
rotation: Quat
scale: Vec3
worldPosition: Vec3
worldRotation: Quat
worldScale: Vec3
```

## TransitionConfig — interface @beta
```
color: Color | undefined
duration: number
type: "fade"
```

## TransitionOptions — interface @beta
```
color: Color | undefined
duration: number | undefined
keepPersistent: boolean | undefined
onComplete: (() => void) | undefined
onStart: (() => void) | undefined
transition: "fade" | "none" | undefined
```

## Tween — const @experimental
```
ResourceDef<TweenAPI>
```

## TweenAPI — class @experimental
```
@internal activeCompositionCount: number
@internal clear: () => void
cancel: (tweenHandle: TweenHandle) => void
cancelAll: (entity: Entity) => void
delay: (seconds: number) => ValueTweenHandle
parallel: (tweens: Completable[]) => TweenGroup
sequence: (factories: TweenFactory[]) => TweenSequence
to: (entity: Entity, target: TweenTarget, from: number, to: number, duration: number, options?: TweenOptions) => TweenHandle
update: (deltaTime: number) => void
value: (from: number, to: number, duration: number, callback: (value: number) => void, options?: TweenOptions) => ValueTweenHandle
static new (module: AnimCore, registry: CppRegistry): TweenAPI
static prototype: TweenAPI
```

## TweenFactory — type @experimental
```
() => Completable
```

## TweenGroup — class @experimental
```
@internal checkComplete: () => boolean
cancel: () => void
onComplete: (callback: () => void) => TweenGroup
pause: () => void
resume: () => void
state: TweenState
static new (tweens: Completable[]): TweenGroup
static prototype: TweenGroup
```

## TweenHandle — class @experimental
```
bezier: (p1x: number, p1y: number, p2x: number, p2y: number) => TweenHandle
cancel: () => void
entity: number
pause: () => void
resume: () => void
state: TweenState
then: (next: TweenHandle | ValueTweenHandle) => TweenHandle
static new (module: AnimCore, registry: CppRegistry, valueManager: ValueTweenManager, entity: Entity): TweenHandle
static prototype: TweenHandle
```

## TweenOptions — interface @experimental
```
delay: number | undefined
easing: EasingType | undefined
loop: LoopMode | undefined
loopCount: number | undefined
```

## TweenSequence — class @experimental
```
@internal checkComplete: () => boolean
cancel: () => void
onComplete: (callback: () => void) => TweenSequence
pause: () => void
resume: () => void
state: TweenState
static new (factories: TweenFactory[]): TweenSequence
static prototype: TweenSequence
```

## TweenState — type @experimental
```
(typeof TweenState)[keyof typeof TweenState]
```

## TweenTarget — type @experimental
```
(typeof TweenTarget)[keyof typeof TweenTarget]
```

## UICameraData — interface @beta
```
screenH: number
screenW: number
valid: boolean
viewProjection: Float32Array<ArrayBufferLike>
vpH: number
vpW: number
vpX: number
vpY: number
worldBottom: number
worldLeft: number
worldMouseX: number
worldMouseY: number
worldRight: number
worldTop: number
```

## UICameraInfo — const @beta
```
ResourceDef<UICameraData>
```

## UIController — const @experimental
```
ComponentDef<UIControllerData>
```

## UIControllerData — interface @experimental
```
controllers: ControllerState[]
```

## UIDialog — const @experimental
```
ComponentDef<UIDialogData>
```

## UIDialogData — interface @experimental
```
closeOnBackdrop: boolean
closeOnEscape: boolean
```

## UIDisplay — enum @experimental
```
Flex = 0
None = 1
```

## UIDropdown — const @experimental
```
ComponentDef<UIDropdownData>
```

## UIDropdownData — interface @experimental
```
label: number
optionHeight: number
options: string[]
selectedIndex: number
```

## UIEntityInit — interface @experimental
```
node: UINodeInit | undefined
parent: number | undefined
text: TextInit | undefined
visual: UIVisualInit | undefined
world: World
```

## UIEvent — interface @experimental
```
currentTarget: number
data: TData
defaultPrevented: boolean
preventDefault: () => void
propagationStopped: boolean
stopPropagation: () => void
target: number
type: string
```

## UIEventHandler — type @experimental
```
(event: EntityEvent<TData>) => void
```

## UIEventQueue — class @experimental
```
clear: () => void
drain: () => readonly EntityEvent[]
emit: <TData = unknown>(entity: Entity, type: string, data?: TData) => EntityEvent<TData>
emitBubbled: (ancestor: Entity, rootEvent: EntityEvent) => EntityEvent
hasListenersFor: (type: string) => boolean
on: { (entity: Entity, type: string, handler: EntityEventHandler): Unsubscribe; (type: string, handler: EntityEventHandler): Unsubscribe; }
query: (type: string) => readonly EntityEvent[]
removeAll: (entity: Entity) => void
static new (): EntityEventQueue
static prototype: EntityEventQueue
```

## UIEventType — type @experimental
```
typeof UIEventType[keyof typeof UIEventType]
```

## UIEvents — const @experimental
```
ResourceDef<EntityEventQueue>
```

## UIGear — const @experimental
```
ComponentDef<UIGearData>
```

## UIGearData — interface @experimental
```
bindings: GearBinding[]
```

## UIInteraction — const @experimental
```
BuiltinComponentDef<UIInteractionData>
```

## UIInteractionData — interface @experimental
```
hovered: boolean
justPressed: boolean
justReleased: boolean
pressed: boolean
```

## UIInteractionPlugin — class @experimental
```
build: (app: App) => void
dependencies: "uiLayout"[]
name: "uiInteraction"
static new (): UIInteractionPlugin
static prototype: UIInteractionPlugin
```

## UILayoutGeneration — const @experimental
```
ResourceDef<UILayoutGenerationData>
```

## UILayoutGenerationData — interface @experimental
```
generation: number
```

## UILayoutPlugin — class @experimental
```
build: (app: App) => void
name: string
static new (): UILayoutPlugin
static prototype: UILayoutPlugin
```

## UIMask — const @experimental
```
BuiltinComponentDef<UIMaskData>
```

## UIMaskData — interface @experimental
```
alphaCutoff: number
enabled: boolean
mode: MaskMode
```

## UIMaskPlugin — class @experimental
```
build: (app: App) => void
name: "uiMask"
static new (): UIMaskPlugin
static prototype: UIMaskPlugin
```

## UINode — const @beta
```
BuiltinComponentDef<UINodeData>
```

## UINodeInit — interface @experimental
```
fill: boolean | undefined
flexGrow: number | undefined
flexShrink: number | undefined
height: Dimension | undefined
insetBottom: Dimension | undefined
insetLeft: Dimension | undefined
insetRight: Dimension | undefined
insetTop: Dimension | undefined
marginBottom: Dimension | undefined
marginLeft: Dimension | undefined
marginRight: Dimension | undefined
marginTop: Dimension | undefined
opacity: number | undefined
pointerEvents: number | undefined
position: number | undefined
width: Dimension | undefined
```

## UIPlugin — class @experimental
```
attachScrollContainer: (entity: Entity, container: ScrollContainer) => void
build: (app: App) => void
cleanup: (app?: App) => void
detachScrollContainer: (entity: Entity) => void
events: UIEventQueue
name: string
registerListView: (list: ListView<unknown>) => void
unregisterListView: (list: ListView<unknown>) => void
static new (): UIPlugin
static prototype: UIPlugin
```

## UIPointerEvents — enum @experimental
```
Auto = 0
None = 1
```

## UIPositionType — enum @experimental
```
Absolute = 1
Relative = 0
```

## UIRenderOrderPlugin — class @experimental
```
after: ("text" | "uiInteraction" | "uiMask")[]
build: (app: App) => void
dependencies: "uiLayout"[]
name: "uiRenderOrder"
static new (): UIRenderOrderPlugin
static prototype: UIRenderOrderPlugin
```

## UISlider — const @experimental
```
ComponentDef<UISliderData>
```

## UISliderData — interface @experimental
```
fill: number
handle: number
max: number
min: number
step: number
value: number
```

## UIToggle — const @experimental
```
ComponentDef<UIToggleData>
```

## UIToggleData — interface @experimental
```
check: number
isOn: boolean
```

## UIVisibilityPlugin — class @experimental
```
build: (app: App) => void
dependencies: "uiInteraction"[]
name: "uiVisibility"
static new (): UIVisibilityPlugin
static prototype: UIVisibilityPlugin
```

## UIVisual — const @beta
```
BuiltinComponentDef<UIVisualData>
```

## UIVisualData — interface @experimental
```
color: Color
enabled: boolean
fillAmount: number
fillMethod: FillMethod
fillOrigin: FillOrigin
fit: UIVisualFit
material: number
sliceBorder: Vec4
texture: number
tileSize: Vec2
uvOffset: Vec2
uvScale: Vec2
visualType: UIVisualType
```

## UIVisualInit — interface @experimental
```
color: { r: number; g: number; b: number; a: number; } | undefined
enabled: boolean | undefined
fillAmount: number | undefined
fillMethod: FillMethod | undefined
fillOrigin: FillOrigin | undefined
fit: UIVisualFit | undefined
material: number | undefined
sliceBorder: { x: number; y: number; z: number; w: number; } | undefined
texture: number | undefined
tileSize: Vec2 | undefined
uvOffset: Vec2 | undefined
uvScale: Vec2 | undefined
visualType: UIVisualType | undefined
```

## UIVisualType — enum @experimental
```
Filled = 5
Image = 2
NineSlice = 3
None = 0
SolidColor = 1
Tiled = 4
```

## UniformValue — type @experimental
```
number | Vec2 | Vec3 | Vec4 | number[] | TextureRef
```

## Unsubscribe — type @experimental
```
() => void
```

## UnwrapQueryArg — type @public
```
T extends MutWrapper<infer C> ? C :
    T extends AddedWrapper<infer C> ? C :
    T extends ChangedWrapper<infer C> ? C :
    T
```

## ValidatePrefabOptions — interface @experimental
```
instanceOverrides: readonly PrefabOverride[] | undefined
loadPrefab: ((path: string) => PrefabData | null) | undefined
```

## ValidateResult — interface @experimental
```
duplicateIds: string[]
invalidIds: string[]
orphanedChildren: string[]
stale: StaleOverride[]
```

## ValueTweenHandle — class @experimental
```
@internal manager: ValueTweenManager
bezier: (p1x: number, p1y: number, p2x: number, p2y: number) => ValueTweenHandle
cancel: () => void
id: number
pause: () => void
resume: () => void
state: TweenState
then: { (next: ValueTweenHandle): ValueTweenHandle; (next: { pause(): void; resume(): void; }): ValueTweenHandle; }
static new (manager: ValueTweenManager, id: number): ValueTweenHandle
static prototype: ValueTweenHandle
```

## Vec2 — interface @public
```
x: number
y: number
```

## Vec2Like — interface @experimental
```
x: number
y: number
```

## Vec3 — interface @public
```
x: number
y: number
z: number
```

## Vec4 — interface @public
```
w: number
x: number
y: number
z: number
```

## Velocity — const @experimental
```
BuiltinComponentDef<VelocityData>
```

## VelocityData — interface @experimental
```
angular: Vec3
linear: Vec3
```

## VertexAttributeDescriptor — interface @experimental
```
name: string
type: DataType
```

## Video — const @experimental
```
ComponentDef<VideoData>
```

## VideoAPI — class @experimental
```
backendName: string
baseUrl: string
dispose: () => void
play: (source: string, options?: VideoPlayOptions) => VideoStreamHandle
setRefResolver: (resolver: ((ref: string) => string) | null) => void
stop: (handle: VideoStreamHandle) => void
stopAll: () => void
update: (module: ESEngineModule | null) => void
static new (backend: PlatformVideoBackend): VideoAPI
static prototype: VideoAPI
```

## VideoData — interface @experimental
```
autoplay: boolean
enabled: boolean
fitSize: boolean
loop: boolean
muted: boolean
playbackRate: number
source: string
volume: number
```

## VideoHandle — type @experimental
```
VideoStreamHandle
```

## VideoPlayOptions — type @experimental
```
VideoStreamOptions
```

## VideoPlayer — const @experimental
```
ResourceDef<VideoAPI>
```

## VideoPlugin — class @experimental
```
build: (app: App) => void
cleanup: () => void
name: string
stopAll: () => void
static new (): VideoPlugin
static prototype: VideoPlugin
```

## VideoStreamHandle — interface @experimental
```
bytes: number | undefined
currentTime: number
duration: number
height: number
id: number
isPlaying: boolean
isReady: boolean
onEnded: (() => void) | undefined
onError: ((error: unknown) => void) | undefined
onReady: (() => void) | undefined
pause: () => void
play: () => void
pump: (module: ESEngineModule | null) => void
seek: (timeSeconds: number) => void
setLoop: (loop: boolean) => void
setMuted: (muted: boolean) => void
setPlaybackRate: (rate: number) => void
setVolume: (volume: number) => void
stop: () => void
textureHandle: number
width: number
```

## VideoStreamOptions — interface @experimental
```
audioTrackUrl: string | undefined
autoplay: boolean | undefined
loop: boolean | undefined
muted: boolean | undefined
playbackRate: number | undefined
volume: number | undefined
```

## Viewport — interface @experimental
```
h: number
w: number
x: number
y: number
```

## Virtual — const @public
```
(id: string) => Binding
```

## WangIndex — interface @experimental
```
byKey: Map<number, number>
```

## WangIndices — interface @experimental
```
sets: Map<number, WangIndex>
tileTerrain: Map<number, number>
```

## WeChatRuntimeConfig — interface @experimental
```
audioConfig: AudioProjectConfig | undefined
colorSpace: "gamma" | "linear" | undefined
depthLayers: number | undefined
engineFactory: (opts: unknown) => Promise<ESEngineModule>
engineWasmPath: string | undefined
firstScene: string
physicsConfig: PhysicsPluginConfig | undefined
physicsEnabled: boolean | undefined
runtimeConfig: RuntimeBuildConfig | undefined
sceneNames: string[]
screenFit: { designWidth: number; designHeight: number; scaleMode: number; matchWidthOrHeight: number; } | undefined
sideModuleFactories: Partial<Record<SideModuleId, EmscriptenFactory>> | undefined
sideModules: { id: string; file: string; globalName?: string; }[] | undefined
uiTheme: "dark" | "light" | undefined
uiThemeOverrides: ThemeOverrides | undefined
ySortLayers: number | undefined
```

## WeChatSideModuleFactories — type @experimental
```
Partial<Record<SideModuleId, EmscriptenFactory>>
```

## WeChatSocket — class @experimental
```
close: (code?: number, reason?: string) => void
connect: () => void
on: <K extends keyof PlatformSocketEvents>(event: K, handler: (...args: PlatformSocketEvents[K]) => void) => () => void
readyState: PlatformSocketReadyState
send: (data: string | ArrayBuffer) => void
static new (options: GameSocketOptions): WeChatSocket
static prototype: WeChatSocket
```

## WebAppOptions — interface @experimental
```
colorSpace: "gamma" | "linear" | undefined
depthLayers: number | undefined
getViewportSize: (() => { width: number; height: number; }) | undefined
plugins: Plugin[] | undefined
randomSeed: number | undefined
renderSurface: RenderSurfaceSource | undefined
screenFit: ScreenScalingData | undefined
sideModules: SideModuleHost | undefined
ySortLayers: number | undefined
```

## WeldJoint — const @experimental
```
ComponentDef<WeldJointData>
```

## WheelJoint — const @experimental
```
ComponentDef<WheelJointData>
```

## WidgetInteractionInit — interface @experimental
```
disabled: boolean | undefined
focusable: boolean | undefined
tabIndex: number | undefined
```

## With — function @experimental
```
(component: AnyComponentDef): FilterExpr
```

## Without — function @experimental
```
(component: AnyComponentDef): FilterExpr
```

## World — class @public
```
@internal advanceTick: () => void
@internal anyChangedSince: (component: AnyComponentDef, sinceTick: number) => boolean
@internal applyEntityOrder: (entities: readonly Entity[]) => void
@internal beginIteration: () => void
@internal builtin: BuiltinBridge
@internal changes_: ChangeTracker
@internal cleanRemovedBuffer: (beforeTick: number) => void
@internal connectCpp: (cppRegistry: CppRegistry, module?: ESEngineModule, options?: BridgeConnectOptions) => void
@internal disconnectCpp: () => void
@internal enableChangeTracking: (component: AnyComponentDef) => void
@internal endIteration: () => void
@internal getCppRegistry: () => CppRegistry | null
@internal getQueryCacheStats: () => QueryCacheStats
@internal getRemovedEntitiesSince: (component: AnyComponentDef, sinceTick: number) => Entity[]
@internal getStorageSizes: () => { entities: number; indexSlots: number; spawnCallbacks: number; despawnCallbacks: number; names: ReturnType<NameIndex["sizes"]>; scripts: ReturnType<ScriptStorage["sizes"]>; changes: ReturnType<ChangeTracker["sizes"]>; queryCacheEntries: number; }
@internal getWasmModule: () => ESEngineModule | null
@internal getWorldTick: () => number
@internal getWorldVersion: () => number
@internal hasCpp: boolean
@internal isAddedSince: (entity: Entity, component: AnyComponentDef, sinceTick: number) => boolean
@internal isChangedSince: (entity: Entity, component: AnyComponentDef, sinceTick: number) => boolean
@internal isIterating: () => boolean
@internal markChanged: (entity: Entity, component: AnyComponentDef) => void
@internal queries_: QueryCache
@internal queryEntities: (components: AnyComponentDef[], withFilters?: AnyComponentDef[], withoutFilters?: AnyComponentDef[], precomputedKey?: string, filter?: QueryFilter, precomputedDepIds?: symbol[]) => readonly Entity[]
@internal resetIterationDepth: () => void
@internal resetQueryCacheStats: () => void
@internal resetQueryPool: () => void
@internal resolveGetter: (component: AnyComponentDef) => ((entity: Entity) => unknown) | null
@internal resolveHas: (component: AnyComponentDef) => ((entity: Entity) => boolean) | null
@internal resolveSetter: (component: AnyComponentDef) => ((entity: Entity, data: unknown) => void) | null
despawn: (entity: Entity) => void
entityCount: () => number
findEntityByName: (name: string) => Entity | null
get: <C extends AnyComponentDef>(entity: Entity, component: C) => ComponentData<C>
getAllEntities: () => Entity[]
getComponentTypes: (entity: Entity) => string[]
getEntitiesWithComponents: (components: AnyComponentDef[], withFilters?: AnyComponentDef[], withoutFilters?: AnyComponentDef[]) => readonly Entity[]
has: (entity: Entity, component: AnyComponentDef) => boolean
insert: <C extends AnyComponentDef>(entity: Entity, component: C, data?: Partial<ComponentData<C>>) => ComponentData<C>
isStale: (entity: Entity) => boolean
onDespawn: (callback: (entity: Entity) => void) => () => void
onSpawn: (callback: (entity: Entity) => void) => () => void
remove: (entity: Entity, component: AnyComponentDef) => void
removeParent: (entity: Entity) => void
set: <C extends AnyComponentDef>(entity: Entity, component: C, data: ComponentData<C>) => void
setParent: (child: Entity, parent: Entity) => void
spawn: (name?: string) => Entity
tryGet: <C extends AnyComponentDef>(entity: Entity, component: C) => ComponentData<C> | null
valid: (entity: Entity) => boolean
static new (): World
static prototype: World
```

## WorldRect — interface @experimental
```
bottom: number
left: number
right: number
top: number
```

## WorldTransform — const @experimental @deprecated
```
BuiltinComponentDef<TransformData>
```

## WorldTransformData — type @experimental
```
TransformData
```

## WrapMode — type @experimental
```
(typeof WrapMode)[keyof typeof WrapMode]
```

## actionRefArg — function @experimental
```
(ref: FsmActionRef | undefined): string | undefined
```

## actionRefName — function @experimental
```
(ref: FsmActionRef | undefined): string
```

## actionRefParams — function @experimental
```
(ref: FsmActionRef | undefined): Record<string, AiParamValue> | undefined
```

## activeRemoteRoot — function @experimental
```
(config: AssetGroupsConfig | null | undefined): string | undefined
```

## addAnimatorParam — function @experimental
```
(def: AnimatorControllerDef, name: string, type: AnimatorParamType): AnimatorControllerDef
```

## addAnimatorState — function @experimental
```
(def: AnimatorControllerDef, name: string, x?: number, y?: number): AnimatorControllerDef
```

## addAnimatorTransition — function @experimental
```
(def: AnimatorControllerDef, from: string, to: string): AnimatorControllerDef
```

## addBtChild — function @experimental
```
(def: BtDefinition, parentId: string, type: BtNodeType, x?: number, y?: number): BtDefinition
```

## addBtOrphan — function @experimental
```
(def: BtDefinition, type: BtNodeType, x?: number, y?: number): BtDefinition
```

## addNode — function @beta
```
(g: MaterialGraph, type: GraphNodeType, x: number, y: number): { graph: MaterialGraph; id: string; }
```

## addStartupSystem — function @public
```
(system: SystemDef): void
```

## addState — function @experimental
```
(def: FsmDefinition, name: string, x?: number, y?: number): FsmDefinition
```

## addSystem — function @experimental
```
(system: SystemDef): void
```

## addSystemSetToSchedule — function @public
```
(schedule: Schedule, set: SystemSet): void
```

## addSystemToSchedule — function @public
```
(schedule: Schedule, system: SystemDef): void
```

## addTransition — function @experimental
```
(def: FsmDefinition, from: string, to: string): FsmDefinition
```

## agentBlackboard — function @experimental
```
(states: Map<Entity, AgentState>, entity: Entity): Blackboard
```

## agentBtBlackboard — function @experimental
```
(states: Map<Entity, BtAgentState>, entity: Entity): Blackboard
```

## aiRegistry — const @experimental
```
AiRegistry<AiContext>
```

## analyzeCensusSeries — function @experimental
```
(samples: readonly Census[], opts?: CensusSeriesOptions): CensusReport
```

## anchorPresetFields — function @experimental
```
(preset: AnchorPreset): AnchorFields
```

## animClipCellRect — function @experimental
```
(sheet: AnimClipSheetData, cell: number): { x: number; y: number; width: number; height: number; }
```

## animClipCellUv — function @experimental
```
(sheet: AnimClipSheetData, cell: number): { uvOffset: { x: number; y: number; }; uvScale: { x: number; y: number; }; }
```

## animClipDrivesPivot — function @experimental
```
(data: AnimClipAssetData): boolean
```

## animClipFramePivot — function @experimental
```
(data: AnimClipAssetData, frame: AnimClipFrameData): AnimClipPivotData | null
```

## animClipSheetCols — function @experimental
```
(sheet: AnimClipSheetData): number
```

## animClipSheetRows — function @experimental
```
(sheet: AnimClipSheetData): number
```

## animationPlugin — const @experimental
```
AnimationPlugin
```

## animatorEdges — function @experimental
```
(def: AnimatorControllerDef): AnimatorEdge[]
```

## applyAssetRefResolvers — function @experimental
```
(app: App, resolveRef: (ref: string) => string): void
```

## applyAudioProjectConfig — function @experimental
```
(audio: AudioAPI, config: AudioProjectConfig | undefined): void
```

## applyBuildRuntimeConfig — function @experimental
```
(app: { setMaxDeltaTime(v: number): void; setMaxFixedSteps(v: number): void; }, config: RuntimeBuildConfig): void
```

## applyDeltaToSource — function @experimental
```
(source: PrefabData, delta: SourceDelta): PrefabData
```

## applyOverrides — function @experimental
```
(entity: OverrideTarget, overrides: readonly PrefabOverride[] | undefined): void
```

## applyOverridesToSource — function @experimental
```
(source: PrefabData, overrides: readonly PrefabOverride[]): PrefabData
```

## applyRuntimeConfig — function @experimental
```
(components: { Text?: { _default: Record<string, unknown>; }; TextInput?: { _default: Record<string, unknown>; }; Canvas?: { _default: Record<string, unknown>; }; }): void
```

## applyThemeToWorld — function @experimental
```
(world: World): void
```

## applyWrapMode — function @experimental
```
(time: number, duration: number, mode: WrapMode): { time: number; stopped: boolean; }
```

## arrayDataSource — function @experimental
```
<T>(items: readonly T[]): ArrayDataSource<T>
```

## assetPlugin — const @experimental
```
AssetPlugin
```

## atlasCatalogFields — function @experimental
```
(atlas: CookedAtlasInfo, pagePath: string): Pick<CatalogEntry, "atlas" | "frame" | "uv">
```

## audioPlugin — const @experimental
```
AudioPlugin
```

## auto — const @experimental
```
() => Dimension
```

## bind — function @experimental
```
<C extends object, K extends keyof C>(world: World, entity: Entity, component: ComponentDef<C>, field: K, source: ReadonlySignal<C[K]>): () => void
```

## bindControllerPage — function @experimental
```
(world: World, entity: Entity, controller: string, source: ReadonlySignal<string>): () => void
```

## bindWidgetValue — function @experimental
```
<C extends object, K extends keyof C>(world: World, events: UIEventQueue, entity: Entity, component: ComponentDef<C>, field: K, signal: Signal<C[K]>, payloadKey?: string): () => void
```

## btEdges — function @experimental
```
(def: BtDefinition): BtEdge[]
```

## btNodes — function @experimental
```
(def: BtDefinition): BtNode[]
```

## btPlugin — const @experimental
```
BtPlugin
```

## bucketOverridesByEntity — function @experimental
```
(overrides: readonly PrefabOverride[]): Map<PrefabEntityId, PrefabOverride[]>
```

## buildCollisionPaletteModel — function @experimental
```
(material?: CollisionMaterial): TilesetModel
```

## buildTerrainIndices — function @experimental
```
(asset: TilesetAsset): TerrainIndices
```

## buildText — function @experimental
```
(init?: TextInit): TextData
```

## buildUINode — function @experimental
```
(init?: UINodeInit): UINodeData
```

## buildUIVisual — function @experimental
```
(init?: UIVisualInit): UIVisualData
```

## buildVariant — function @experimental
```
(base: PrefabData, baseRef: string, name: string, delta: { overrides: readonly PrefabOverride[]; added: readonly AddedEntity[]; }): PrefabData
```

## buildWangIndices — function @experimental
```
(asset: TilesetAsset): WangIndices
```

## builtinShaderTemplate — function @experimental
```
(id: string): BuiltinShaderTemplate | undefined
```

## calculateAttenuation — function @experimental
```
(distance: number, config?: SpatialAudioConfig): number
```

## calculatePanning — function @experimental
```
(sourceX: number, sourceY: number, listenerX: number, listenerY: number, maxDistance: number): number
```

## canHaveChildren — function @experimental
```
(type: BtNodeType): boolean
```

## canonicalMask — function @experimental
```
(mode: TerrainMode, mask: number): number
```

## catalogFromManifest — function @experimental
```
(manifest: AddressableManifest): Catalog | undefined
```

## censusProbeIds — function @experimental
```
(): string[]
```

## clearAllCaches — function @experimental
```
(): void
```

## clearAnimatorControllerStore — function @experimental
```
(): void
```

## clearBtStore — function @experimental
```
(): void
```

## clearDrawCallbacks — function @experimental
```
(): void
```

## clearFsmStore — function @experimental
```
(): void
```

## clearProjectSideModules — function @experimental
```
(): void
```

## clearTilemapSourceCache — function @experimental
```
(): void
```

## clearUserComponents — function @experimental
```
(): void
```

## cloneComponentData — function @experimental
```
(data: Record<string, unknown>): Record<string, unknown>
```

## cloneComponents — function @experimental
```
(components: ComponentData[]): ComponentData[]
```

## cloneMetadata — function @experimental
```
(metadata: Record<string, unknown>): Record<string, unknown>
```

## closeDialog — function @experimental
```
(world: World, events: UIEventQueue, root: Entity): void
```

## closeDropdown — function @experimental
```
(world: World, e: Entity): void
```

## col — const @experimental
```
{ readonly create: (r?: number, g?: number, b?: number, a?: number) => Color; readonly rgb: (r: number, g: number, b: number) => Color; readonly from255: (r: number, g: number, b: number, a?: number) => Color; readonly clone: (c: Color) => Color; readonly lerp: (a: Color, b: Color, t: number) => Color; readonly withAlpha: (c: Color, a: number) => Color; readonly multiply: (a: Color, b: Color) => Color; readonly scaleRgb: (c: Color, s: number) => Color; readonly fromHex: (hex: string) => Color; readonly toHex: (c: Color, withAlpha?: boolean) => string; readonly equals: (a: Color, b: Color, epsilon?: number) => boolean; }
```

## collapseEntry — function @experimental
```
(prefab: PrefabData, prefabRef: string, expanded: readonly ProcessedEntity[], rootId: number, sceneParent: number | null, loadPrefab?: SyncPrefabResolver): PrefabInstanceEntry
```

## collapseInstance — function @experimental
```
(prefab: PrefabData, prefabRef: string, expanded: readonly ProcessedEntity[], loadPrefab?: SyncPrefabResolver): PrefabInstanceDelta
```

## collectExternalEntityRefs — function @experimental
```
(entities: readonly ExtractEntity[]): ExternalEntityRef[]
```

## collectGarbage — function @experimental
```
(): boolean
```

## collectNestedPrefabPaths — function @experimental
```
(prefab: PrefabData, loadPrefab: (path: string) => PrefabData | null, visited?: Set<string>): string[]
```

## collidableTileIds — function @experimental
```
(asset: TilesetAsset): number[]
```

## colliderShapeOutline — function @experimental
```
(shape: ColliderShape, center: Vec2, angle: number, ppu: number): ColliderOutline
```

## collisionRefWithMaterial — function @experimental
```
(material: CollisionMaterial): string
```

## color — const @experimental
```
(r?: number, g?: number, b?: number, a?: number) => Color
```

## compareDrawRank — function @experimental
```
(a: DrawRank, b: DrawRank): number
```

## compileFsm — function @experimental
```
(def: FsmDefinition): CompiledFsm
```

## compileMaterialGraph — function @beta
```
(graph: MaterialGraph): string
```

## computeEffectiveOrthoSize — function @experimental
```
(baseOrthoSize: number, designAspect: number, actualAspect: number, scaleMode: number, matchWidthOrHeight: number): number
```

## computeStreaming — function @experimental
```
(cells: readonly StreamCell[], focusX: number, focusY: number, loadRadius: number, unloadRadius: number, active: ReadonlySet<string>): StreamDecision
```

## connect — function @beta
```
(g: MaterialGraph, fromId: string, toId: string, toSlot: string): MaterialGraph
```

## contentHashHex — function @experimental
```
(data: Uint8Array): string
```

## contentHashOf — function @experimental
```
(data: Uint8Array | string): string
```

## controllerState — function @experimental
```
(name: string, pages: string[], current?: string): ControllerState
```

## corePlugin — const @experimental
```
Plugin
```

## createAnimClip — function @experimental
```
(texture: string, cellWidth: number, cellHeight: number, pageWidth: number, pageHeight: number): AnimClipAssetData
```

## createBtRunState — function @experimental
```
(): BtRunState
```

## createButton — function @experimental
```
(opts: ButtonOptions): ButtonHandle
```

## createDialog — function @experimental
```
(opts: DialogOptions): DialogHandle
```

## createDropdown — function @experimental
```
<T>(opts: DropdownOptions<T>): DropdownHandle<T>
```

## createEmbeddedSideModuleHost — function @experimental
```
(registry: EmbeddedSideModuleRegistry): SideModuleHost
```

## createFetchSideModuleHost — function @experimental
```
(baseUrl: string): SideModuleHost
```

## createFsmRunState — function @experimental
```
(fsm: CompiledFsm): FsmRunState
```

## createHeadlessApp — function @beta
```
(module: ESEngineModule, options?: HeadlessAppOptions): App
```

## createListView — function @experimental
```
<T>(opts: CreateListViewOptions<T>): ListViewHandle<T>
```

## createLocalAchievements — function @experimental
```
(): AchievementProvider
```

## createLocalLeaderboard — function @experimental
```
(options?: LocalLeaderboardOptions): LeaderboardProvider
```

## createMemoryTransportPair — function @experimental
```
(options?: MemoryTransportOptions): [MemoryTransport, MemoryTransport]
```

## createMockAdProvider — function @experimental
```
(options?: MockAdProviderOptions): AdProvider
```

## createPackagedAssetSource — function @experimental
```
(index: PackagedAssetIndex, options?: PackagedAssetSourceOptions): RuntimeAssetSource
```

## createProgress — function @experimental
```
(opts: ProgressOptions): ProgressHandle
```

## createRuntimeSceneConfig — function @experimental
```
(name: string, sceneData: SceneData | undefined, options: Omit<LoadRuntimeSceneOptions, "sceneData" | "sceneName">, scenePath?: string): SceneConfig
```

## createScrollView — function @experimental
```
(opts: ScrollViewOptions): ScrollViewHandle
```

## createSlider — function @experimental
```
(opts: SliderOptions): SliderHandle
```

## createSocket — function @experimental
```
(options: GameSocketOptions): PlatformSocket
```

## createTakeover — function @experimental
```
(host: TakeoverHost): Takeover
```

## createTextInput — function @experimental
```
(opts: TextInputOptions): TextInputHandle
```

## createTileset — function @experimental
```
(texture: string, tileWidth?: number, tileHeight?: number, columns?: number): TilesetAsset
```

## createToggle — function @experimental
```
(opts: ToggleOptions): ToggleHandle
```

## createWeChatSideModuleHost — function @experimental
```
(factories: MiniGameSideModuleFactories): SideModuleHost
```

## createWebApp — function @experimental
```
(module: ESEngineModule, options?: CreateWebAppOptions): App
```

## debug — function @experimental
```
(category: string, message: string, data?: unknown): void
```

## decodeImageBitmap — function @experimental
```
(src: ImageBitmapSource | PlatformImage, flip: boolean): Promise<ImageBitmap>
```

## decodeImagePixels — function @experimental
```
(src: ImageBitmapSource): Promise<DecodedPixels>
```

## decodeTilemapChunks — function @experimental
```
(blob: string): DecodedChunk[]
```

## defaultFrameStats — function @experimental
```
(): FrameStats
```

## defaultPluralSelector — const @experimental
```
PluralSelector
```

## defineBehavior — function @experimental
```
<S extends object>(name: string, def: BehaviorDef<S>): ComponentDef<S>
```

## defineComponent — function @public
```
<T extends object>(name: string, defaults: T, metadata?: ComponentMetadata): ComponentDef<T>
```

## defineEvent — function @public
```
<T>(name: string): EventDef<T>
```

## defineInputMap — function @public
```
(actions: Record<string, ActionDef>): InputMap
```

## defineResource — function @public
```
<T>(defaultValue: T, name?: string): ResourceDef<T>
```

## defineSystem — function @public
```
<P extends readonly SystemParam[]>(params: [...P], fn: (...args: InferParams<P>) => void | Promise<void>, options?: SystemOptions): SystemDef
```

## defineSystemSet — function @public
```
(name: string, options: SystemSetOptions): SystemSet
```

## defineTag — function @public
```
(name: string): ComponentDef<{}>
```

## derived — function @experimental
```
<T>(sources: ReadonlySignal<unknown>[], compute: () => T): ReadonlySignal<T>
```

## detectAnchor — function @experimental
```
(node: UINodeData): AnchorPreset | null
```

## detectAnchorAxes — function @experimental
```
(node: UINodeData): { h: AnchorAxis | null; v: AnchorAxis | null; }
```

## diagnosticsPlugin — const @experimental
```
DiagnosticsPlugin
```

## diffAgainstSource — function @experimental
```
(source: PrefabData, instance: readonly ProcessedEntity[], options?: DiffOptions): { overrides: PrefabOverride[]; untracked: ProcessedEntity[]; orphanedSourceIds: PrefabEntityId[]; }
```

## diffCensus — function @experimental
```
(before: Census, after: Census): CensusDelta[]
```

## disconnect — function @beta
```
(g: MaterialGraph, toId: string, toSlot: string): MaterialGraph
```

## discoverSceneAssets — function @experimental
```
(sceneData: SceneData, refResolver?: RefResolver): SceneAssetRefs
```

## dragPlugin — const @experimental
```
DragPlugin
```

## dragonBonesPlugin — const @experimental
```
() => DragonBonesPlugin
```

## editorViewHalfExtent — function @experimental
```
(view: EditorViewData, aspect: number): { halfW: number; halfH: number; }
```

## editorViewHalfHeight — function @experimental
```
(view: EditorViewData): number
```

## emptyAnimatorController — function @experimental
```
(): AnimatorControllerDef
```

## emptyBt — function @experimental
```
(): BtDefinition
```

## emptyFsm — function @experimental
```
(): FsmDefinition
```

## encodeTile — function @experimental
```
(id: number, flags?: TileFlags): number
```

## ensureBtIds — function @experimental
```
(def: BtDefinition): BtDefinition
```

## enterStatePath — function @experimental
```
(scope: AnimatorScope, name: string): string[]
```

## enumOptions — function @experimental
```
(obj: Record<string, unknown>, only?: readonly string[]): ReadonlyArray<{ label: string; value: number; }>
```

## error — function @experimental
```
(category: string, message: string, data?: unknown): void
```

## evalGuard — function @experimental
```
(bb: Blackboard, guard: BlackboardGuard): boolean
```

## evalGuards — function @experimental
```
(bb: Blackboard, guards: BlackboardGuard | BlackboardGuard[]): boolean
```

## evaluateAnimatorPath — function @experimental
```
(def: AnimatorControllerDef, currentPath: string, params: AnimatorParamValues, triggers: ReadonlySet<string>, clipFinished?: boolean): AnimatorPathEvalResult
```

## evaluateAnimatorTransitions — function @experimental
```
(def: AnimatorControllerDef, currentState: string, params: AnimatorParamValues, triggers: ReadonlySet<string>, clipFinished?: boolean): AnimatorEvalResult
```

## evaluateChannel — function @experimental
```
(channel: PropertyChannel, time: number): number
```

## eventBindingPlugin — const @experimental
```
EventBindingPlugin
```

## evictTextureDimensions — function @experimental
```
(handle: number): void
```

## expandEntry — function @experimental
```
(prefab: PrefabData, entry: PrefabInstanceEntry, allocateId: () => number, loadPrefab?: SyncPrefabResolver): { entities: ProcessedEntity[]; rootId: number; }
```

## expandInstance — function @experimental
```
(prefab: PrefabData, delta: PrefabInstanceDelta, allocateId: () => number, loadPrefab?: SyncPrefabResolver): { entities: ProcessedEntity[]; rootId: number; }
```

## extractAnimClipTexturePaths — function @experimental
```
(data: AnimClipAssetData): string[]
```

## extractPrefab — function @experimental
```
(entities: readonly ExtractEntity[], rootId: number, name: string, makeId?: (sourceId: number) => PrefabEntityId): PrefabData
```

## facingFromQuat — function @experimental
```
(z: number, w: number): number
```

## fetchDecodePixels — function @experimental
```
(url: string): Promise<DecodedPixels>
```

## findControllerOwner — function @experimental
```
(world: World, entity: Entity, name: string): Entity | null
```

## findEntityByName — function @experimental
```
(world: World, name: string): Entity | null
```

## findPath — function @experimental
```
(grid: NavGrid, start: Cell, goal: Cell, opts?: PathfindOptions): Cell[] | null
```

## fingerprint — function @experimental
```
(report: DiagnosticReport): string
```

## finishDeviceRecovery — function @experimental
```
(): void
```

## flattenPrefab — function @experimental
```
(prefab: PrefabData, overrides: PrefabOverride[], ctx: FlattenContext): FlattenResult
```

## flipFlagsH — function @experimental
```
(flags: TileFlags): TileFlags
```

## flipFlagsV — function @experimental
```
(flags: TileFlags): TileFlags
```

## flipStampH — function @experimental
```
(s: TileStamp): TileStamp
```

## flipStampV — function @experimental
```
(s: TileStamp): TileStamp
```

## flushPendingSystems — function @experimental
```
(app: App): void
```

## focusPlugin — const @experimental
```
FocusPlugin
```

## folderAlwaysInclude — function @experimental
```
(config: AssetGroupsConfig | null | undefined, folder: string): boolean
```

## folderGroupMode — function @experimental
```
(config: AssetGroupsConfig | null | undefined, folder: string): AssetGroupMode
```

## formatActionArg — function @experimental
```
(params: Readonly<Record<string, AiParamValue>> | undefined, defs: readonly AiParamDef[], separator?: string): string | undefined
```

## formatCensusDiff — function @experimental
```
(before: Census, after: Census, all?: boolean): string
```

## formatCensusReport — function @experimental
```
(report: CensusReport): string
```

## fsmEdges — function @experimental
```
(def: FsmDefinition): FsmEdge[]
```

## fsmPlugin — const @experimental
```
FsmPlugin
```

## fxPreviewOrPlayMode — function @experimental
```
(): boolean
```

## gearBinding — function @experimental
```
(controller: string, component: string, property: string, pages: Record<string, GearValue>, tween?: GearTween): GearBinding
```

## getAddressableType — function @experimental
```
(path: string): AddressableAssetType | null
```

## getAddressableTypeByEditorType — function @experimental
```
(editorType: string): AddressableAssetType | null
```

## getAllAssetExtensions — function @experimental
```
(): Set<string>
```

## getAllEffectDefs — function @experimental
```
(): EffectDef[]
```

## getAssetBuildTransform — function @experimental
```
(extensionOrPath: string): AssetBuildTransform | undefined
```

## getAssetMimeType — function @experimental
```
(ext: string): string | undefined
```

## getAssetTypeEntry — function @experimental
```
(extensionOrPath: string): AssetTypeEntry | undefined
```

## getBt — function @experimental
```
(key: string): BtDefinition | undefined
```

## getCacheForEntity — function @experimental
```
(entityId: number): BitmapCache | undefined
```

## getComponent — function @experimental
```
(name: string): AnyComponentDef | undefined
```

## getComponentAssetFieldDescriptors — function @experimental
```
(componentType: string): readonly { field: string; type: AssetFieldType; }[]
```

## getComponentAssetFields — function @experimental
```
(componentType: string): string[]
```

## getComponentDefaults — function @experimental
```
(typeName: string): Record<string, unknown> | null
```

## getComponentFieldMeta — function @experimental
```
(name: string): Readonly<Record<string, FieldMeta>>
```

## getComponentRegistry — function @experimental
```
(): Map<string, AnyComponentDef>
```

## getComponentSkeletalFieldDescriptor — function @experimental
```
(componentType: string): { skeletonField: string; atlasField: string; runtime: string; } | null
```

## getContextLossGuardInfo — function @experimental
```
(): { target: string; lostEventsSeen: number; }
```

## getControllerPage — function @experimental
```
(world: World, entity: Entity, name: string): string | null
```

## getCustomExtensions — function @experimental
```
(): string[]
```

## getDefaultContext — function @experimental
```
(): AppContext
```

## getDeviceIdentity — function @experimental
```
(): DeviceIdentity | null
```

## getDeviceLostReport — function @experimental
```
(): string
```

## getDeviceStatus — function @experimental
```
(): number
```

## getEditorType — function @experimental
```
(path: string): EditorAssetType
```

## getEffectDef — function @experimental
```
(type: string): EffectDef | undefined
```

## getEffectTypes — function @experimental
```
(): string[]
```

## getFsm — function @experimental
```
(key: string): CompiledFsm | undefined
```

## getLogger — function @experimental
```
(): Logger
```

## getPlatform — function @experimental
```
(): PlatformAdapter
```

## getPlatformType — function @experimental
```
(): PlatformAdapter["name"] | null
```

## getRegisteredAnimatorController — function @experimental
```
(key: string): AnimatorControllerDef | undefined
```

## getReplicatedFields — function @experimental
```
(name: string): readonly string[]
```

## getResourceManager — function @experimental
```
(): CppResourceManager | null
```

## getResourceStats — function @experimental
```
(): ResourceStats | null
```

## getTextureDimensions — function @experimental
```
(handle: number): TextureDimensions | null
```

## getTheme — function @experimental
```
(): ThemeTokens
```

## getTilemapSource — function @experimental
```
(path: string): LoadedTilemapSource | undefined
```

## getUserComponent — function @experimental
```
(name: string): ComponentDef<any> | undefined
```

## getUserComponentFingerprint — function @experimental
```
(): string
```

## getUserComponents — function @experimental
```
(): Map<string, AnyComponentDef>
```

## getWeChatPackOptions — function @experimental
```
(): Array<{ type: string; value: string; }>
```

## hasPlayableCta — function @experimental
```
(): boolean
```

## hasVisibility — function @experimental
```
(world: World, entity: Entity): boolean
```

## headlessBasePlugins — const @experimental
```
() => Plugin[]
```

## imageBitmapOptions — function @experimental
```
(flip: boolean): ImageBitmapOptions
```

## indexPackagedManifest — function @experimental
```
(manifest: AddressableManifest): PackagedAssetIndex
```

## info — function @experimental
```
(category: string, message: string, data?: unknown): void
```

## initPlayRealmRuntime — function @experimental
```
(config: PlayRealmRuntimeConfig): Promise<void>
```

## initPlayableRuntime — function @experimental
```
(config: PlayableRuntimeConfig): Promise<void>
```

## initRuntime — function @experimental
```
(config: RuntimeInitConfig): Promise<void>
```

## initWeChatRuntime — function @experimental
```
(config: WeChatRuntimeConfig): Promise<void>
```

## inputEventCallbacks — function @experimental
```
(state: InputState): InputEventCallbacks
```

## inputPlugin — const @experimental
```
InputPlugin
```

## inputRouter — const @experimental
```
InputRouter
```

## installEditorGrid — function @experimental
```
(app: App): void
```

## instantiatePrefab — function @experimental
```
(world: World, prefab: PrefabData, options?: InstantiatePrefabOptions): Promise<InstantiatePrefabResult>
```

## interactionController — function @experimental
```
(pages?: string[]): ControllerState
```

## interpolate — function @experimental
```
(template: string, params?: TParams): string
```

## intersectRects — function @experimental
```
(a: ScreenRect, b: ScreenRect): ScreenRect
```

## invertMatrix4 — function @experimental
```
(m: Float32Array, result?: Float32Array): Float32Array
```

## invokeAction — function @experimental
```
<Ctx>(registry: AiRegistry<Ctx>, name: string, ctx: Ctx, bb: Blackboard, input?: AiActionInput): void | Status
```

## isAuto — const @experimental
```
(d: Dimension) => boolean
```

## isBuiltinComponent — function @experimental
```
(comp: AnyComponentDef): comp is BuiltinComponentDef<any>
```

## isCollisionPaletteRef — function @experimental
```
(refs: readonly string[]): boolean
```

## isCustomExtension — function @experimental
```
(path: string): boolean
```

## isDialogOpen — function @experimental
```
(world: World, root: Entity): boolean
```

## isDropdownOpen — function @experimental
```
(world: World, e: Entity): boolean
```

## isEditor — function @experimental
```
(): boolean
```

## isEmptyStamp — function @experimental
```
(s: TileStamp): boolean
```

## isEntityActive — function @experimental
```
(world: World, entity: Entity): boolean
```

## isEntityVisible — function @experimental
```
(world: World, entity: Entity): boolean
```

## isFxEditPreview — function @experimental
```
(): boolean
```

## isHexOrientation — function @experimental
```
(orientation: number): boolean
```

## isKnownAssetExtension — function @experimental
```
(ext: string): boolean
```

## isNative — function @experimental
```
(): boolean
```

## isNonOrthogonal — function @experimental
```
(orientation: number): boolean
```

## isPlatformInitialized — function @experimental
```
(): boolean
```

## isPlayMode — function @experimental
```
(): boolean
```

## isRuntime — function @experimental
```
(): boolean
```

## isTextureRef — function @experimental
```
(v: UniformValue): v is TextureRef
```

## isWeChat — function @experimental
```
(): boolean
```

## isWeb — function @experimental
```
(): boolean
```

## layerFrontness — function @experimental
```
(order: LayerOrder, worldY: number, worldZ: number): number
```

## layerOrderOf — function @experimental
```
(layer: number, ySortMask: number, depthMask: number): LayerOrder
```

## leafStateOf — function @experimental
```
(def: AnimatorControllerDef, path: string): AnimatorState | null
```

## lifecyclePlugin — const @experimental
```
LifecyclePlugin
```

## loadComponent — function @experimental
```
(world: World, entity: Entity, compData: SceneComponentData, entityName?: string): void
```

## loadInputMapAsset — function @experimental
```
(asset: InputMapAsset): InputMap
```

## loadPackagedAssetIndex — function @experimental
```
(manifestPath?: string): Promise<PackagedAssetIndex>
```

## loadPhysicsModule — function @experimental
```
(wasmUrl: string, factory?: PhysicsModuleFactory): Promise<PhysicsWasmModule>
```

## loadRuntimeScene — function @experimental
```
(options: LoadRuntimeSceneOptions): Promise<void>
```

## loadSceneData — function @experimental
```
(world: World, sceneData: SceneData): Map<number, Entity>
```

## loadSceneWithAssets — function @experimental
```
(world: World, sceneData: SceneData, options?: SceneLoadOptions): Promise<Map<number, Entity>>
```

## loadTiledMap — function @experimental
```
(world: World, mapData: TiledMapData, textureHandles: Map<string, number>, options?: TilemapLoadOptions): Entity[]
```

## localizationPlugin — const @experimental
```
LocalizationPlugin
```

## log — const @experimental
```
Logger
```

## looksLikeAssetPath — function @experimental
```
(value: unknown): value is string
```

## makeLosCheck — function @experimental
```
(physics: PhysicsAPI): LosCheck
```

## makeWidgetInteractable — function @experimental
```
(world: World, entity: Entity, init?: WidgetInteractionInit): void
```

## markThemed — function @experimental
```
(world: World, entity: Entity, style: ThemeStyleData): void
```

## matchLocale — function @experimental
```
(tag: string, available: readonly string[]): string | null
```

## maxChildren — function @experimental
```
(type: BtNodeType): number
```

## measureText — function @experimental
```
(text: string, opts: MeasureTextOptions): TextMetrics
```

## mesh2dPlugin — const @experimental
```
Mesh2DPlugin
```

## messageOf — function @experimental
```
(error: unknown): string
```

## migratePrefabData — function @experimental
```
(raw: unknown): MigrationResult
```

## migrateSaveData — function @experimental
```
(version: number, data: unknown, currentVersion: number, migrations: Record<number, SaveMigration>): unknown
```

## migrateSceneData — function @experimental
```
(raw: SceneData): SceneMigrationResult
```

## modeToDelivery — function @experimental
```
(mode: AssetGroupMode): BundleMode
```

## moveAnimatorState — function @experimental
```
(def: AnimatorControllerDef, name: string, x: number, y: number): AnimatorControllerDef
```

## moveBtNode — function @experimental
```
(def: BtDefinition, id: string, x: number, y: number): BtDefinition
```

## moveNode — function @beta
```
(g: MaterialGraph, id: string, x: number, y: number): MaterialGraph
```

## moveState — function @experimental
```
(def: FsmDefinition, name: string, x: number, y: number): FsmDefinition
```

## navGridFromTilemapLayer — function @experimental
```
(entity: Entity, opts: BuildNavGridOptions): NavGrid
```

## navGridFromTiles — function @experimental
```
(getTile: (x: number, y: number) => number, opts: BuildNavGridOptions): NavGrid
```

## navPlugin — const @experimental
```
NavPlugin
```

## newMaterialGraph — function @beta
```
(): MaterialGraph
```

## normalizeAngle — function @experimental
```
(a: number): number
```

## normalizeBundleMode — function @experimental
```
(mode: string | undefined | null): BundleMode
```

## normalizeCornerMask — function @experimental
```
(mask: number): number
```

## openDialog — function @experimental
```
(world: World, events: UIEventQueue, root: Entity): void
```

## openDropdown — function @experimental
```
(world: World, events: UIEventQueue, e: Entity): void
```

## orientationPerm — function @experimental
```
(flags: TileFlags): Perm
```

## packCorners — function @experimental
```
(tl: number, tr: number, br: number, bl: number): number
```

## packagedAppOptions — function @experimental
```
(config: Pick<PackagedGameConfig, "ySortLayers" | "depthLayers" | "colorSpace" | "screenFit">): { ySortLayers?: number; depthLayers?: number; colorSpace?: "gamma" | "linear"; screenFit?: PackagedGameConfig["screenFit"]; }
```

## packagedRuntimeInit — function @experimental
```
(config: Pick<PackagedGameConfig, "physicsEnabled" | "physicsConfig" | "audioConfig" | "uiTheme" | "uiThemeColors" | "achievements" | "steamAppId" | "screenFit">): { physicsEnabled?: boolean; physicsConfig?: PhysicsPluginConfig; audioConfig?: AudioProjectConfig; uiTheme?: "light"; uiThemeOverrides?: ThemeOverrides; achievements?: string[]; steamAppId?: number; screenFit?: PackagedGameConfig["screenFit"]; }
```

## paramDefaultValue — function @experimental
```
(param: ShaderParam): unknown
```

## parseActionArg — function @experimental
```
(arg: string | undefined, params: readonly AiParamDef[], separator?: string): Record<string, AiParamValue>
```

## parseAnimClipAsset — function @experimental
```
(rawJson: unknown): AnimClipAssetData
```

## parseAnimClipData — function @experimental
```
(clipPath: string, data: AnimClipAssetData, textureHandles: Map<string, number>): SpriteAnimClip
```

## parseAnimationClip — function @experimental
```
(rawJson: unknown): TimelineAsset
```

## parseAudioProjectConfig — function @experimental
```
(raw: unknown): AudioProjectConfig
```

## parseBusEffects — function @experimental
```
(raw: unknown): BusEffectDef[]
```

## parseCollisionMaterial — function @experimental
```
(refs: readonly string[]): CollisionMaterial
```

## parseLocaleTable — function @experimental
```
(text: string, path: string): LocaleTableAsset
```

## parseRichText — function @experimental
```
(input: string): RichTextRun[]
```

## parseThemeOverrides — function @experimental
```
(colors?: Record<string, string>): ThemeOverrides | undefined
```

## parseTileset — function @experimental
```
(rawJson: unknown): TilesetAsset
```

## parseTimelineAsset — function @experimental
```
(rawJson: unknown): TimelineAsset
```

## parseTmjJson — function @experimental
```
(json: Record<string, unknown>): TiledMapData | null
```

## parseTmjWithExternals — function @experimental
```
(json: Record<string, unknown>, resolveExternal: (source: string) => Promise<string>): Promise<TiledMapData | null>
```

## particlePlugin — const @experimental
```
ParticlePlugin
```

## pathToWorld — function @experimental
```
(grid: NavGrid, path: Cell[]): Vec2[]
```

## percent — const @public
```
(n: number) => Dimension
```

## perceptionPlugin — const @experimental
```
PerceptionPlugin
```

## platformFetch — function @experimental
```
(url: string, options?: PlatformRequestOptions): Promise<PlatformResponse>
```

## platformFileExists — function @experimental
```
(path: string): Promise<boolean>
```

## platformInstantiateWasm — function @experimental
```
(pathOrBuffer: string | ArrayBuffer, imports: WebAssembly.Imports): Promise<WasmInstantiateResult>
```

## platformReadFile — function @experimental
```
(path: string): Promise<ArrayBuffer>
```

## platformReadTextFile — function @experimental
```
(path: string): Promise<string>
```

## playModeOnly — function @experimental
```
(): boolean
```

## playableCta — function @experimental
```
(): void
```

## pointInHitArea — function @experimental
```
(px: number, py: number, area: HitAreaShape): boolean
```

## pointInOBB — function @experimental
```
(px: number, py: number, worldX: number, worldY: number, worldW: number, worldH: number, pivotX: number, pivotY: number, rotationZ: number, rotationW: number): boolean
```

## pointInWorldRect — function @experimental
```
(px: number, py: number, worldX: number, worldY: number, worldW: number, worldH: number, pivotX: number, pivotY: number): boolean
```

## postProcessPlugin — const @experimental
```
PostProcessPlugin
```

## prefabsPlugin — const @experimental
```
PrefabsPlugin
```

## preloadNestedPrefabs — function @experimental
```
(prefab: PrefabData, loadPrefab: (path: string) => Promise<PrefabData>, cache: Map<string, PrefabData>, visited?: Set<string>, depth?: number): Promise<void>
```

## previewPlugin — function @experimental
```
(sceneUrl: string, baseUrl?: string): PreviewPlugin
```

## probeRegistrations — function @experimental
```
(register: () => Promise<void>): Promise<ProbedRegistrations>
```

## projectSideModuleIds — function @experimental
```
(): string[]
```

## px — const @public
```
(n: number) => Dimension
```

## quat — const @experimental
```
(w?: number, x?: number, y?: number, z?: number) => Quat
```

## quaternionToAngle2D — function @experimental
```
(rz: number, rw: number): number
```

## radiusInterest — function @experimental
```
(radius: number, options?: RadiusInterestOptions): InterestPolicy
```

## readColliderShapes — function @experimental
```
(world: World, entity: number): ColliderInstance[]
```

## readFieldPath — function @experimental
```
(data: Record<string, unknown>, path: string): unknown
```

## readPtrField — function @experimental
```
(f32: Float32Array, u32: Uint32Array, u8: Uint8Array, ptr: number, field: PtrFieldDesc): unknown
```

## rebuildChildren — function @experimental
```
(entities: ProcessedEntity[]): void
```

## recoverDevice — function @experimental
```
(): boolean
```

## reflectEsshader — function @experimental
```
(source: string): ShaderReflection
```

## registerAction — function @experimental
```
(name: string, fn: AiAction<AiContext>): void
```

## registerAnimatorController — function @experimental
```
(key: string, def: AnimatorControllerDef): void
```

## registerAssetBuildTransform — function @experimental
```
(editorType: EditorAssetType, transform: AssetBuildTransform): void
```

## registerBt — function @experimental
```
(key: string, def: BtDefinition): BtDefinition
```

## registerCensusProbe — function @experimental
```
(probe: CensusProbe): () => void
```

## registerComponent — function @experimental
```
(name: string, def: AnyComponentDef): void
```

## registerCondition — function @experimental
```
(name: string, fn: AiCondition<AiContext>): void
```

## registerDrawCallback — function @experimental
```
(id: string, fn: DrawCallback, scene?: string): void
```

## registerFsm — function @experimental
```
(key: string, def: FsmDefinition): CompiledFsm
```

## registerPackagedSideModules — function @experimental
```
(config: Pick<PackagedGameConfig, "sideModules">): void
```

## registerPreSceneDrawCallback — function @experimental
```
(id: string, fn: PreSceneDrawCallback): void
```

## registerSceneComponentCodec — function @experimental
```
(type: string, codec: SceneComponentCodec): void
```

## registerSideModule — function @experimental
```
(id: string, descriptor: SideModuleDescriptor): void
```

## registerTilemapSource — function @experimental
```
(path: string, data: LoadedTilemapSource): void
```

## registerTimelineAsset — function @experimental
```
(path: string, asset: TimelineAsset): void
```

## remapComponentEntityRefs — function @experimental
```
(components: ComponentData[], idMapping: Map<PrefabEntityId, number>): void
```

## remapEntityFields — function @experimental
```
(compData: SceneComponentData, entityMap: Map<number, Entity>): void
```

## removeAnimatorParam — function @experimental
```
(def: AnimatorControllerDef, name: string): AnimatorControllerDef
```

## removeAnimatorState — function @experimental
```
(def: AnimatorControllerDef, name: string): AnimatorControllerDef
```

## removeAnimatorTransition — function @experimental
```
(def: AnimatorControllerDef, from: string, index: number): AnimatorControllerDef
```

## removeBtNode — function @experimental
```
(def: BtDefinition, id: string): BtDefinition
```

## removeCacheForEntity — function @experimental
```
(entityId: number): BitmapCache | undefined
```

## removeNode — function @beta
```
(g: MaterialGraph, id: string): MaterialGraph
```

## removeState — function @experimental
```
(def: FsmDefinition, name: string): FsmDefinition
```

## removeTransition — function @experimental
```
(def: FsmDefinition, from: string, index: number): FsmDefinition
```

## renameAnimatorState — function @experimental
```
(def: AnimatorControllerDef, oldName: string, newName: string): AnimatorControllerDef
```

## renameState — function @experimental
```
(def: FsmDefinition, oldName: string, newName: string): FsmDefinition
```

## reparentBtNode — function @experimental
```
(def: BtDefinition, id: string, newParentId: string): BtDefinition
```

## replicationPlugin — const @experimental
```
ReplicationPlugin
```

## reportDeviceLost — function @experimental
```
(reason: number, message: string): void
```

## requireResourceManager — function @experimental
```
(): CppResourceManager
```

## resetWorldTo — function @experimental
```
(world: World, sceneData: SceneData): Map<number, Entity>
```

## resolveAssetGroup — function @experimental
```
(projectRelPath: string, config: AssetGroupsConfig | null | undefined): ResolvedAssetGroup
```

## resolveAtlas — function @experimental
```
(projectRelPath: string, config: AssetGroupsConfig | null | undefined): ResolvedAtlas | null
```

## resolveAutotile — function @experimental
```
(index: TerrainIndex, neighbors: readonly boolean[]): number
```

## resolveBindingTarget — function @experimental
```
(world: World, from: Entity, name: string): Entity | null
```

## resolveChildEntity — function @experimental
```
(world: Pick<World, "tryGet">, rootEntity: Entity, childPath: string): Entity | null
```

## resolveParams — function @experimental
```
(def: AnimatorControllerDef, overrides: ReadonlyMap<string, number | boolean>): Record<string, number | boolean>
```

## resolveRelativePath — function @experimental
```
(basePath: string, relativePath: string): string
```

## resolveThemeTokens — function @experimental
```
(base: "dark" | "light", overrides?: ThemeOverrides): ThemeTokens
```

## resolveTilesetModel — function @experimental
```
(tilesets: ResolvedTileset[]): TilesetModel
```

## resolveWang — function @experimental
```
(index: WangIndex, corners: readonly number[]): number
```

## rotateFlagsCW — function @experimental
```
(flags: TileFlags): TileFlags
```

## rotateStampCW — function @experimental
```
(s: TileStamp): TileStamp
```

## runHeadless — function @beta
```
(app: App, options?: { fps?: number; }): () => void
```

## safeAreaPlugin — const @experimental
```
SafeAreaPlugin
```

## sampleTimeline — function @experimental
```
(asset: TimelineAsset, time: number, rootEntity: Entity, deps: SampleDeps, opts?: SampleOptions): void
```

## sampleTimelineInWorld — function @experimental
```
(asset: TimelineAsset, time: number, world: SampleWorld & Pick<World, "tryGet">, rootEntity: Entity, opts?: SampleOptions): void
```

## scalar — const @experimental
```
{ readonly clamp: (x: number, min: number, max: number) => number; readonly clamp01: (x: number) => number; readonly lerp: (a: number, b: number, t: number) => number; readonly inverseLerp: (a: number, b: number, value: number) => number; readonly remap: (value: number, inMin: number, inMax: number, outMin: number, outMax: number) => number; readonly deg2rad: (deg: number) => number; readonly rad2deg: (rad: number) => number; readonly approximately: (a: number, b: number, epsilon?: number) => boolean; readonly smoothstep: (edge0: number, edge1: number, x: number) => number; readonly mod: (x: number, n: number) => number; readonly sign: (x: number) => number; }
```

## sceneManagerPlugin — const @experimental
```
Plugin
```

## sceneUsesI18n — function @experimental
```
(sceneData: SceneData): boolean
```

## screenToUiWorld — function @experimental
```
(camera: UICameraData, screenGLX: number, screenGLY: number): { x: number; y: number; }
```

## screenToWorld — function @experimental
```
(screenX: number, screenY: number, inverseVP: Float32Array, vpX: number, vpY: number, vpW: number, vpH: number, planeZ?: number): { x: number; y: number; }
```

## selectBlendClip — function @experimental
```
(blend: AnimatorBlend1D, value: number): AnimatorBlendThreshold
```

## selectPluralForm — function @experimental
```
(forms: PluralForms, count: number, selector: PluralSelector): string
```

## senseTarget — function @experimental
```
(ox: number, oy: number, facing: number, tx: number, ty: number, range: number, halfFov: number, isBlocked?: (ox: number, oy: number, tx: number, ty: number) => boolean): SenseResult
```

## serializeAnimClip — function @experimental
```
(asset: AnimClipAssetData): Record<string, unknown>
```

## serializeScene — function @experimental
```
(world: World, sceneName?: string): SceneData
```

## serializeTileset — function @experimental
```
(asset: TilesetAsset): Record<string, unknown>
```

## serializeTimelineAsset — function @experimental
```
(asset: TimelineAsset): Record<string, unknown>
```

## serializeTimelineToJson — function @experimental
```
(asset: TimelineAsset, indent?: number): string
```

## servicesPlugin — const @experimental
```
ServicesPlugin
```

## setAnimatorConditions — function @experimental
```
(def: AnimatorControllerDef, from: string, index: number, conditions: AnimatorCondition[]): AnimatorControllerDef
```

## setAnimatorInitial — function @experimental
```
(def: AnimatorControllerDef, name: string): AnimatorControllerDef
```

## setAnimatorStateClip — function @experimental
```
(def: AnimatorControllerDef, name: string, clip: string): AnimatorControllerDef
```

## setAnimatorStateProps — function @experimental
```
(def: AnimatorControllerDef, name: string, patch: { speed?: number; loop?: boolean; }): AnimatorControllerDef
```

## setBtNodeField — function @experimental
```
(def: BtDefinition, id: string, patch: Partial<BtNode>): BtDefinition
```

## setButtonState — function @experimental
```
(world: World, entity: Entity, state: string): void
```

## setCacheForEntity — function @experimental
```
(entityId: number, cache: BitmapCache): void
```

## setControllerPage — function @experimental
```
(world: World, entity: Entity, name: string, page: string): boolean
```

## setDefaultContext — function @experimental
```
(ctx: AppContext): void
```

## setEditorMode — function @experimental
```
(active: boolean): void
```

## setEditorViewHalfHeight — function @experimental
```
(view: EditorViewData, halfH: number): void
```

## setEntityActive — function @experimental
```
(world: World, entity: Entity, active: boolean): void
```

## setEntityVisible — function @experimental
```
(world: World, entity: Entity, visible: boolean): void
```

## setFxEditPreview — function @experimental
```
(enabled: boolean): void
```

## setInitial — function @experimental
```
(def: FsmDefinition, name: string): FsmDefinition
```

## setLogLevel — function @experimental
```
(level: LogLevel): void
```

## setNavDestination — function @experimental
```
(world: World, entity: Entity, target: Vec2): void
```

## setPlayMode — function @experimental
```
(active: boolean): void
```

## setStateHook — function @experimental
```
(def: FsmDefinition, name: string, hook: "onEnter" | "onUpdate" | "onExit", action: string, arg?: string, params?: Record<string, AiParamValue>): FsmDefinition
```

## setTextureBudget — function @experimental
```
(bytes: number): void
```

## setTextureFilter — function @experimental
```
(textureId: number, filter: TextureFilter): void
```

## setTextureParams — function @experimental
```
(textureId: number, minFilter: TextureFilter, magFilter: TextureFilter, wrapS: TextureWrap, wrapT: TextureWrap): void
```

## setTextureSliceBorder — function @experimental
```
(textureId: number, left: number, right: number, top: number, bottom: number): void
```

## setTextureWrap — function @experimental
```
(textureId: number, wrap: TextureWrap): void
```

## setTheme — function @experimental
```
(tokens: ThemeTokens): void
```

## setUIVisible — function @experimental
```
(world: World, entity: Entity, visible: boolean): void
```

## setViewTarget — function @experimental
```
(host: CameraDirectorHost, entity: number, opts?: { time?: number; curve?: number; }): void
```

## setWasmErrorHandler — function @experimental
```
(handler: WasmErrorHandler | null): void
```

## shakeCamera — function @experimental
```
(host: CameraDirectorHost, opts?: { amplitude?: number; rotation?: number; frequency?: number; duration?: number; }): void
```

## shapeCenter — function @experimental
```
(shape: ColliderShape, worldPos: Vec2, angle: number, ppu: number): Vec2
```

## shapeOffset — function @experimental
```
(shape: ColliderShape): Vec2
```

## shutdownDrawAPI — function @experimental
```
(): void
```

## shutdownGLDebugAPI — function @experimental
```
(): void
```

## shutdownGeometryAPI — function @experimental
```
(): void
```

## shutdownMaterialAPI — function @experimental
```
(): void
```

## shutdownRendererAPI — function @experimental
```
(): void
```

## shutdownResourceManager — function @experimental
```
(): void
```

## sideModuleDescriptor — function @experimental
```
(id: SideModuleId): SideModuleDescriptor | undefined
```

## signal — function @experimental
```
<T>(initial: T): Signal<T>
```

## singleStamp — function @experimental
```
(raw: number): TileStamp
```

## spawnUIEntity — function @beta
```
(init: UIEntityInit): Entity
```

## spineModuleId — function @experimental
```
(version: SpineVersion): SideModuleId
```

## stackOf — function @experimental
```
(error: unknown): string | undefined
```

## statsPlugin — const @experimental
```
StatsPlugin
```

## stepFsm — function @experimental
```
<Ctx>(fsm: CompiledFsm, run: FsmRunState, ctx: Ctx, bb: Blackboard, registry: AiRegistry<Ctx>): boolean
```

## stepPerception — function @experimental
```
(world: PerceptionWorldView, isBlocked?: LosCheck): void
```

## stopNavAgent — function @experimental
```
(world: World, entity: Entity): void
```

## switchTheme — function @experimental
```
(world: World, tokens: ThemeTokens): void
```

## takeCensus — function @experimental
```
(ctx?: CensusContext): Census
```

## textInputPlugin — const @experimental
```
TextInputPlugin
```

## textPlugin — const @experimental
```
TextPlugin
```

## textureImportSettingsFrom — function @experimental
```
(importer: Record<string, unknown> | undefined | null): ParsedTextureImportSettings | undefined
```

## themeButtonStates — function @experimental
```
(): Record<string, ButtonStateVisual>
```

## themeColors — function @beta
```
(): ThemeColors
```

## themeType — function @experimental
```
(): ThemeType
```

## tickBt — function @experimental
```
<Ctx>(def: BtDefinition, ctx: Ctx, bb: Blackboard, registry: AiRegistry<Ctx>, rs: BtRunState, dt: number): Status
```

## tileCellCenter — function @experimental
```
(p: TileGridParams, tx: number, ty: number): Vec2Like
```

## tileCellOutline — function @experimental
```
(p: TileGridParams): Vec2Like[]
```

## tileCollisionOutlines — function @experimental
```
(chunks: DecodedChunk[], model: TilesetModel, tileW: number, tileH: number, originX: number, originY: number): TileCollisionPiece[]
```

## tileFlagsOf — function @experimental
```
(raw: number): TileFlags
```

## tileIdOf — function @experimental
```
(raw: number): number
```

## tilemapPlugin — const @experimental
```
TilemapPlugin
```

## timelinePlugin — const @experimental
```
TimelinePlugin
```

## timerPlugin — const @experimental
```
Plugin
```

## toBuildPath — function @experimental
```
(path: string): string
```

## trailPlugin — const @experimental
```
TrailPlugin
```

## transitionTo — function @beta
```
(host: App | SceneManagerState, targetScene: string, config: TransitionConfig): Promise<void>
```

## trimTextureCache — function @experimental
```
(): number
```

## uiHitTestWorld — function @experimental
```
(world: PickableWorld, worldX: number, worldY: number, mouseDown?: boolean, mousePressed?: boolean, mouseReleased?: boolean): Entity | null
```

## uiInteractionPlugin — const @experimental
```
UIInteractionPlugin
```

## uiLayoutPlugin — const @experimental
```
UILayoutPlugin
```

## uiLayoutRect — function @experimental
```
(cam: CameraExtents, fit: CanvasScale | null, width: number, height: number, previewAspect?: number): WorldRect
```

## uiMaskPlugin — const @experimental
```
UIMaskPlugin
```

## uiPickAllWorld — function @experimental
```
(world: PickableWorld, worldX: number, worldY: number): Entity[]
```

## uiPickWorld — function @experimental
```
(world: PickableWorld, worldX: number, worldY: number): Entity | null
```

## uiPlugin — const @experimental
```
UIPlugin
```

## uiPlugins — const @experimental
```
Plugin[]
```

## uiRenderOrderPlugin — const @experimental
```
UIRenderOrderPlugin
```

## uiVisibilityPlugin — const @experimental
```
UIVisibilityPlugin
```

## uiWorldToScreen — function @experimental
```
(camera: UICameraData, worldX: number, worldY: number): { x: number; y: number; }
```

## unregisterComponent — function @experimental
```
(name: string): void
```

## unregisterDrawCallback — function @experimental
```
(id: string): void
```

## unregisterPreSceneDrawCallback — function @experimental
```
(id: string): void
```

## updateAnimatorParam — function @experimental
```
(def: AnimatorControllerDef, name: string, patch: Partial<AnimatorParam>): AnimatorControllerDef
```

## updateAnimatorTransition — function @experimental
```
(def: AnimatorControllerDef, from: string, index: number, patch: Partial<AnimatorTransition>): AnimatorControllerDef
```

## updateCameraAspectRatio — function @experimental
```
(world: World, aspectRatio: number): void
```

## updateTransition — function @experimental
```
(def: FsmDefinition, from: string, index: number, patch: Partial<FsmTransition>): FsmDefinition
```

## usesStagger — function @experimental
```
(orientation: number): boolean
```

## v2 — const @experimental
```
{ readonly create: (x?: number, y?: number) => Vec2; readonly clone: (a: Vec2) => Vec2; readonly add: (a: Vec2, b: Vec2) => Vec2; readonly sub: (a: Vec2, b: Vec2) => Vec2; readonly scale: (a: Vec2, s: number) => Vec2; readonly mul: (a: Vec2, b: Vec2) => Vec2; readonly neg: (a: Vec2) => Vec2; readonly dot: (a: Vec2, b: Vec2) => number; readonly cross: (a: Vec2, b: Vec2) => number; readonly len: (a: Vec2) => number; readonly len2: (a: Vec2) => number; readonly dist: (a: Vec2, b: Vec2) => number; readonly dist2: (a: Vec2, b: Vec2) => number; readonly normalize: (a: Vec2) => Vec2; readonly lerp: (a: Vec2, b: Vec2, t: number) => Vec2; readonly angle: (a: Vec2) => number; readonly fromAngle: (radians: number, length?: number) => Vec2; readonly rotate: (a: Vec2, radians: number) => Vec2; readonly perp: (a: Vec2) => Vec2; readonly equals: (a: Vec2, b: Vec2, epsilon?: number) => boolean; }
```

## v3 — const @experimental
```
{ readonly create: (x?: number, y?: number, z?: number) => Vec3; readonly clone: (a: Vec3) => Vec3; readonly add: (a: Vec3, b: Vec3) => Vec3; readonly sub: (a: Vec3, b: Vec3) => Vec3; readonly scale: (a: Vec3, s: number) => Vec3; readonly mul: (a: Vec3, b: Vec3) => Vec3; readonly neg: (a: Vec3) => Vec3; readonly dot: (a: Vec3, b: Vec3) => number; readonly cross: (a: Vec3, b: Vec3) => Vec3; readonly len: (a: Vec3) => number; readonly len2: (a: Vec3) => number; readonly dist: (a: Vec3, b: Vec3) => number; readonly normalize: (a: Vec3) => Vec3; readonly lerp: (a: Vec3, b: Vec3, t: number) => Vec3; readonly equals: (a: Vec3, b: Vec3, epsilon?: number) => boolean; }
```

## validateOverrides — function @experimental
```
(prefab: PrefabData, options?: { instanceOverrides?: readonly PrefabOverride[]; loadPrefab?: (path: string) => PrefabData | null; }): ValidateResult
```

## validatePrefab — function @experimental
```
(prefab: PrefabData, options?: ValidatePrefabOptions): PrefabDiagnostic[]
```

## vec2 — const @experimental
```
(x?: number, y?: number) => Vec2
```

## vec3 — const @experimental
```
(x?: number, y?: number, z?: number) => Vec3
```

## vec4 — const @experimental
```
(x?: number, y?: number, z?: number, w?: number) => Vec4
```

## velocityPlugin — const @experimental
```
Plugin
```

## velocitySystem — const @experimental
```
SystemDef
```

## videoPlugin — const @experimental
```
VideoPlugin
```

## warn — function @experimental
```
(category: string, message: string, data?: unknown): void
```

## widgetToPrefab — function @experimental
```
(build: (world: World) => Entity, name: string): PrefabData
```

## withActiveRemoteRoot — function @experimental
```
(config: AssetGroupsConfig | null | undefined, remoteRoot: string): AssetGroupsConfig
```

## withFolderAlwaysInclude — function @experimental
```
(config: AssetGroupsConfig | null | undefined, folder: string, on: boolean): AssetGroupsConfig
```

## withFolderGroup — function @experimental
```
(config: AssetGroupsConfig | null | undefined, folder: string, mode: AssetGroupMode): AssetGroupsConfig
```

## wrapSceneSystem — function @experimental
```
(app: App, sceneName: string, system: SystemDef): SystemDef
```

## writeFieldPath — function @experimental
```
(data: Record<string, unknown>, path: string, value: unknown): boolean
```

## writePtrField — function @experimental
```
(f32: Float32Array, u32: Uint32Array, u8: Uint8Array, ptr: number, field: PtrFieldDesc, value: PtrFieldValue | unknown): void
```

## wxFileExists — function @experimental
```
(path: string): Promise<boolean>
```

## wxFileExistsSync — function @experimental
```
(path: string): boolean
```

## wxGetImagePixels — function @experimental
```
(img: MiniGameImage): ImageLoadResult
```

## wxLoadImage — function @experimental
```
(path: string): Promise<MiniGameImage>
```

## wxLoadImagePixels — function @experimental
```
(path: string): Promise<ImageLoadResult>
```

## wxReadFile — function @experimental
```
(path: string): Promise<ArrayBuffer>
```

## wxReadTextFile — function @experimental
```
(path: string, encoding?: "utf8" | "utf-8"): Promise<string>
```

## wxWriteFile — function @experimental
```
(path: string, data: string | ArrayBuffer): Promise<void>
```

