// Declare your components here with defineComponent / defineTag. The editor
// imports only this module to read component schemas — keep startup code out.
// A 3D scene's own components come with the engine; this is for yours.
export {};
