// Game entry — runs when the scene plays. Register systems here, e.g.
// addSystemToSchedule(Schedule.Update, mySystem).
//
// IDE says "cannot find module 'esengine'"? Open this project in the Estella
// editor once — it stages the SDK types under .esengine/sdk (which tsconfig's
// `paths` points at). The folder is editor-managed and gitignored, so a fresh
// clone needs one editor open to regenerate it.
import './components';
